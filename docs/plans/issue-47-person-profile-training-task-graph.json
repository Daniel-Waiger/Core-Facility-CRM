{
  "objective": "Implement GitHub issue #47 (person profile screen with personal details, projects, recent bookings and instrument training/permission records) in C:\\Users\\Owner\\repos\\cfc-47 without bumping any version: additive person_instrument_training table + people.mobile/people.campus, a #/person/N route rendered by Views.personDetail, add/edit/remove training dialog honouring the retired-selectable rule, Trained Users on the Instruments list and the stewardship scorecard (screen, custom report, XLSX, Notes), a per-person Activity Certificate XLSX export, demo seed rows in one fenced block, CHANGELOG bullets under '## [Unreleased]' ending with '(#47)', a manual callout update, and a unit test file that proves each rule under TZ='Asia/Jerusalem'.",
  "assumptions": [
    "The worktree C:\\\\Users\\\\Owner\\\\repos\\\\cfc-47 is clean at 4f5214e (branch created from origin/main). NEVER run git reset, git checkout --, git stash or git clean: tasks run one after another with no commits in between, so any reset wipes the previous tasks’ uncommitted work. Do not commit; the orchestrator commits after the run.",
    "`python3` does not exist on this machine; nothing here needs it. Node 24 is available and `TZ='Asia/Jerusalem' node --test 'test/unit/*.test.js'` is the meaningful test command (pass the glob, never the directory).",
    "Existing people columns are reused for affiliation: `organization` is labelled 'Lab / Group / Company' and `department` is 'Department'. Only `mobile` and `campus` are added; no `institute` column is invented and `department` already exists so it is NOT re-added.",
    "Training levels are a stored vocabulary added to js/consts.js as `TRAINING_LEVELS: ['User', 'Super User']`, stored verbatim like PERSON_TYPES; the seed and dialog use these exact strings. Editing consts.js is allowed as long as APP_VERSION is untouched.",
    "A training record is 'valid as of date D' when (trained_on = '' OR trained_on <= D) AND (expires_on = '' OR expires_on IS NULL OR expires_on >= D). This single rule lives in db.js (`DB.trainingActiveOn` + `DB.trainedUserCountsAsOf`) and is read by views, reports and exports — never re-derived. For an unbounded range end, D = UI.today().",
    "Training records are plain records (like milestones), so the dialog offers a real Remove with a danger confirm dialog; history preservation applies to the person/instrument they reference, which stay retire-only and now count training rows as references (trainer_id is a SET NULL soft link and is NOT counted, matching how instrument_staff is treated).",
    "CHANGELOG.md currently has no '## [Unreleased]' heading; T8 creates it directly above '## [1.11.0] — 2026-09-12'. test/unit/versions.test.js's heading regex requires digits, so the Unreleased heading does not break it. docs/index.html's footer-version regex would print 'vUnreleased' from a fetched changelog; that is a hand-merge/release concern and is intentionally not touched here.",
    "The People list navigates to the profile with the existing `data-goto=\"person\" data-id` mechanism (the delegated click handler routes data-goto before data-act), so no new dispatcher case is needed for opening a profile; `hashFor`/`parseHash` must learn the 'person' name the way they special-case 'project'.",
    "No browser is guaranteed. Verification is node --check, the unit suite, grep/awk structure checks and node scripts that boot the real schema through test/unit/helpers (freshDb/loadApp/app-harness). Executors should still state plainly that the screens were not rendered in a browser if they were not.",
    "Research outputs per person (issue #41) are out of scope; no output_people table or column is added anywhere."
  ],
  "tasks": [
    {
      "id": "T1",
      "title": "db.js + consts.js: training table, people.mobile/campus, ref counts, clear, shared validity rule",
      "scope": "Files: C:\\Users\\Owner\\repos\\cfc-47\\js\\db.js and C:\\Users\\Owner\\repos\\cfc-47\\js\\consts.js. (1) consts.js: add `TRAINING_LEVELS: ['User', 'Super User']` to the window.CONST object next to PERSON_TYPES; do NOT touch APP_VERSION. (2) db.js SCHEMA string: add `mobile TEXT DEFAULT ''` and `campus TEXT DEFAULT ''` to the `people` CREATE TABLE; after the `project_outputs` CREATE TABLE add `CREATE TABLE IF NOT EXISTS person_instrument_training (id INTEGER PRIMARY KEY AUTOINCREMENT, person_id INTEGER NOT NULL REFERENCES people(id) ON DELETE CASCADE, instrument_id INTEGER NOT NULL REFERENCES instruments(id) ON DELETE CASCADE, level TEXT NOT NULL DEFAULT 'User', trained_on TEXT DEFAULT '', trainer_id INTEGER REFERENCES people(id) ON DELETE SET NULL, expires_on TEXT DEFAULT '', note TEXT DEFAULT '', created_at TEXT NOT NULL DEFAULT (datetime('now')));` plus `CREATE INDEX IF NOT EXISTS ix_pit_person ON person_instrument_training(person_id);` and `ix_pit_instrument ON person_instrument_training(instrument_id)`. (3) migrate(): append ONE contiguous block as the very last statements of the function (after the project_outputs try block, before the closing `}`), fenced exactly by the lines `// --- #47 begin ---` and `// --- #47 end ---`, containing: `try { db.exec(\"ALTER TABLE people ADD COLUMN mobile TEXT DEFAULT ''\"); } catch (_) {}`, same for campus, and a `try { db.exec(\\`CREATE TABLE IF NOT EXISTS person_instrument_training (...identical DDL...); CREATE INDEX IF NOT EXISTS ...\\`); } catch (_) {}`. Do not add department (already exists). (4) clearAllData(): add `DELETE FROM person_instrument_training;` before `DELETE FROM people;` and add `'person_instrument_training'` to the sqlite_sequence name list. (5) countPersonRefs: add `(SELECT COUNT(*) FROM person_instrument_training WHERE person_id=?) AS training` (trainer_id deliberately NOT counted — comment why, mirroring the instrument_staff comment), include `training` in parts and total; countInstrumentRefs: same with instrument_id. (6) New exported helpers: `trainingActiveOn(rec, asOf)` pure function implementing (trained_on='' OR trained_on<=asOf) AND (expires_on blank/null OR expires_on>=asOf); `trainedUserCountsAsOf(asOf)` returning rows `{instrument_id, trained_users}` = COUNT(DISTINCT person_id) per instrument under that same rule in SQL (asOf bound as a parameter; caller passes UI.today() when the range end is blank); `listPersonTraining(personId)` returning training rows LEFT JOINed to instruments (instrument_name, instrument_retired) and trainer people (trainer_name, trainer_retired) ORDER BY trained_on DESC, id DESC; `listInstrumentTraining(instrumentId)` returning rows joined to people (person_name, person_retired) ORDER BY person name. Add all four to the `global.DB = {...}` export. Do not modify hasAnyDataLocal, seedBooking, or any seed data in this task. Dates never via toISOString().slice.",
      "depends_on": [],
      "parallel_safe": false,
      "verification": "From C:\\Users\\Owner\\repos\\cfc-47: `node --check js/db.js && node --check js/consts.js` exit 0. `grep -c '// --- #47 begin ---' js/db.js` prints 1 and `awk '/\\/\\/ --- #47 end ---/{e=NR} /^  function migrate\\(\\) \\{/{s=NR} END{print (e>s)}' js/db.js` prints 1, and the `// --- #47 end ---` line is followed (ignoring blank lines) by the `  }` that closes migrate (check with `awk` or by reading). `grep -c \"TRAINING_LEVELS: \\['User', 'Super User'\\]\" js/consts.js` prints 1. Node script (write to the scratchpad, run with `TZ='Asia/Jerusalem' node <script>`): `const {freshDb, seedFixture} = require('C:/Users/Owner/repos/cfc-47/test/unit/helpers/sqlite.js'); (async()=>{const {DB}=await freshDb(); seedFixture(DB); const cols=DB.rows('PRAGMA table_info(people)').map(c=>c.name); assert(cols.includes('mobile')&&cols.includes('campus')); DB.run(\"INSERT INTO person_instrument_training (person_id, instrument_id, level, trained_on, trainer_id, expires_on) VALUES (1,1,'Super User','2026-01-10',2,'')\"); assert.equal(DB.countPersonRefs(1).training,1); assert.ok(DB.countPersonRefs(1).total>=3); assert.equal(DB.countInstrumentRefs(1).training,1); assert.equal(DB.countPersonRefs(2).training,0); assert.equal(DB.trainedUserCountsAsOf('2026-06-01').find(r=>r.instrument_id===1).trained_users,1); DB.run(\"UPDATE person_instrument_training SET expires_on='2026-05-01'\"); assert.equal((DB.trainedUserCountsAsOf('2026-06-01').find(r=>r.instrument_id===1)||{trained_users:0}).trained_users,0); assert.equal(DB.trainingActiveOn({trained_on:'2026-01-10',expires_on:'2026-05-01'},'2026-05-01'),true); assert.equal(DB.trainingActiveOn({trained_on:'2026-01-10',expires_on:'2026-05-01'},'2026-05-02'),false); assert.equal(DB.listPersonTraining(1)[0].instrument_name,'Scope A'); assert.equal(DB.listPersonTraining(1)[0].trainer_name,'Sam'); DB.run('DELETE FROM people WHERE id=2'); assert.equal(DB.row('SELECT trainer_id t FROM person_instrument_training').t,null); await DB.clearAllData(); assert.equal(DB.row('SELECT COUNT(*) c FROM person_instrument_training').c,0); console.log('T1 OK');})()` prints `T1 OK`. Finally `TZ='Asia/Jerusalem' node --test 'test/unit/*.test.js'` reports 0 failures."
    },
    {
      "id": "T2",
      "title": "app.js: retire/delete paths count and clean training rows; Mobile and Campus on person dialogs",
      "scope": "File: C:\\Users\\Owner\\repos\\cfc-47\\js\\app.js only. (1) retirePerson zero-ref branch: inside the existing DB.transaction, before `DELETE FROM people`, add `DB.run('DELETE FROM person_instrument_training WHERE person_id=?', [id]);` and `DB.run('UPDATE person_instrument_training SET trainer_id=NULL WHERE trainer_id=?', [id]);` (comment: trainer_id is a SET NULL soft link not counted by countPersonRefs, same belt-and-suspenders convention). In the retire (non-zero) message list add `if (refs.training) where.push('trained on ' + plural(refs.training, 'instrument'));`. Also update the zero-ref confirm body so it reads \"isn't referenced by any project, milestone, booking or training record\". (2) retireInstrument zero-ref branch: add `DB.run('DELETE FROM person_instrument_training WHERE instrument_id=?', [id]);` inside the transaction; message `if (refs.training) where.push('on ' + plural(refs.training, 'training record'));`; confirm body mentions training records. (3) addPerson modal: after the Department/Email grid add `<div class=\"grid cols-2\">` with `<div class=\"field\"><label>Mobile</label><input type=\"tel\" class=\"input\" id=\"p-mobile\" placeholder=\"Optional\" /></div>` and `<div class=\"field\"><label>Campus</label><input class=\"input\" id=\"p-campus\" placeholder=\"e.g. Main campus\" /></div>`. editPerson: same with ids `pe-mobile`/`pe-campus` and `value=\"${esc(p.mobile || '')}\"`, `value=\"${esc(p.campus || '')}\"`. (4) pSave: read `#p-mobile`/`#p-campus` (trim) and extend the INSERT to `(name, type, organization, department, email, note, is_staff, rate, mobile, campus)`; pEditSave: read `#pe-mobile`/`#pe-campus` and extend the UPDATE with `mobile=?, campus=?`. Labels are Title Case; placeholders sentence case. Do not add dispatcher cases or routes in this task (T4 does). Keep using UI.topModal().",
      "depends_on": [
        "T1"
      ],
      "parallel_safe": true,
      "verification": "`node --check js/app.js` exit 0. `grep -c 'person_instrument_training' js/app.js` prints at least 3. `grep -c 'id=\"pe-mobile\"' js/app.js`, `grep -c 'id=\"p-mobile\"' js/app.js`, `grep -c 'id=\"pe-campus\"' js/app.js`, `grep -c 'id=\"p-campus\"' js/app.js` each print 1. `grep -c 'mobile, campus) VALUES' js/app.js` prints 1 and `grep -c 'mobile=?, campus=? WHERE id=?' js/app.js` prints 1. Node script run with `TZ='Asia/Jerusalem' node <script>` using `const {freshApp}=require('C:/Users/Owner/repos/cfc-47/test/unit/helpers/app-harness.js'); const {seedFixture}=require('C:/Users/Owner/repos/cfc-47/test/unit/helpers/sqlite.js');`: (a) `app=await freshApp({confirm:true}); seedFixture(app.DB); app.DB.run(\"INSERT INTO people (name,type) VALUES ('Zed','PhD')\"); zed=app.DB.row('SELECT last_insert_rowid() id').id; app.DB.run('INSERT INTO person_instrument_training (person_id, instrument_id, level, trained_on) VALUES (?,1,\\'User\\',\\'2026-02-01\\')',[zed]); await app.internals.retirePerson(zed);` then assert `people.is_retired=1` for zed and the training row still exists (a referenced person is retired, never deleted). (b) `app.DB.run(\"INSERT INTO people (name,type) VALUES ('Solo','PI')\"); solo=lastId; app.DB.run('UPDATE person_instrument_training SET trainer_id=? WHERE person_id=?',[solo,zed]); await app.internals.retirePerson(solo);` assert Solo row is gone and the training row's trainer_id is NULL. (c) `app.DB.run(\"INSERT INTO instruments (name,status) VALUES ('Spare','Available')\"); sp=lastId; app.DB.run('INSERT INTO person_instrument_training (person_id, instrument_id, level, trained_on) VALUES (1,?,\\'User\\',\\'2026-02-01\\')',[sp]); await app.internals.retireInstrument(sp);` assert instruments row still exists with is_retired=1. Script prints `T2 OK`. `TZ='Asia/Jerusalem' node --test 'test/unit/*.test.js'` reports 0 failures (delete-paths.test.js and wiring.test.js included)."
    },
    {
      "id": "T3",
      "title": "views.js: Views.personDetail renderer, People list profile link, Trained Users column on Instruments",
      "scope": "File: C:\\Users\\Owner\\repos\\cfc-47\\js\\views.js only. (1) New `function personDetail(id)` in the People section, exported in `global.Views`. If `DB.row('SELECT * FROM people WHERE id=?',[id])` is null return `emptyState('users', 'Person not found', 'This person may have been deleted.')`. Otherwise render, in order: a header card — name via `global.UI.retiredName(p.name, p.is_retired)`, a `badge neutral` with p.type, a `Facility Staff` success badge if is_staff, buttons `<button class=\"btn btn-secondary btn-sm\" data-goto=\"people\">Back to People</button>` and `<button class=\"btn btn-primary btn-sm\" data-act=\"edit-person\" data-id=\"${p.id}\">Edit Person</button>`; a 'Personal Details' card as a two-column definition grid with rows Full Name, Email, Mobile, Lab / Group / Company (p.organization), Department, Campus, Rate/hr (only when is_staff, via UI.fmtMoney), Notes — blank values render '—'. (2) 'Projects' card: `SELECT p.id, p.code, p.title, p.status, p.is_archived, CASE WHEN p.pi_id=? THEN 'PI' ELSE COALESCE(pp.role,'') END AS role FROM projects p LEFT JOIN project_people pp ON pp.project_id=p.id AND pp.person_id=? WHERE p.pi_id=? OR pp.person_id IS NOT NULL ORDER BY p.is_archived, p.title` with `<tr class=\"row-link\" data-goto=\"project\" data-id>` rows showing Code, Title, Role, Status (statusBadge), and an 'Archived' neutral badge when is_archived; empty state 'No projects yet'. (3) 'Recent Activity' card: `SELECT m.id, m.title, m.date, m.start_time, m.end_time, m.category, m.is_cancelled, m.billing_retained, m.project_id, pr.title AS project_title, (SELECT GROUP_CONCAT(i.name || CASE WHEN i.is_retired THEN ' (Retired)' ELSE '' END, ', ') FROM meeting_instruments mi JOIN instruments i ON i.id=mi.instrument_id WHERE mi.meeting_id=m.id) AS instruments FROM meetings m JOIN meeting_people mp ON mp.meeting_id=m.id LEFT JOIN projects pr ON pr.id=m.project_id WHERE mp.person_id=? ORDER BY m.date DESC, m.start_time DESC, m.id DESC LIMIT 25`; columns Date (UI.fmtDate), Time (start–end or '—'), Title, Project (project_title or 'Facility-wide'), Instruments, Status: `<span class=\"badge danger\">Cancelled</span>` when is_cancelled (add `row-retired` class to the row), else '—'; footer note in sentence case: 'Showing the 25 most recent bookings this person attended. Cancelled bookings are kept and marked.' (4) 'Instrument Training' card: header row with `<button class=\"btn btn-primary btn-sm\" data-act=\"training-add\" data-person-id=\"${p.id}\">Add Training</button>`; rows from `global.DB.listPersonTraining(p.id)` with columns Instrument (retiredName), Level (badge primary), Trained On (fmtDate or '—'), Trainer (retiredName or '—'), Expires On (fmtDate or 'No expiry') plus `<span class=\"badge warning\">Expired</span>` when `!global.DB.trainingActiveOn(rec, global.UI.today())`, Note, Actions: `<button class=\"btn btn-ghost btn-xs\" data-act=\"training-edit\" data-id=\"${rec.id}\" title=\"Edit Training Record\">${ic('edit')}</button>` and `<button class=\"btn btn-ghost btn-xs\" data-act=\"training-del\" data-id=\"${rec.id}\" title=\"Remove Training Record\">${ic('trash')}</button>`; empty state 'No training records yet' / 'Add the instruments this person has been trained to use.' (5) 'Activity Certificate' card: two date inputs `<input type=\"date\" class=\"input\" id=\"cert-from\" value=\"${global.UI.ymd(new Date(new Date().getFullYear(), 0, 1))}\">` and `<input type=\"date\" class=\"input\" id=\"cert-to\" value=\"${global.UI.today()}\">` with labels 'From' / 'To', and `<button class=\"btn btn-secondary\" data-act=\"export-activity-certificate\" data-id=\"${p.id}\">${ic('file')} Export Activity Certificate</button>`; hint sentence: 'Exports this person's details, training records and the bookings they attended in the range as a spreadsheet.' (6) people(): make each `<tr>` `class=\"row-link ${r.is_retired ? 'row-retired' : ''}\" data-goto=\"person\" data-id=\"${r.id}\"` (mirrors the projects table at views.js ~171; the click handler skips clicks inside [data-act] so the action buttons keep working) and add a first action button `<button class=\"btn btn-ghost btn-xs\" data-goto=\"person\" data-id=\"${r.id}\" title=\"Open Profile\">${ic('chevron')}</button>`; widen the Actions col to 110px. (7) instruments(): add subquery `(SELECT COUNT(DISTINCT t.person_id) FROM person_instrument_training t WHERE t.instrument_id=i.id AND (t.trained_on='' OR t.trained_on<=?) AND (t.expires_on='' OR t.expires_on IS NULL OR t.expires_on>=?)) AS trained_users` bound twice to `global.UI.today()`; add `<th title=\"People with a training record valid today\">Trained Users</th>` after Supervisor(s), a matching `<col>`, and cell `<span class=\"badge neutral\">${r.trained_users}</span>`. All dates via UI.ymd/UI.today/UI.fmtDate; every value through esc().",
      "depends_on": [
        "T1"
      ],
      "parallel_safe": true,
      "verification": "`node --check js/views.js` exit 0. `grep -c 'personDetail,' js/views.js` prints 1 (export). Node script run with `TZ='Asia/Jerusalem' node <script>`: `const path=require('path'); const {loadApp,REPO}=require('C:/Users/Owner/repos/cfc-47/test/unit/helpers/load-module.js'); const {seedFixture}=require('C:/Users/Owner/repos/cfc-47/test/unit/helpers/sqlite.js'); (async()=>{const app=loadApp(['consts','db','ui','views']); globalThis.initSqlJs=require(path.join(REPO,'libs','sql-asm.js')); await app.DB.boot(); const {DB,Views}=app; seedFixture(DB); DB.run(\"UPDATE people SET mobile='050-000', campus='North' WHERE id=1\"); DB.run(\"INSERT INTO person_instrument_training (person_id, instrument_id, level, trained_on, trainer_id, expires_on) VALUES (1,1,'Super User','2026-01-10',2,'2020-01-01')\"); DB.run(\"INSERT INTO meetings (project_id, title, date, start_time, end_time, is_cancelled) VALUES (1,'Cancelled run','2026-03-01','09:00','10:00',1)\"); const mid=DB.row('SELECT last_insert_rowid() id').id; DB.run('INSERT INTO meeting_people (meeting_id, person_id) VALUES (?,1)',[mid]); DB.run('INSERT INTO meeting_instruments (meeting_id, instrument_id) VALUES (?,1)',[mid]); DB.run(\"INSERT INTO meetings (title, date) VALUES ('Facility consult','2026-02-01')\"); const m2=DB.row('SELECT last_insert_rowid() id').id; DB.run('INSERT INTO meeting_people (meeting_id, person_id) VALUES (?,1)',[m2]); const h=Views.personDetail(1); for (const s of ['Alice','050-000','North','Scope A','Super User','Sam','Expired','Cancelled','Facility-wide','Live','Old','Archived','data-act=\"training-add\"','data-act=\"training-edit\"','data-act=\"training-del\"','data-act=\"export-activity-certificate\"','id=\"cert-from\"','id=\"cert-to\"']) assert.ok(h.includes(s), 'missing '+s); assert.ok(h.indexOf('Cancelled run') < h.indexOf('Facility consult'), 'newest first'); assert.ok(Views.personDetail(999).includes('Person not found')); assert.ok(Views.people().includes('data-goto=\"person\"')); const ih=Views.instruments(); assert.ok(ih.includes('Trained Users')); DB.run(\"UPDATE person_instrument_training SET expires_on=''\"); assert.ok(/Trained Users[\\s\\S]*badge neutral\">1</.test(Views.instruments())); console.log('T3 OK');})()` prints `T3 OK`. `TZ='Asia/Jerusalem' node --test 'test/unit/*.test.js'` reports 0 failures (wiring.test.js will fail until T4 adds the dispatcher cases — if T4 has not landed yet, the ONLY acceptable failures are the four new data-act values training-add/training-edit/training-del/export-activity-certificate listed by wiring.test.js, and the executor must state this explicitly)."
    },
    {
      "id": "T4",
      "title": "app.js: #/person/N route, training dialog (add/edit/remove), dispatcher cases in the People block",
      "scope": "File: C:\\Users\\Owner\\repos\\cfc-47\\js\\app.js only (T2 already landed). (1) Routing: add `person: 'Person Profile'` to TITLES; `ctx` gets `person: null`; hashFor: `if (name === 'person' && id) return '#/person/' + Number(id);`; parseHash: `if (parts[0] === 'person') { const id = Number(parts[1]); return id ? { name: 'person', id } : null; }`; applyRoute: `ctx.person = name === 'person' ? Number(id) : null;` (keep ctx.project logic as is); onHashChange: `if (parsed.name === 'person' && !DB.row('SELECT id FROM people WHERE id=?', [parsed.id])) { location.replace('#/people'); return; }`; initRouting: same existence guard for 'person'; renderView chain: add `name === 'person' ? Views.personDetail(ctx.person) :` immediately after the `name === 'project'` line. (2) Dispatcher: inside the `// People CRUD` block of handleAct (after `case 'restore-person'` and before `// Project Collaborators & Instruments link`), add: `case 'training-add': return openTrainingModal(null, el.dataset.personId);` `case 'training-edit': return openTrainingModal(el.dataset.id);` `case 'training-save': return trainingSave(el.dataset.id, el.dataset.personId);` `case 'training-del': return removeTraining(el.dataset.id);` `case 'export-activity-certificate': { const f = document.getElementById('cert-from'), t = document.getElementById('cert-to'); return Exports.exportActivityCertificate(el.dataset.id, f ? f.value : '', t ? t.value : ''); }`. Never append cases at the end of the switch. (3) New functions in the People CRUD section after restorePerson: `openTrainingModal(trainingId, personId)` — loads `cur = trainingId ? DB.row('SELECT * FROM person_instrument_training WHERE id=?')` else null; pid = cur ? cur.person_id : Number(personId); title `${ic('cpu')} Add Training Record` / `Edit Training Record`; instrument `<select class=\"input\" id=\"tr-instrument\">` from `DB.rows('SELECT id, name, is_retired FROM instruments WHERE is_retired=0 OR id=? ORDER BY name', [cur ? cur.instrument_id : -1])` (the 'selectable = not retired OR already selected here' rule; label retired ones via UI.retiredName), placeholder option '-- Select Instrument --'; level `<select id=\"tr-level\">` from C.TRAINING_LEVELS (selected cur.level, default 'User'); `<input type=\"date\" id=\"tr-trained-on\">` default `cur ? cur.trained_on : UI.today()`; trainer `<select id=\"tr-trainer\">` with '-- None --' then `DB.rows('SELECT id, name, is_retired, is_staff FROM people WHERE (is_retired=0 OR id=?) AND id<>? ORDER BY is_staff DESC, name', [cur ? cur.trainer_id : -1, pid])` labelled via UI.retiredName; `<input type=\"date\" id=\"tr-expires-on\">` optional with hint 'Leave blank if the training does not expire.'; `<input id=\"tr-note\">` label 'Note'; footer Cancel + `<button class=\"btn btn-primary\" data-act=\"training-save\" data-id=\"${cur ? cur.id : ''}\" data-person-id=\"${pid}\">Save Training</button>`. `trainingSave(id, personId)`: `const m = UI.topModal();` read fields; if no instrument → `UI.toast('Choose an instrument', 'error')` + is-invalid; if no trained_on → toast 'Training date required'; if expires_on && expires_on < trained_on → toast 'Expiry cannot be before the training date'; trainer_id = Number(value) || null; INSERT (person_id, instrument_id, level, trained_on, trainer_id, expires_on, note) or UPDATE by id; `UI.closeDim(m.closest('.modal-dim')); UI.toast('Training record saved'); refresh();`. `removeTraining(id)`: async; loads row + instrument name; `UI.confirmModal('Remove Training Record', \\`Remove the ${esc(level)} training record on \"${esc(instrumentName)}\"? This cannot be undone.\\`, { danger: true, confirmText: 'Remove' })`; on ok `DB.run('DELETE FROM person_instrument_training WHERE id=?', [id]); UI.toast('Training record removed'); refresh();`. UI text: Title Case for labels/buttons/titles, sentence case for toasts/hints. Date strings come straight from the date inputs or UI.today(); no toISOString.",
      "depends_on": [
        "T2",
        "T3"
      ],
      "parallel_safe": true,
      "verification": "`node --check js/app.js` exit 0. Ordering check: `awk '/\\/\\/ People CRUD/{a=NR} /case .training-save.:/{b=NR} /\\/\\/ Project Collaborators & Instruments link/{c=NR} END{print (a<b && b<c)}' js/app.js` prints 1; same check prints 1 for `case 'export-activity-certificate'`, `case 'training-add'`, `case 'training-edit'`, `case 'training-del'`. `grep -c \"'#/person/'\" js/app.js` prints 1; `grep -c \"person: 'Person Profile'\" js/app.js` prints 1; `grep -c \"Views.personDetail(ctx.person)\" js/app.js` prints 1; `grep -c \"location.replace('#/people')\" js/app.js` prints at least 1. Retired-selectable rule present: `grep -c 'WHERE is_retired=0 OR id=? ORDER BY name' js/app.js` prints 1. `grep -c \"confirmText: 'Remove'\" js/app.js` prints at least 1. `TZ='Asia/Jerusalem' node --test 'test/unit/*.test.js'` reports 0 failures — wiring.test.js in particular must pass (every new data-act has a case, no dead case). Node script using app-harness with a temporary text injection is NOT required here; behaviour of removeTraining/trainingSave is covered in T9."
    },
    {
      "id": "T5",
      "title": "reports.js: Trained Users on the stewardship scorecard (compute, screen, custom-report columns, footnote)",
      "scope": "File: C:\\Users\\Owner\\repos\\cfc-47\\js\\reports.js only. (1) Add a memoized loader next to loadInstrumentSupervisors: `function loadTrainedUserCounts(facts, asOf) { return memo(facts, 'trainedUsers:' + asOf, () => new Map(DB.trainedUserCountsAsOf(asOf).map((r) => [r.instrument_id, r.trained_users]))); }`. (2) computeStewardshipRows: `const asOf = to || UI.today();` (comment: an unbounded range end means 'today'), `const trainedByInstrument = loadTrainedUserCounts(facts, asOf);` and add `trainedUsers: trainedByInstrument.get(r.id) || 0` to each row object. Rewrite the card header comment lines that say trained-user pool is omitted: now only downtime share is omitted; Trained Users is a headcount valid on the range end, not a trend. (3) Screen render: add `<th>Trained Users</th>` right after `<th>Distinct Users</th>` and `<td class=\"mono small\">${r.trainedUsers}</td>` after the distinctUsers cell. Replace the footnote's final sentence 'Omitted on purpose (need Tier 4 data this app doesn't have yet): trained-user pool trend and downtime share.' with: '\"Trained Users\" counts people holding a training record on that instrument that is valid on the last day of the selected range (no expiry, or an expiry on or after that day; \"today\" when the range has no end) — a headcount as of that date, not a trend, and independent of whether they booked in the range. Omitted on purpose (needs downtime records this app doesn't have yet): downtime share.' (4) Custom report `stewardship` entity: in buildRows pass `trainedUsers: r.trainedUsers`; in columns add `ccol('trainedUsers', 'Trained Users', 'number', (r) => r.trainedUsers)` immediately after the distinctUsers ccol. Do NOT add or reorder anything in CUSTOM_REPORT_ENTITY_ORDER and do not add a new entity. (5) Grep js/reports.js for any other 'trained-user' mention and update it consistently.",
      "depends_on": [
        "T1"
      ],
      "parallel_safe": true,
      "verification": "`node --check js/reports.js` exit 0. `grep -c 'trained-user pool' js/reports.js` prints 0. `grep -c \"const CUSTOM_REPORT_ENTITY_ORDER = \\['instrument', 'staff', 'projects', 'consults', 'service', 'stewardship', 'activitymix', 'funnel', 'bookings'\\];\" js/reports.js` prints 1 (unchanged). `grep -c \"ccol('trainedUsers', 'Trained Users', 'number'\" js/reports.js` prints 1; `grep -c '<th>Trained Users</th>' js/reports.js` prints 1. Node script run with `TZ='Asia/Jerusalem' node <script>`: `const path=require('path'); const {loadApp,REPO}=require('C:/Users/Owner/repos/cfc-47/test/unit/helpers/load-module.js'); const {seedFixture}=require('C:/Users/Owner/repos/cfc-47/test/unit/helpers/sqlite.js'); (async()=>{const app=loadApp(['consts','db','ui','views','reports']); globalThis.initSqlJs=require(path.join(REPO,'libs','sql-asm.js')); await app.DB.boot(); const {DB,Reports}=app; seedFixture(DB); DB.run(\"INSERT INTO meetings (project_id, title, date, start_time, end_time) VALUES (1,'Run','2026-03-05','09:00','11:00')\"); const mid=DB.row('SELECT last_insert_rowid() id').id; DB.run('INSERT INTO meeting_instruments (meeting_id, instrument_id) VALUES (?,1)',[mid]); DB.run('INSERT INTO meeting_people (meeting_id, person_id) VALUES (?,1)',[mid]); DB.run(\"INSERT INTO person_instrument_training (person_id, instrument_id, level, trained_on, expires_on) VALUES (1,1,'User','2026-01-01','')\"); DB.run(\"INSERT INTO person_instrument_training (person_id, instrument_id, level, trained_on, expires_on) VALUES (2,1,'Super User','2026-01-01','2026-02-01')\"); DB.run(\"INSERT INTO person_instrument_training (person_id, instrument_id, level, trained_on, expires_on) VALUES (2,1,'User','2025-06-01','2025-12-31')\"); const row=(f,t)=>Reports.computeStewardshipRows(f,t,Reports.makeFacts(f,t)).groups.flatMap(g=>g.rows).find(r=>r.name==='Scope A'); assert.equal(row('2026-01-01','2026-12-31').trainedUsers,1,'Sam expired by year end; Sam counted once at most'); assert.equal(row('2026-01-01','2026-01-15').trainedUsers,2,'both valid mid-January, distinct people not records'); const cols=Reports.getCustomReportColumns('stewardship').map(c=>c.key); assert.ok(cols.includes('trainedUsers')); console.log('T5 OK');})()` prints `T5 OK`. `TZ='Asia/Jerusalem' node --test 'test/unit/*.test.js'` reports 0 failures (aggregation.test.js included)."
    },
    {
      "id": "T6",
      "title": "exports.js: Trained Users in the Stewardship sheet and Notes, plus Exports.exportActivityCertificate",
      "scope": "File: C:\\Users\\Owner\\repos\\cfc-47\\js\\exports.js only. (1) exportReportsXlsx Stewardship sheet: insert 'Trained Users' into the header array right after 'Distinct Users', push `r.trainedUsers` in the same position in each row, and add a `{ wch: 14 }` at the same index in `wsSteward['!cols']`. In the Notes sheet's 'Instrument stewardship scorecard' paragraph replace 'Omitted on purpose (need data this app does not track yet): trained-user pool trend and downtime share.' with '\"Trained Users\" counts people holding a training record on that instrument that is valid on the last day of the exported range (no expiry, or an expiry on or after that day; today when the range has no end) — a headcount as of that date, not a trend. Omitted on purpose (needs downtime records this app does not track yet): downtime share.' (2) New `function exportActivityCertificate(personId, from, to)` placed before `exportCustomXlsx`: `const p = DB.row('SELECT * FROM people WHERE id=?', [Number(personId)]); if (!p) { UI.toast('Person not found', 'error'); return; }` XLSX guard as in exportXlsx; `const asOf = to || UI.today();`. Sheet 'Details' (aoa, 2 cols): ['ACTIVITY CERTIFICATE', ''], ['Full Name', p.name], ['Position / Role', p.type], ['Lab / Group / Company', p.organization || '—'], ['Department', p.department || '—'], ['Campus', p.campus || '—'], ['Email', p.email || '—'], ['Mobile', p.mobile || '—'], ['Facility Staff', p.is_staff ? 'Yes' : 'No'], ['Record Status', p.is_retired ? 'Retired' : 'Active'], ['Range From', from || 'Earliest'], ['Range To', to || 'Latest'], ['Exported', new Date().toLocaleString()]. Sheet 'Training': header ['Instrument', 'Level', 'Trained On', 'Trainer', 'Expires On', 'Status', 'Note'] from `DB.listPersonTraining(p.id)`, Instrument/Trainer via UI.retiredName, Status = `DB.trainingActiveOn(rec, asOf) ? 'Valid' : 'Expired'`, blank expiry → 'No expiry'. Sheet 'Bookings': header ['Date', 'Start', 'End', 'Title', 'Project', 'Instruments', 'Category', 'Status'] from `SELECT m.date, m.start_time, m.end_time, m.title, m.category, m.is_cancelled, m.billing_retained, pr.title AS project_title, (GROUP_CONCAT of instruments with the ' (Retired)' CASE WHEN) AS instruments FROM meetings m JOIN meeting_people mp ON mp.meeting_id=m.id LEFT JOIN projects pr ON pr.id=m.project_id WHERE mp.person_id=? AND (?='' OR m.date>=?) AND (?='' OR m.date<=?) ORDER BY m.date, m.start_time`; Project = project_title || 'Facility-wide'; Status = is_cancelled ? (billing_retained ? 'Cancelled (charge retained)' : 'Cancelled') : 'Held'. Sheet 'Projects': ['Code', 'Title', 'Role', 'Status', 'Archived'] using the same PI-or-team query as Views.personDetail (role 'PI' when pi_id matches else project_people.role). Set '!cols' widths on each sheet. Filename: `Activity-Certificate-${p.name.replace(/[^A-Za-z0-9]+/g, '_')}-${from || 'earliest'}_to_${to || 'latest'}.xlsx` via blobDownload; toast 'Exported activity certificate to XLSX'. Never use toISOString().slice for any of this. (3) Add `exportActivityCertificate` to the `global.Exports` object.",
      "depends_on": [
        "T5"
      ],
      "parallel_safe": true,
      "verification": "`node --check js/exports.js` exit 0. `grep -c 'trained-user pool' js/exports.js` prints 0. `grep -c 'exportActivityCertificate' js/exports.js` prints at least 2 (definition + export). Node script run with `TZ='Asia/Jerusalem' node <script>` that replicates test/unit/exports.test.js's freshApp stubs (copy lines 26–55 of that file: loadApp(['consts','db','ui','views','reports','exports']), initSqlJs, DOMParser/Blob/URL stubs, UI.sanitizeHtml/UI.toast stubs, XLSX from libs/xlsx.full.min.js with XLSX.write wrapped to capture workbooks): seedFixture(DB); `DB.run(\"INSERT INTO person_instrument_training (person_id, instrument_id, level, trained_on, trainer_id, expires_on) VALUES (1,1,'Super User','2026-01-10',2,'2026-02-01')\")`; two meetings for alice via meeting_people — ('Held run','2026-03-01', project 1, instrument 1) and ('Dropped','2026-04-01', project NULL, is_cancelled=1) — plus one out-of-range meeting ('Old','2025-01-01'); call `Exports.exportActivityCertificate(1,'2026-01-01','2026-12-31')`; take `wb = captured.at(-1)`; assert `wb.SheetNames` includes 'Details','Training','Bookings','Projects'; `rows = XLSX.utils.sheet_to_json(wb.Sheets.Bookings,{header:1})` has exactly 3 rows (header + 2), one row containing 'Cancelled' and 'Facility-wide', none containing 'Old'; Training data row includes 'Scope A', 'Super User', 'Sam' and 'Expired'; Details contains a row ['Full Name','Alice']. Then call `Exports.exportReportsXlsx('2026-01-01','2026-12-31')`; in the newest captured workbook the 'Stewardship' sheet header row includes 'Trained Users' immediately after 'Distinct Users', and no cell of the 'Notes' sheet contains 'trained-user pool'. Script prints `T6 OK`. `TZ='Asia/Jerusalem' node --test 'test/unit/*.test.js'` reports 0 failures."
    },
    {
      "id": "T7",
      "title": "db.js demo seed: fenced #47 block with training records and demo mobile/campus values",
      "scope": "File: C:\\Users\\Owner\\repos\\cfc-47\\js\\db.js only (T1 already landed). Inside seedSampleData, append ONE contiguous block immediately before the final `markDirty(); return true;` (after the `// 11. Project Outputs` inserts), fenced exactly by `// --- #47 begin ---` and `// --- #47 end ---`. Contents: a comment `// 12. Instrument training records (#47)` explaining the mix (valid, expiring, expired, no-trainer); `const trainingRows = [ [4, 2, 'User', day(-120), 6, '', 'Trained on the FV3000 for intravital time-lapses'], [4, 4, 'User', day(-90), 6, day(275), ''], [5, 1, 'Super User', day(-200), 6, '', 'Independent after-hours use approved'], [1, 2, 'User', day(-400), 6, day(-5), 'Refresher due'], [7, 5, 'Super User', day(-300), null, '', ''], [3, 3, 'User', day(-60), 6, day(305), ''] ];` inserted with `run('INSERT INTO person_instrument_training (person_id, instrument_id, level, trained_on, trainer_id, expires_on, note) VALUES (?,?,?,?,?,?,?)', r)` — ids refer to the seeded people (1 Rostova, 3 Lin, 4 Chen, 5 Patel, 6 Kim, 7 Anand) and instruments (1 Leica, 2 FV3000, 3 Lightsheet, 4 Nikon, 5 Glacios). Then demo affiliation values via UPDATE only (no change to the peopleData INSERT above): `run(\"UPDATE people SET campus='Longwood' WHERE id IN (1,4)\"); run(\"UPDATE people SET campus='Cambridge' WHERE id IN (2,5)\"); run(\"UPDATE people SET campus='Main Campus' WHERE id IN (6,7,8)\"); run(\"UPDATE people SET mobile='+1 617 555 0142' WHERE id=4\"); run(\"UPDATE people SET mobile='+1 617 555 0187' WHERE id=6\");`. Do not touch any seedBooking() call, peopleData, instData, pricing tiers or discounts — seed booking #1's 490 / 546.25 / 589.95 triple must be unchanged. Use only `day(n)` for dates.",
      "depends_on": [
        "T1"
      ],
      "parallel_safe": true,
      "verification": "`node --check js/db.js` exit 0. `grep -c '// --- #47 begin ---' js/db.js` prints 2 and `grep -c '// --- #47 end ---' js/db.js` prints 2. Placement: `awk '/seedBooking\\(\\{/{s=NR} /\\/\\/ --- #47 begin ---/{b=NR} /^    markDirty\\(\\);$/{m=NR} END{print (b>s && b<m)}' js/db.js` prints 1, and `awk '/\\/\\/ --- #47 end ---/{e=NR} /^    markDirty\\(\\);$/{m=NR} END{print (m-e)<=2}' js/db.js` prints 1 (the fence closes right before markDirty). Node script run with `TZ='Asia/Jerusalem' node <script>`: `const {freshDb}=require('C:/Users/Owner/repos/cfc-47/test/unit/helpers/sqlite.js'); (async()=>{const {DB,UI}=await freshDb({search:'?demo=1'}); assert.ok(globalThis.IS_DEMO, 'demo mode must be on for the seed to run'); await DB.seedSampleData({force:true}); assert.equal(DB.row('SELECT COUNT(*) c FROM person_instrument_training').c,6); const today=UI.today(); assert.ok(DB.row(\"SELECT COUNT(*) c FROM person_instrument_training WHERE expires_on<>'' AND expires_on<?\",[today]).c>=1,'one expired record'); assert.ok(DB.trainedUserCountsAsOf(today).find(r=>r.instrument_id===2).trained_users===1,'Rostova expired on FV3000, Chen valid'); assert.equal(DB.row('SELECT campus FROM people WHERE id=4').campus,'Longwood'); const m=DB.row('SELECT * FROM meetings WHERE id=1'); assert.equal(Number(m.total_cost.toFixed(2)),589.95); console.log('T7 OK');})()` prints `T7 OK`. `TZ='Asia/Jerusalem' node --test 'test/unit/*.test.js'` reports 0 failures (money.test.js's 490 / 546.25 / 589.95 assertions included)."
    },
    {
      "id": "T8",
      "title": "CHANGELOG '## [Unreleased]' bullets ending with (#47) and the manual's stewardship callout",
      "scope": "Files: C:\\Users\\Owner\\repos\\cfc-47\\CHANGELOG.md and C:\\Users\\Owner\\repos\\cfc-47\\docs\\manual\\reports.html. (1) CHANGELOG.md: insert a new section directly above `## [1.11.0] — 2026-09-12` (after the intro paragraph) reading `## [Unreleased]` followed by a blank line, `### Added` and bullets, each a single bullet ending with ` (#47)`: a person profile page opened from the People list (details, projects, recent bookings with cancelled ones marked, instrument training); training records per person and instrument with a level (User / Super User), date, trainer and optional expiry, added, edited and removed from the profile; Mobile and Campus fields on the person dialog; a 'Trained Users' count on the Instruments list; a 'Trained Users' column on the Instrument Stewardship scorecard, in the custom report and in the Reports spreadsheet; an Activity Certificate spreadsheet export per person (details, training, bookings in a date range, projects); demo data now includes training records. Then `### Changed` with one bullet: the stewardship footnote no longer says the trained-user pool is unavailable (#47). Do not touch the 1.11.0 heading or any version string. Prose for facility managers: say 'dialog', never 'modal', 'join table', 'schema', 'migration' or 'boolean'. (2) docs/manual/reports.html line ~151: rewrite the callout `Why two things are missing` to `Why one thing is missing`: Trained Users is now shown as a headcount of people whose training record on that instrument is valid on the last day of the range (no expiry, or an expiry on or after that day), not a trend; downtime share is still left out because the app has no downtime log. Keep the surrounding HTML structure and class names. Do not touch docs/index.html, index.html, sw.js or js/consts.js.",
      "depends_on": [
        "T5"
      ],
      "parallel_safe": true,
      "verification": "`awk '/^## \\[Unreleased\\]$/{u=NR} /^## \\[1\\.11\\.0\\] — 2026-09-12$/{r=NR} END{print (u>0 && r>0 && u<r)}' CHANGELOG.md` prints 1. Every bullet line between those two headings ends with '(#47)': `awk '/^## \\[Unreleased\\]$/{f=1;next} /^## \\[1\\.11\\.0\\]/{f=0} f && /^- /{n++; if ($0 !~ /\\(#47\\)$/) bad++} END{print n>=6 && !bad}' CHANGELOG.md` prints 1. `grep -c 'modal' <(awk '/^## \\[Unreleased\\]$/{f=1;next} /^## \\[1\\.11\\.0\\]/{f=0} f' CHANGELOG.md)` prints 0. `grep -c 'Why two things are missing' docs/manual/reports.html` prints 0 and `grep -c 'Why one thing is missing' docs/manual/reports.html` prints 1; `grep -ci 'no training records' docs/manual/reports.html` prints 0. `git -C C:/Users/Owner/repos/cfc-47 diff --stat -- index.html sw.js js/consts.js` prints nothing for those three (T1's consts.js change is allowed but must not touch the APP_VERSION line: `git diff -- js/consts.js | grep -c APP_VERSION` prints 0). `TZ='Asia/Jerusalem' node --test test/unit/versions.test.js` passes."
    },
    {
      "id": "T9",
      "title": "test/unit/training.test.js and harness exposure: prove every #47 rule under a UTC+ timezone",
      "scope": "Files: new C:\\Users\\Owner\\repos\\cfc-47\\test\\unit\\training.test.js; edit C:\\Users\\Owner\\repos\\cfc-47\\test\\unit\\helpers\\app-harness.js (add `removeTraining: removeTraining,` to the injected `__TEST_APP_INTERNALS__` list); append one row to the table in C:\\Users\\Owner\\repos\\cfc-47\\test\\README.md describing training.test.js. Follow the style of delete-paths.test.js / exports.test.js (node:test + node:assert/strict, helpers freshDb/seedFixture/freshApp/loadApp, test names that state the rule). Tests, each its own `test()`: (a) migration: people has mobile and campus, person_instrument_training exists with the expected columns (PRAGMA table_info). (b) countPersonRefs/countInstrumentRefs count a training row; trainer_id alone counts for nobody. (c) retirePerson via app.internals: with a training row the person is retired and the row kept; with zero refs the person is deleted and a record where they were trainer gets trainer_id NULL (harness confirm:true). (d) retireInstrument: a trained-on instrument is retired, not deleted. (e) cascade: deleting a person with foreign_keys on removes their training rows (mirror schema.test.js's approach). (f) clearAllData empties the table. (g) DB.trainingActiveOn boundary: valid on expires_on, expired the next day, blank expiry always valid, blank trained_on valid. (h) Reports.computeStewardshipRows trainedUsers: distinct people not records; uses the range end; a blank `to` uses UI.today() (insert a record expiring yesterday via UI.todayPlusDays(-1) and one with no expiry, expect 1). (i) Exports.exportActivityCertificate (replicate exports.test.js's freshApp stubs): sheet names, range filtering, 'Cancelled' and 'Facility-wide' text, 'Expired' status. (j) exportReportsXlsx Stewardship header contains 'Trained Users' and Notes has no 'trained-user pool'. (k) Views.personDetail renders name, training level, 'Cancelled', 'Archived', and the four data-act markers; Views.instruments contains 'Trained Users'; Views.people contains data-goto=\"person\". (l) removeTraining via app.internals with confirm:true deletes exactly that row and with confirm:false keeps it. (m) demo seed under freshDb({search:'?demo=1'}) + seedSampleData({force:true}): 6 training rows, at least one expired as of UI.today(), booking #1 total_cost 589.95. (n) source lint: js/app.js has the five new dispatcher cases between the '// People CRUD' and '// Project Collaborators' markers; js/db.js has exactly two '// --- #47 begin ---' fences. Do not weaken any existing test.",
      "depends_on": [
        "T4",
        "T6",
        "T7",
        "T8"
      ],
      "parallel_safe": false,
      "verification": "`TZ='Asia/Jerusalem' node --test test/unit/training.test.js` exits 0 and its summary line shows `pass` >= 14 and `fail 0`. `TZ='Asia/Jerusalem' node --test 'test/unit/*.test.js'` exits 0 with `fail 0` (wiring, versions, money, delete-paths, exports, aggregation all included). `grep -c 'removeTraining: removeTraining,' test/unit/helpers/app-harness.js` prints 1. `grep -c 'training.test.js' test/README.md` prints 1. Sanity that the new test reaches real code: temporarily running `TZ='Asia/Jerusalem' node --test test/unit/training.test.js` after `git stash` of js/reports.js changes is NOT required; instead confirm the test file imports from './helpers/…' and never re-declares a CREATE TABLE (`grep -c 'CREATE TABLE' test/unit/training.test.js` prints 0)."
    }
  ],
  "batches": [
    [
      "T1"
    ],
    [
      "T2",
      "T3",
      "T7"
    ],
    [
      "T4",
      "T5"
    ],
    [
      "T6",
      "T8"
    ],
    [
      "T9"
    ]
  ],
  "risks": [
    {
      "risk": "Version bump reflex: an executor 'helpfully' bumps ?v= in index.html, CACHE_VERSION/PRECACHE_URLS in sw.js or APP_VERSION in js/consts.js, or dates the CHANGELOG heading, colliding with the sibling branch being merged by hand.",
      "mitigation": "Every task's scope forbids it; T8's verification runs `git diff --stat -- index.html sw.js js/consts.js` and greps the consts.js diff for APP_VERSION; versions.test.js keeps CHANGELOG's newest dated heading pinned to 1.11.0. The verifier rejects any task whose diff touches those lines."
    },
    {
      "risk": "Schema written once, not twice: the table or the mobile/campus columns land in the SCHEMA string but not in migrate() (or vice versa), so a brand-new database and an upgraded one disagree; or the migrate/seed additions are not single fenced blocks at the very end, breaking the hand-merge.",
      "mitigation": "T1 and T7 verifications grep for exactly two `// --- #47 begin ---` fences and awk-check their position relative to migrate()'s close and the seed's `markDirty();`. The freshDb boot in T1/T9 runs the shipped SCHEMA plus migrate() together, so a column missing from either side surfaces as a failed INSERT or PRAGMA check."
    },
    {
      "risk": "Retired-record data loss through the new dialog: the instrument or trainer select filters out retired records, so editing a record that already points at a retired instrument/trainer silently rewrites it to something else or blanks the trainer.",
      "mitigation": "T4's scope hard-codes the `WHERE is_retired=0 OR id=?` shape for both selects and its verification greps for it; T9 (l) exercises removeTraining through the harness and T2 (a)-(c) prove the retire paths keep training rows on retire and clean them only on a zero-reference delete."
    },
    {
      "risk": "The stewardship 'Trained Users' figure drifts between screen, custom report, XLSX sheet and Notes/manual prose (this codebase has shipped exactly this class of defect: a Notes sheet promising an '(Archived)' suffix no code wrote).",
      "mitigation": "One rule lives in DB.trainedUserCountsAsOf/trainingActiveOn (T1); reports.js reads it (T5) and exports.js reads Reports.computeStewardshipRows (T6) so the number cannot fork. T5/T6/T8 verifications grep every 'trained-user pool' mention out of js/ and the manual, and T9 (h)(j) assert the exported header and Notes text against the real workbook via sheet_to_json."
    },
    {
      "risk": "wiring.test.js fails between T3 and T4 because views.js emits data-act values (training-add/-edit/-del, export-activity-certificate) whose dispatcher cases only arrive in T4; an executor might 'fix' this by adding the values to HANDLED_ELSEWHERE or by appending cases at the end of the switch.",
      "mitigation": "T3's verification names the only acceptable transient failure and forbids editing wiring.test.js; T4's verification uses awk line-ordering checks to prove the cases sit inside the People CRUD block, and the full suite must be green at the end of batch 3."
    },
    {
      "risk": "Date handling east of Greenwich: a 'today' default, an expiry comparison or a filename built via toISOString().slice(0,10) shows yesterday, and the profile's Expired badge or the Trained Users count is off by one day.",
      "mitigation": "All scopes mandate UI.today/UI.ymd/UI.todayPlusDays/UI.fmtDate and plain string comparison on 'YYYY-MM-DD'; wiring.test.js's date lint already fails on the forbidden pattern; every node verification and the suite run under TZ='Asia/Jerusalem'; T9 (g)(h) test the expiry boundary day explicitly."
    },
    {
      "risk": "Seed drift: adding training rows or affiliation UPDATEs disturbs seed booking #1's cost snapshot or the hard-coded ids the seed relies on (people 1-8, instruments 1-5), or the seed runs outside demo mode.",
      "mitigation": "T7 only appends INSERTs into the new table and UPDATEs of the two new columns after every seedBooking() call; its verification boots with search '?demo=1', asserts 6 rows and total_cost 589.95, and money.test.js remains in the suite."
    },
    {
      "risk": "No browser in the pipeline: a template-string typo in Views.personDetail or the training dialog surfaces only when the screen is visited, and 'node --check passed' is mistaken for the screen rendering.",
      "mitigation": "T3/T9 render Views.personDetail, Views.people and Views.instruments in Node against a real in-memory database and assert on their HTML; the dialog HTML cannot be rendered in Node, so the verifier must state plainly that the training dialog was not exercised in a browser and the run's final report must flag `node --test 'test/browser/*.spec.js'` (smoke.spec.js) plus a manual visit of #/people → a profile → Add Training as the remaining manual check."
    },
    {
      "risk": "docs/index.html's footer-version script takes the first `## [...]` heading from the fetched CHANGELOG and will read 'vUnreleased' once this lands on main before a release.",
      "mitigation": "Out of scope by constraint (docs page is not part of the app shell and the Unreleased heading is mandated); recorded here so whoever cuts the release renames the heading to the version and date, which restores the footer automatically."
    }
  ]
}