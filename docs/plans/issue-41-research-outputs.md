# Plan: issue-41-research-outputs

Fable planner, 2026-09-16. Repo: C:/Users/Owner/repos/cfc-41

## Objective

Implement GitHub issue #41 (Enhance Research Outputs) in C:\Users\Owner\repos\cfc-41: additive columns doi/url/authors/acknowledges_facility/file_id on project_outputs (SCHEMA + fenced migrate block), type-driven form fields, PDF drop/picker on the Research Outputs card stored through the existing files/uploads mechanism with inline /Title extraction, clickable DOI/URL links, click-row read-only detail modal, click-badge edit, new fields in per-project XLSX/DOCX/PDF and facility XLSX, seed updates, CHANGELOG bullets under '## [Unreleased]' ending '(#41)'. No version bumps, reports.js untouched, funnel counting unchanged, no output_people table.

## Assumptions

- The worktree C:/Users/Owner/repos/cfc-41 is clean at 4f5214e (branch created from origin/main). NEVER run git reset, git checkout --, git stash or git clean: tasks run one after another with no commits in between, so any reset wipes the previous tasks’ uncommitted work. Do not commit; the orchestrator commits after the run.
- Tests run with `TZ='Asia/Jerusalem' node --test 'test/unit/*.test.js'` (glob, not directory); `python` not `python3`; no browser is guaranteed, so browser specs may skip.
- Files/attachments mechanism (verified): `files` table rows (project_id, name, kind 'upload'|'link', path=storageKey) plus Blob in IndexedDB under `uploads:<storageKey>` via DB.saveUpload/getUpload/deleteUpload; DB.buildBackup base64-encodes uploads and restoreBackup decodes them, so a `file_id` link needs no backup changes. A dropped PDF is saved exactly like `fSave` (app.js ~4316-4341) does.
- Stored type values ('publication','acknowledgement','dataset','other', plus any facility vocab) stay lowercase; they are data and comparison targets. Adding 'thesis','software','talk','poster' to CONST.OUTPUT_TYPE defaults is additive because DB.vocabList merges CONST defaults with vocab rows.
- Hidden type-specific fields keep their stored values on save (a type change never silently blanks doi/url/authors); visibility is a display concern only.
- Cancelling the Add Output dialog after a drop leaves the uploaded PDF as an ordinary attachment in the Files card (no orphan blob, nothing lost); the toast says so.
- CHANGELOG.md currently has no '## [Unreleased]' heading; T10 creates it directly above '## [1.11.0] — 2026-09-12'. versions.test.js's heading regex matches only numbered headings, so this does not break it.
- The wiring test (test/unit/wiring.test.js) requires every emitted data-act to have a dispatcher case and vice versa, and every ic('name') to exist in UI's ICONS map (available: file, file-plus, external, eye, edit, x, tag, book, trash, plus, copy; there is NO 'link' or 'download' icon). Tasks are batched so app.js cases (T5/T6) land before views.js emitters (T7).
- Icons: use ic('file-plus') for the drop hint, ic('file') for a linked attachment, ic('external') for links, ic('eye') for view; do not add new ICONS entries.
- Seed booking #1's cost triple 490 / 546.25 / 589.95 is guarded by test/unit/money.test.js and must keep passing; the seed change is UPDATEs to existing output rows only, so funnel demo figures are unchanged.

## Tasks

| id | title | depends on | parallel safe |
|---|---|---|---|
| T1 | consts.js: extra default output types and the per-type field map | - | true |
| T2 | ui.js: inline PDF /Title parser, DOI/URL helpers, authors splitter + unit test | - | true |
| T3 | db.js: additive project_outputs columns in SCHEMA and a fenced migrate() block; extend schema test | - | true |
| T4 | db.js: fenced demo-seed block setting DOI/authors/acknowledgement on existing outputs | T3 | true |
| T5 | app.js: type-driven Add/Edit Output forms, new columns in saves, read-only detail modal, dispatcher case | T1, T2, T3 | true |
| T6 | app.js: PDF drop zone and file picker on the Research Outputs card, stored via the files/uploads path | T5 | true |
| T7 | views.js: Research Outputs card as clickable rows with badge-edit, links, acknowledged badge, drop zone hint | T6, T2 | true |
| T8 | css/app.css: styles for output rows, badge-as-button, links strip, drop hint and drag-over state | - | true |
| T9 | exports.js: new output fields in per-project XLSX/DOCX/PDF and the facility-wide XLSX, with sheet-level tests | T3 | true |
| T10 | CHANGELOG '## [Unreleased]' bullets (#41) and manual update for the enhanced card | T5, T6, T7, T9, T4 | true |
| T11 | Integration gate: full unit suite, untouched-file and invariant checks | T1, T2, T3, T4, T5, T6, T7, T8, T9, T10 | false |

## Batches

1. T1, T2, T3
2. T4, T5, T9, T8
3. T6
4. T7
5. T10
6. T11

## Task details

### T1 consts.js: extra default output types and the per-type field map

**Scope:** Edit C:\Users\Owner\repos\cfc-41\js\consts.js only. (a) Extend `OUTPUT_TYPE` to `['publication', 'acknowledgement', 'dataset', 'thesis', 'software', 'talk', 'poster', 'other']` (all lowercase — these are stored values). (b) Add a sibling export `window.OUTPUT_TYPE_FIELDS` (defined right after the CONST object, same IIFE) mapping each lowercase type to `{ fields: [...], referenceLabel: '...' }` where `fields` is a subset of `['doi','reference','authors','url','acknowledges_facility']`: publication → fields doi, reference, authors, url, acknowledges_facility; referenceLabel 'Journal / Citation'. thesis → reference, authors, url, acknowledges_facility; 'Institution / Degree'. dataset → doi, url, reference, authors, acknowledges_facility; 'Repository / Accession'. software → url, doi, reference, authors, acknowledges_facility; 'Repository / Version'. talk → reference, authors, url, acknowledges_facility; 'Venue'. poster → same as talk. acknowledgement → reference, doi, url, authors; 'Where Acknowledged'. other → all five; 'Reference'. Also export a `default` key = the 'other' entry so unknown facility-added vocab types show every field. Title Case labels. Add a short comment explaining Title/Date/Note are always shown and hidden fields keep their stored values (display-only rule). Do NOT touch APP_VERSION or anything else in the file.

**Verification:** cd C:\Users\Owner\repos\cfc-41 && node --check js/consts.js && node -e "const {loadApp}=require('./test/unit/helpers/load-module');const a=loadApp(['consts']);const F=globalThis.OUTPUT_TYPE_FIELDS;const t=a.CONST.OUTPUT_TYPE;if(!t.includes('thesis')||!t.includes('poster')||t[0]!=='publication')throw new Error('types');for(const k of ['publication','thesis','dataset','software','talk','poster','acknowledgement','other','default']){if(!F[k]||!Array.isArray(F[k].fields)||!F[k].referenceLabel)throw new Error('map '+k)}if(F.publication.fields.indexOf('doi')<0||F.talk.referenceLabel!=='Venue')throw new Error('content');console.log('ok')" && grep -c "APP_VERSION = '" js/consts.js && git diff --stat -- js/consts.js | grep -q consts.js && TZ='Asia/Jerusalem' node --test test/unit/labels.test.js test/unit/wiring.test.js

### T2 ui.js: inline PDF /Title parser, DOI/URL helpers, authors splitter + unit test

**Scope:** Edit C:\Users\Owner\repos\cfc-41\js\ui.js: add four pure functions near isSafeUrl (~line 798) and export them on `global.UI`: (1) `pdfTitleFromBytes(bytes)` — accepts ArrayBuffer or Uint8Array, decodes as latin1 in chunks (String.fromCharCode.apply on <=8192-byte slices, never a CDN lib), finds the LAST `/Title` entry (`/\/Title\s*\(((?:\\.|[^\\)])*)\)/g` literal string, or `/\/Title\s*<([0-9A-Fa-f\s]*)>/g` hex string); for literal strings unescape \( \) \\ \n \r \t and 3-digit octal, and if the result starts with the UTF-16BE BOM (0xFE 0xFF) decode pairs to UTF-16; for hex strings decode bytes, honouring the same BOM; trim; return '' when nothing found, when the result is empty/whitespace, or when it is only 'untitled' (case-insensitive) — the caller falls back to the filename. (2) `normalizeDoi(s)` — trims, strips a leading 'doi:' (any case), 'https://doi.org/', 'http://doi.org/', 'https://dx.doi.org/'; returns '' unless the remainder matches /^10\.\d{4,9}\/\S+$/. (3) `doiUrl(s)` — 'https://doi.org/' + encodeURI(normalizeDoi(s)) or '' when normalizeDoi is empty. (4) `splitAuthors(s)` — split on ';' or newline (NOT on commas, since 'Smith, J.' is one author), trim, drop empties. No DOM use. Then CREATE C:\Users\Owner\repos\cfc-41\test\unit\output-helpers.test.js using loadApp(['consts','ui']) with tests: a hand-built minimal PDF buffer containing `/Title (Volumetric \(islet\) mapping)` yields 'Volumetric (islet) mapping'; a hex `/Title <FEFF0056006F006C>` yields 'Vol'; a buffer with no /Title yields ''; `/Title (untitled)` yields ''; normalizeDoi('https://doi.org/10.1000/abc') === '10.1000/abc', normalizeDoi('doi:10.1000/abc') === '10.1000/abc', normalizeDoi('not a doi') === ''; doiUrl('10.1000/abc') === 'https://doi.org/10.1000/abc'; splitAuthors('Smith, J.; Doe, A.\nLee, K') has length 3. Test names state the rule.

**Verification:** cd C:\Users\Owner\repos\cfc-41 && node --check js/ui.js && TZ='Asia/Jerusalem' node --test test/unit/output-helpers.test.js test/unit/labels.test.js test/unit/wiring.test.js && grep -n 'pdfTitleFromBytes,\|normalizeDoi,\|doiUrl,\|splitAuthors,' js/ui.js | wc -l | grep -q '^4$' && ! grep -n 'cdn\|importScripts' js/ui.js | grep -i pdf

### T3 db.js: additive project_outputs columns in SCHEMA and a fenced migrate() block; extend schema test

**Scope:** Edit C:\Users\Owner\repos\cfc-41\js\db.js. (a) In the SCHEMA string's `CREATE TABLE IF NOT EXISTS project_outputs` (~line 228-237) add after `note TEXT DEFAULT ''`: `doi TEXT DEFAULT ''`, `url TEXT DEFAULT ''`, `authors TEXT DEFAULT ''`, `acknowledges_facility INTEGER DEFAULT 0`, `file_id INTEGER REFERENCES files(id) ON DELETE SET NULL` (keep created_at last is not required; keep the existing columns' order). (b) At the VERY END of `migrate()` (after the existing project_outputs CREATE TABLE try/catch ending ~line 608, immediately before the function's closing `}` at ~line 609) append ONE contiguous block fenced exactly by the lines `    // --- #41 begin ---` and `    // --- #41 end ---` containing five separate `try { db.exec("ALTER TABLE project_outputs ADD COLUMN ..."); } catch (_) {}` statements with the same definitions as (a) (SQLite permits adding a REFERENCES column with NULL default), plus a 2-line comment explaining it runs after the CREATE TABLE IF NOT EXISTS so older DBs get the table first, then the columns. Nothing else in migrate() changes. (c) Nowhere else in db.js (seed is T4). Then edit C:\Users\Owner\repos\cfc-41\test\unit\schema.test.js: add `project_outputs: ['doi', 'url', 'authors', 'acknowledges_facility', 'file_id']` to the `migratedColumns` map (~line 407), and add a new test in that describe: insert a project, a files row for it, an output with file_id = that file; `DELETE FROM files WHERE id=?`; assert the output row still exists and its file_id IS NULL (proves ON DELETE SET NULL with the pragma on). Also assert `PRAGMA foreign_key_list(project_outputs)` includes a row with table 'files' and on_delete 'SET NULL'.

**Verification:** cd C:\Users\Owner\repos\cfc-41 && node --check js/db.js && grep -c '// --- #41 begin ---' js/db.js | grep -q '^1$' && grep -c '// --- #41 end ---' js/db.js | grep -q '^1$' && node -e "const s=require('fs').readFileSync('js/db.js','utf8').split('\n');const e=s.findIndex(l=>l.includes('// --- #41 end ---'));const b=s.findIndex(l=>l.includes('// --- #41 begin ---'));const after=s.slice(e+1,e+3).join('\n');if(!/^\s*\}\s*$/m.test(after))throw new Error('fence not at end of migrate()');const block=s.slice(b,e+1).join('\n');if((block.match(/ALTER TABLE project_outputs ADD COLUMN/g)||[]).length!==5)throw new Error('need 5 ALTERs');if(!/file_id INTEGER REFERENCES files\(id\) ON DELETE SET NULL/.test(block))throw new Error('fk');console.log('fence ok')" && grep -n 'acknowledges_facility INTEGER DEFAULT 0' js/db.js | wc -l | grep -q '^2$' && TZ='Asia/Jerusalem' node --test test/unit/schema.test.js test/unit/backup-roundtrip.test.js test/unit/persistence.test.js

### T4 db.js: fenced demo-seed block setting DOI/authors/acknowledgement on existing outputs

**Scope:** Edit C:\Users\Owner\repos\cfc-41\js\db.js only, inside `seedSampleData` (~line 2541-3052). At the VERY END of the seed — immediately before the final `markDirty();` / `return true;` (currently ~line 3050) and after the three project_outputs INSERTs (~3046-3048) — append ONE contiguous block fenced exactly by `    // --- #41 begin ---` and `    // --- #41 end ---` (these are the second pair in the file; the migrate() pair from T3 stays). The block issues UPDATEs only (no INSERTs, so funnel demo figures are unchanged): (1) the project-3 publication (`WHERE project_id=3 AND type='publication'`): doi '10.1000/jei.2026.12.3.200' (a clearly fictional DOI — add a comment saying so), authors 'Chen, L.; Okafor, T.; Rivera, M.', acknowledges_facility 1, file_id = the id of the seeded project-3 file `Pancreatic_Islets_3D_Summary.xlsx` looked up via a subselect `(SELECT id FROM files WHERE project_id=3 AND name='Pancreatic_Islets_3D_Summary.xlsx')`; (2) the project-1 dataset (`WHERE project_id=1 AND type='dataset'`): url 'https://example.org/bioimaging/vol4', authors 'Park, S.; Nguyen, H.', acknowledges_facility 0. Do not change any existing seed line, dates (keep `day(n)`), bookings or prices.

**Verification:** cd C:\Users\Owner\repos\cfc-41 && node --check js/db.js && grep -c '// --- #41 begin ---' js/db.js | grep -q '^2$' && node -e "const s=require('fs').readFileSync('js/db.js','utf8').split('\n');const ends=s.map((l,i)=>l.includes('// --- #41 end ---')?i:-1).filter(i=>i>=0);const e=ends[ends.length-1];const nxt=s.slice(e+1,e+4).join('\n');if(!/markDirty\(\);/.test(nxt)||!/return true;/.test(nxt))throw new Error('seed fence must sit right before markDirty(); return true;');console.log('seed fence ok')" && node -e "(async()=>{const path=require('path');const {loadApp,REPO}=require('./test/unit/helpers/load-module');const a=loadApp(['consts','db','ui']);globalThis.initSqlJs=require(path.join(REPO,'libs','sql-asm.js'));await a.DB.boot();await a.DB.seedSampleData({force:true});const r=a.DB.row(\"SELECT SUM(doi!='') d, SUM(acknowledges_facility=1) a, SUM(authors!='') au, SUM(file_id IS NOT NULL) f, COUNT(*) n FROM project_outputs\");if(!(r.d>=1&&r.a>=1&&r.au>=2&&r.f>=1&&r.n===3))throw new Error(JSON.stringify(r));const fk=a.DB.row('SELECT po.file_id, f.project_id fp, po.project_id pp FROM project_outputs po JOIN files f ON f.id=po.file_id');if(fk.fp!==fk.pp)throw new Error('linked file belongs to another project');console.log('seed ok',JSON.stringify(r))})().catch(e=>{console.error(e);process.exit(1)})" && TZ='Asia/Jerusalem' node --test test/unit/money.test.js test/unit/funnel-output-date.test.js test/unit/aggregation.test.js test/unit/dashboard.test.js

### T5 app.js: type-driven Add/Edit Output forms, new columns in saves, read-only detail modal, dispatcher case

**Scope:** Edit C:\Users\Owner\repos\cfc-41\js\app.js, Research Outputs section (~4212-4293) and the dispatcher outputs block (~1428-1433). (1) Add a helper `outputFieldsHtml(prefix, item)` that renders ALL fields for both modals with ids `${prefix}-type|title|reference|doi|url|authors|ack|date|note|file-id` — each optional field wrapped as `<div class="field" data-out-field="doi">` etc.; `authors` is a textarea (hint text, sentence case: 'One author per line, or separate with semicolons.'); `acknowledges_facility` is a checkbox labelled 'Acknowledges the Facility'; a hidden `<input type="hidden" id="${prefix}-file-id">`; when item.file_id has a matching files row show a static line 'Attached File: <name>' with a `data-act="download-file" data-id data-name` button (existing case) — kind 'link' files render the name only. (2) Add `applyOutputTypeFields(modalEl, prefix)`: reads the type select, looks up `window.OUTPUT_TYPE_FIELDS[type] || OUTPUT_TYPE_FIELDS.default`, toggles the `hidden` attribute on each `[data-out-field]` wrapper (Title/Date/Note always visible) and sets the reference `<label>` text to `referenceLabel`. Wire it in `UI.openModal`'s onMount callback for both modals and re-run on the type select's `change` event (also after the vocab '+ Add New' flow injects a new option — listen on the same select). Hidden fields keep their values and are still saved. (3) `addOutput(projectId, prefill = {})` accepts `{ title, fileId, fileName, type }` and pre-populates; `outputSave` and `outputEditSave` read all fields, normalise DOI via `UI.normalizeDoi` (toast error 'Enter a valid DOI, e.g. 10.1000/xyz123' when non-empty but invalid; sentence case), require URL to pass `UI.isSafeUrl` when non-empty ('Enter a full web address starting with http:// or https://'), and INSERT/UPDATE the columns doi, url, authors, acknowledges_facility, file_id (file_id from the hidden input, null when blank; on edit, preserve the existing file_id). Use `UI.topModal()` like the existing functions. (4) Add `viewOutput(id)`: a read-only modal titled `${ic('eye')} Research Output` showing Type badge, Title, Reference (label from the type map), DOI as `<a href=UI.doiUrl target=_blank rel="noopener noreferrer">`, URL as a link only when `UI.isSafeUrl`, Authors as a `<ul>` from `UI.splitAuthors` (or '—'), Date via `fmt`/UI.fmtDate with '(logged)' fallback to created_at, Note, the line 'Facility acknowledged: Yes' or 'Facility acknowledged: No', and the linked file (name + Download button when kind='upload'); footer buttons 'Close' (data-act="close") and 'Edit' (data-act="output-edit" data-id). (5) Dispatcher: add `case 'output-view': return viewOutput(el.dataset.id);` INSIDE the existing '// Research Outputs CRUD' block (between the existing output-* cases), never at the end of the switch. (6) Never use toISOString().slice for dates. Title Case for labels/buttons, sentence case for hints/toasts. Do not touch versions.

**Verification:** cd C:\Users\Owner\repos\cfc-41 && node --check js/app.js && grep -n "case 'output-view'" js/app.js | head -1 && node -e "const s=require('fs').readFileSync('js/app.js','utf8');const i=s.indexOf(\"case 'output-view'\");const a=s.indexOf(\"case 'output-add'\");const f=s.indexOf(\"case 'add-file'\");if(!(i>a&&i<f))throw new Error('output-view case must sit inside the outputs block before the Files CRUD block');for(const k of ['function viewOutput','function applyOutputTypeFields','function outputFieldsHtml','OUTPUT_TYPE_FIELDS','acknowledges_facility','Facility acknowledged: ','UI.normalizeDoi','UI.doiUrl','UI.splitAuthors'])if(!s.includes(k))throw new Error('missing '+k);const ins=s.match(/INSERT INTO project_outputs \(([^)]*)\)/g)||[];if(!ins.some(x=>/doi/.test(x)&&/file_id/.test(x)))throw new Error('INSERT lacks new columns');if(!/UPDATE project_outputs SET[^;]*doi=\?/.test(s))throw new Error('UPDATE lacks doi');console.log('app ok')" && ! git diff -- js/app.js | grep '^+' | grep -q 'toISOString().slice' && TZ='Asia/Jerusalem' node --test test/unit/wiring.test.js test/unit/delete-paths.test.js

### T6 app.js: PDF drop zone and file picker on the Research Outputs card, stored via the files/uploads path

**Scope:** Edit C:\Users\Owner\repos\cfc-41\js\app.js only. (1) Add `async function importOutputFile(projectId, file)` in the Research Outputs section: reject non-PDF (`file.type !== 'application/pdf'` and name not ending .pdf) with toast 'Drop a PDF file to log it as a research output.' (sentence case); read `await file.arrayBuffer()`, title = `UI.pdfTitleFromBytes(bytes)` falling back to the filename without its extension; then save EXACTLY as `fSave` does for uploads: `storageKey = `${pid}_${Date.now()}_${name}``, `DB.run('INSERT INTO files (project_id, name, kind, path) VALUES (?,?,?,?)', [pid, name, 'upload', storageKey])`, read `last_insert_rowid()`, `await DB.saveUpload(storageKey, file)`; then `addOutput(pid, { title, fileId, fileName: name, type: 'publication' })` and toast 'File attached — fill in the output details' (if the dialog is cancelled the PDF stays as an ordinary attachment). Wrap in try/catch and report failures via UI.toast(..., 'error'). (2) Add `pickOutputFile(projectId)`: finds `document.querySelector('[data-output-file-input]')` in the current view and calls `.click()`. (3) In `wireGlobal()` (~line 1147) add document-level delegated listeners: `dragover` — if `e.target.closest('[data-drop-outputs]')`, preventDefault and add class `is-dragover` to that card; `dragleave`/`drop` remove it; `drop` on such a card calls preventDefault and `importOutputFile(Number(card.dataset.dropOutputs), e.dataTransfer.files[0])`. Extend the existing `document.addEventListener('change', ...)` (~line 1206) so an `[data-output-file-input]` input with a file calls `importOutputFile(Number(input.dataset.outputFileInput), input.files[0])` and then resets `input.value = ''`. (4) Dispatcher: `case 'output-pick-file': return pickOutputFile(el.dataset.projectId || ctx.project);` INSIDE the outputs block. Note: wiring.test.js will fail on 'orphan case' until T7 adds the emitters; that is expected in this batch and the task's verification excludes it — T7's verification runs it.

**Verification:** cd C:\Users\Owner\repos\cfc-41 && node --check js/app.js && node -e "const s=require('fs').readFileSync('js/app.js','utf8');for(const k of ['async function importOutputFile','function pickOutputFile','data-drop-outputs','data-output-file-input','is-dragover',\"addEventListener('dragover'\",\"addEventListener('drop'\",'UI.pdfTitleFromBytes','DB.saveUpload(storageKey','[data-output-file-input]'])if(!s.includes(k))throw new Error('missing '+k);const i=s.indexOf(\"case 'output-pick-file'\");const a=s.indexOf(\"case 'output-add'\");const f=s.indexOf(\"case 'add-file'\");if(!(i>a&&i<f))throw new Error('output-pick-file case must be inside the outputs block');const fn=s.slice(s.indexOf('async function importOutputFile'));const body=fn.slice(0,fn.indexOf('\n  }\n'));if(!/INSERT INTO files \(project_id, name, kind, path\)/.test(body)||!/'upload'/.test(body))throw new Error('must store through the files table like fSave');console.log('drop ok')" && ! git diff -- js/app.js | grep '^+' | grep -q 'toISOString().slice' && TZ='Asia/Jerusalem' node --test test/unit/delete-paths.test.js test/unit/schema.test.js

### T7 views.js: Research Outputs card as clickable rows with badge-edit, links, acknowledged badge, drop zone hint

**Scope:** Edit C:\Users\Owner\repos\cfc-41\js\views.js, projectDetail: (1) change the outputs query (~line 257) to `SELECT po.*, f.name AS file_name, f.kind AS file_kind FROM project_outputs po LEFT JOIN files f ON f.id = po.file_id WHERE po.project_id=? ORDER BY ${global.DB.outputEffDate('po')} DESC, po.id DESC`. (2) Rewrite the card (~387-408): the card `<div class="card mb-16 output-drop" data-drop-outputs="${p.id}">`; header keeps 'Add Output' (data-act="output-add") and adds a ghost button `${ic('file-plus')} Attach PDF` with `data-act="output-pick-file" data-project-id="${p.id}"`; a hidden `<input type="file" accept="application/pdf,.pdf" class="visually-hidden-input" data-output-file-input="${p.id}" hidden>`. Each output renders as `<div class="output-row">` containing (a) `<button type="button" class="output-main" data-act="output-view" data-id="${o.id}" aria-label="View Output">` with inside it the type badge `<span class="badge neutral output-type" role="button" tabindex="0" data-act="output-edit" data-id="${o.id}" title="Edit Output" style="text-transform:capitalize">${esc(o.type)}</span>` (the dispatcher's closest('[data-act]') picks the badge first, so badge = edit, rest of row = view), the title, an 'Acknowledged' success badge when `o.acknowledges_facility`, a `${ic('file')}` marker with the file name when `o.file_name`, then a faint line with authors (esc, truncated to 80 chars with …), reference and date (existing fmt fallback to created_at); (b) a sibling `<div class="output-links">` OUTSIDE the button holding `<a class="file-link" href="${esc(global.UI.doiUrl(o.doi))}" target="_blank" rel="noopener noreferrer">DOI ${ic('external')}</a>` when doiUrl is non-empty, `<a ... href="${esc(o.url)}">Link ${ic('external')}</a>` only when `global.UI.isSafeUrl(o.url)`, a Download button (`data-act="download-file" data-id="${o.file_id}" data-name`) when file_kind==='upload', and the existing delete span (data-act="output-del"). Remove the old separate edit span (badge replaces it). (3) Below the list (and also when the list is empty, replacing the old emptyState's second line) render a persistent `<div class="drop-hint">${ic('file-plus')} Drop a PDF here or use Attach PDF to log it as an output — the title is read from the file when it has one.</div>` (sentence case prose). Keep the empty state when there are no outputs but still show the hint. All user data through `esc`. Do not add new icon names.

**Verification:** cd C:\Users\Owner\repos\cfc-41 && node --check js/views.js && node -e "const s=require('fs').readFileSync('js/views.js','utf8');for(const k of ['data-drop-outputs=','data-output-file-input=','data-act=\"output-view\"','data-act=\"output-pick-file\"','class=\"drop-hint\"','LEFT JOIN files f ON f.id = po.file_id','global.UI.doiUrl(','global.UI.isSafeUrl(o.url)','acknowledges_facility'])if(!s.includes(k))throw new Error('missing '+k);const card=s.slice(s.indexOf('Research Outputs Card'),s.indexOf('Instruments Card'));if(!/output-type[^>]*data-act=\"output-edit\"/.test(card))throw new Error('badge must carry output-edit');if((card.match(/data-act=\"output-edit\"/g)||[]).length!==1)throw new Error('exactly one edit emitter (the badge)');const mainIdx=card.indexOf('class=\"output-main\"');const linksIdx=card.indexOf('class=\"output-links\"');if(!(mainIdx>0&&linksIdx>mainIdx))throw new Error('links must be a sibling after the view button');if(card.slice(mainIdx,linksIdx).includes('<a '))throw new Error('no anchors inside the view button');console.log('views ok')" && TZ='Asia/Jerusalem' node --test test/unit/wiring.test.js test/unit/dashboard.test.js test/unit/exports.test.js

### T8 css/app.css: styles for output rows, badge-as-button, links strip, drop hint and drag-over state

**Scope:** Edit C:\Users\Owner\repos\cfc-41\css\app.css only; append a commented block after the `.file-link` rules (~line 681-688). Add: `.output-row` (display:flex; align-items:flex-start; gap:8px; padding:8px 0; border-bottom:1px dashed var(--border); flex-wrap:wrap), `.output-main` (button reset: background:none; border:0; padding:6px 8px; margin:0 -8px; text-align:left; color:inherit; font:inherit; flex:1 1 240px; min-width:0; border-radius:8px; cursor:pointer) with `:hover`/`:focus-visible` background var(--surface-2) and outline using var(--primary); `.output-main .output-type` (cursor:pointer) with hover border-color var(--border-strong); `.output-links` (display:flex; gap:6px; align-items:center; flex-wrap:wrap; margin-left:auto); `.drop-hint` (margin-top:10px; padding:10px 12px; border:1px dashed var(--border); border-radius:8px; color:var(--text-muted); font-size:12.5px; display:flex; gap:8px; align-items:center) with svg sizing 16px; `.card.output-drop.is-dragover` (outline:2px dashed var(--primary); outline-offset:2px; background:var(--primary-soft)) and `.card.output-drop.is-dragover .drop-hint` (border-color:var(--primary); color:var(--primary)); `.visually-hidden-input` (position:absolute; width:1px; height:1px; opacity:0; overflow:hidden). Use only CSS variables already defined in the file (check `:root` for var names before using them); ensure phone width works (flex-wrap, no fixed widths > 240px). No version bump anywhere.

**Verification:** cd C:\Users\Owner\repos\cfc-41 && node -e "const s=require('fs').readFileSync('css/app.css','utf8');for(const k of ['.output-row','.output-main','.output-links','.drop-hint','.output-drop.is-dragover','.visually-hidden-input'])if(!s.includes(k))throw new Error('missing '+k);const vars=new Set((s.match(/--[a-z0-9-]+(?=\s*:)/g)||[]));const used=[...new Set((s.slice(s.indexOf('.output-row')).match(/var\((--[a-z0-9-]+)\)/g)||[]).map(m=>m.slice(4,-1)))];const bad=used.filter(v=>!vars.has(v));if(bad.length)throw new Error('undefined css vars: '+bad.join(','));const open=(s.match(/\{/g)||[]).length,close=(s.match(/\}/g)||[]).length;if(open!==close)throw new Error('brace mismatch '+open+'/'+close);console.log('css ok')" && git diff --stat -- index.html sw.js js/consts.js | grep -c '' | grep -q '^0$'

### T9 exports.js: new output fields in per-project XLSX/DOCX/PDF and the facility-wide XLSX, with sheet-level tests

**Scope:** Edit C:\Users\Owner\repos\cfc-41\js\exports.js. (1) `loadProjectData` (~line 78): outputs query becomes `SELECT po.*, f.name AS file_name, ${DB.outputEffDate('po')} AS eff_date FROM project_outputs po LEFT JOIN files f ON f.id = po.file_id WHERE po.project_id=? ORDER BY ${DB.outputEffDate('po')} DESC, po.id DESC`. (2) Per-project XLSX Sheet 7 (~578-584): header becomes ['Type', 'Title', 'Authors', 'Reference', 'DOI', 'URL', 'Acknowledges Facility', 'Attached File', 'Date (* = logged date, none set)', 'Note']; rows fill `o.authors || '—'`, `o.doi || '—'`, `o.url || '—'`, `o.acknowledges_facility ? 'Yes' : 'No'`, `o.file_name || '—'`; extend `!cols` to 10 entries. (3) DOCX (~696-713): after the existing TextRuns add runs for authors (`Authors: …`), `DOI: …`, `URL: …`, `Acknowledges facility: Yes/No`, `File: <name>` — each only when present except the acknowledgement line which is always emitted. (4) PDF (~978-1000): after the reference/note detail line, add a second faint 8pt line built from the same fields (authors · DOI · URL · 'Acknowledges facility: Yes/No' · file), using `pdfText` and `checkPage`, and bump `y` accordingly so nothing overlaps. (5) Facility XLSX Sheet 8 (~1214-1224): query adds `LEFT JOIN files f ON f.id = po.file_id` and `f.name AS file_name`; header becomes ['Project Code', 'Project', 'Type', 'Title', 'Authors', 'Reference', 'DOI', 'URL', 'Acknowledges Facility', 'Attached File', 'Date (* = logged date, none set)', 'Note'] with matching row values and 12 `!cols`. Keep the eff_date '*' convention and sheet names unchanged ('Research Outputs' is a comparison target in tests). Then extend C:\Users\Owner\repos\cfc-41\test\unit\exports.test.js with two tests using the existing freshApp/captured pattern: (a) per-project export — insert a project + a files row + an output with doi '10.1000/x1', authors 'A; B', url 'https://example.org/d', acknowledges_facility 1, file_id = that file; call the per-project XLSX export function (find its name in `global.Exports`, e.g. exportXlsx(projectId)) and assert via sheetRows(wb,'Research Outputs') that the header row equals the 10-column list above and the data row carries '10.1000/x1', 'A; B', 'Yes' and the file name; (b) facility-wide `exportAllXlsx` — same assertions on its 'Research Outputs' sheet with the 12-column header, and that an output with acknowledges_facility 0 reads 'No'.

**Verification:** cd C:\Users\Owner\repos\cfc-41 && node --check js/exports.js && TZ='Asia/Jerusalem' node --test test/unit/exports.test.js && node -e "const s=require('fs').readFileSync('js/exports.js','utf8');if((s.match(/'Acknowledges Facility'/g)||[]).length!==2)throw new Error('both XLSX outputs headers need Acknowledges Facility');if((s.match(/LEFT JOIN files f ON f.id = po.file_id/g)||[]).length<2)throw new Error('both outputs queries must join files');if((s.match(/Acknowledges facility: /g)||[]).length<2)throw new Error('DOCX and PDF need the acknowledgement line');if((s.match(/'Research Outputs'\)/g)||[]).length!==2)throw new Error('sheet names must be unchanged');console.log('exports ok')" && git diff --stat -- js/reports.js | grep -c '' | grep -q '^0$'

### T10 CHANGELOG '## [Unreleased]' bullets (#41) and manual update for the enhanced card

**Scope:** (1) Edit C:\Users\Owner\repos\cfc-41\CHANGELOG.md: insert, between the intro paragraph (line 4) and `## [1.11.0] — 2026-09-12`, a new section `## [Unreleased]` with `### Added` bullets, each ending with ` (#41)`: research outputs carry DOI, URL, authors, a facility-acknowledgement flag and an attached file; the Add/Edit Output dialog shows the fields that fit the chosen type (publication, thesis, dataset, software, talk, poster, acknowledgement); drop a PDF onto the Research Outputs card (or use Attach PDF) to store it as an attachment and start a new output with the title read from the file; each output row opens a read-only detail view, and clicking its type badge opens the edit dialog; DOI and URL are clickable links (DOI resolves through doi.org); the new fields appear in the project XLSX/DOCX/PDF exports and the facility-wide 'Research Outputs' sheet; the demo dataset shows a DOI, authors and an acknowledged output. Under `### Changed`: default output types now also include thesis, software, talk and poster (#41). Do NOT add a numbered heading, a date, or bump any version. (2) Edit C:\Users\Owner\repos\cfc-41\docs\manual\projects.html Research Outputs section (~lines 193-216): extend the type list with Thesis, Software, Talk and Poster; rewrite the steps to say the dialog shows the fields that fit the type (DOI, authors, journal for a publication; institution and degree for a thesis; repository link for a dataset or software; venue for a talk or poster), that a checkbox records whether the facility was acknowledged, that clicking an output opens its details and clicking its type tag opens the edit dialog, and that a PDF can be dropped onto the card or attached with Attach PDF to start an output from the file's own title (falling back to the file name). Update the figure caption to match. Prose for facility managers: no 'modal', 'schema', 'column', 'join', 'boolean', 'CRUD' — say 'dialog'. Docs-only, no version bump.

**Verification:** cd C:\Users\Owner\repos\cfc-41 && node -e "const s=require('fs').readFileSync('CHANGELOG.md','utf8');const u=s.indexOf('## [Unreleased]');const r=s.indexOf('## [1.11.0]');if(!(u>0&&u<r))throw new Error('Unreleased must sit above 1.11.0');const sec=s.slice(u,r);const bullets=sec.split('\n').filter(l=>/^\s*[-*] /.test(l));if(bullets.length<5)throw new Error('need >=5 bullets');const bad=bullets.filter(l=>!/\(#41\)\s*$/.test(l));if(bad.length)throw new Error('bullets not ending (#41): '+bad.join(' | '));if(/^## \[Unreleased\] —/m.test(s))throw new Error('no date on Unreleased');console.log('changelog ok')" && TZ='Asia/Jerusalem' node --test test/unit/versions.test.js && grep -c 'Thesis\|thesis' docs/manual/projects.html | grep -qv '^0$' && grep -ci 'Attach PDF' docs/manual/projects.html | grep -qv '^0$' && ! grep -n -i 'modal\|schema\|join table\|boolean\|CRUD' docs/manual/projects.html | grep -q . && git diff --stat -- index.html sw.js js/consts.js | grep -c '' | grep -q '^0$'

### T11 Integration gate: full unit suite, untouched-file and invariant checks

**Scope:** No source edits (fix-forward only if a check fails, and then re-run every check). Run the whole unit suite under TZ='Asia/Jerusalem'; node --check every js/*.js; confirm via git diff that js/reports.js, index.html, sw.js are byte-identical to HEAD and that js/consts.js's APP_VERSION line is unchanged; confirm CUSTOM_REPORT_ENTITY_ORDER untouched (implied by reports.js unchanged); confirm no added line in any js/ file uses toISOString().slice(0, 10) (wiring.test.js also lints this) and no new `<script src=` or CDN URL was added to index.html/js; confirm no `output_people` table anywhere; confirm exactly two '#41 begin'/'#41 end' fence pairs in js/db.js; run the funnel and money tests once more explicitly (funnel counting unchanged, 490 / 546.25 / 589.95 intact). If Playwright happens to be available, also run `node --test 'test/browser/smoke.spec.js'` and report; if not, state plainly that no browser check ran (per docs/cma-lessons.md).

**Verification:** cd C:\Users\Owner\repos\cfc-41 && for f in js/*.js; do node --check "$f" || exit 1; done && TZ='Asia/Jerusalem' node --test 'test/unit/*.test.js' && git diff --quiet HEAD -- js/reports.js index.html sw.js && git diff HEAD -- js/consts.js | grep '^[-+]' | grep -v '^[-+][-+]' | grep -q 'APP_VERSION' && exit 1 || true && ! git diff HEAD -- js/ | grep '^+' | grep -q 'toISOString().slice(0, *10)' && ! grep -rn 'output_people' js/ test/ CHANGELOG.md && grep -c '// --- #41 begin ---' js/db.js | grep -q '^2$' && grep -c '// --- #41 end ---' js/db.js | grep -q '^2$' && ! git diff HEAD -- js/ index.html | grep '^+' | grep -qi 'cdn\.\|<script src=' && TZ='Asia/Jerusalem' node --test test/unit/money.test.js test/unit/funnel-output-date.test.js test/unit/wiring.test.js test/unit/versions.test.js && echo GATE-OK

## Risks

- **wiring.test.js fails between T6 (adds dispatcher cases) and T7 (adds the data-act emitters), or a stray `ic('link')`/`ic('download')` is used although those icons do not exist.** Mitigation: T6's verification deliberately excludes wiring.test.js and T7's runs it; every task lists the only icon names allowed (file, file-plus, external, eye, edit, x, tag). T11 re-runs the whole suite.
- **A sibling branch also creates '## [Unreleased]' or touches migrate()/seed ends, producing a hand-merge conflict.** Mitigation: Both db.js additions are single contiguous blocks fenced by '// --- #41 begin/end ---' at the very end of migrate() and of seedSampleData, so a merger can move them verbatim; the CHANGELOG section is bullet-only with each bullet self-labelled '(#41)'.
- **Clicking a DOI/URL link inside a row also opens the detail modal (delegated click finds the row's data-act), or clicking the badge opens both edit and view.** Mitigation: T7 places anchors and buttons in a sibling `.output-links` element outside the `output-main` view button and verifies programmatically that no `<a` sits inside it; the badge is the innermost [data-act] so closest() resolves to edit only.
- **Type-driven field hiding silently blanks DOI/URL/authors when a user switches type on an existing output (the retired-picker class of bug).** Mitigation: T5 makes visibility display-only: hidden inputs remain in the DOM and are still read on save, and the edit path preserves file_id; documented in consts.js and the task scope.
- **Dropped PDF saved to IndexedDB but the output dialog is cancelled, leaving a blob nobody sees; or the drop bypasses the files table and is lost from backups.** Mitigation: T6 routes through the identical `files` INSERT + DB.saveUpload path as fSave (verified by a source check), so the file shows in Files & Attachments and rides DB.buildBackup; the toast tells the user it was attached.
- **PDF /Title parsing misreads binary or UTF-16 titles, producing garbage titles.** Mitigation: Parser is tolerant (last /Title wins, escapes and BOM handled, 'untitled'/empty rejected) and always falls back to the filename; T2 unit-tests literal, hex/UTF-16, missing and 'untitled' cases; the user confirms the title in the dialog before anything is saved.
- **Seed changes alter funnel demo figures or the 490 / 546.25 / 589.95 booking triple.** Mitigation: T4 uses UPDATEs on existing output rows only (no inserts, no date changes) and its verification runs money.test.js, funnel-output-date.test.js and aggregation.test.js.
- **No browser is available, so the rendered card, drag-and-drop and modals are unverified visually (lesson: 'looks correct' is not verification).** Mitigation: Every UI task has a programmatic source/sheet check plus the Node-run unit suite that evaluates the real IIFEs; T11 runs the Playwright smoke spec if present and otherwise states explicitly that no browser check ran so the human reviewer knows to load http://localhost:8000 (python, not python3) and exercise the card.