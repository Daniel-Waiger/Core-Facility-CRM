{
  "objective": "Release 1.13.0 of Core Facility Tracker on branch claude/jolly-maxwell-95wu0m in /home/user/Core-Facility-CRM: (1) a reusable booking Tags picker with per-tag usage counts (cancelled included) and search-or-create in both booking dialogs only; (2) one role pill on the person profile coloured by a shared Views.typeBadge helper, 'Facility Staff' added to PERSON_TYPES; (3) training levels renamed Regular / Super User with a fenced, idempotent migration of stored 'User' rows; (4) a #/instrument/<id> Instrument Profile route mirroring the person profile (header + New Booking, utilization tiles from Reports aggregations, details, upcoming 14 days, Recent Activity with User / Date / Start–End / Duration / Assisted / Notes / Status and em-dash fallbacks, trained users, active projects); (5) a full-width Today's Agenda card on the Dashboard; (6) a richer demo dataset (9 more people with real roles, 17 more bookings incl. two today, 7 more training rows) respecting every seed/test invariant; (7) a docs-only extensions/importers concept document linked from ROADMAP.md; (8) a final release task bumping index.html/sw.js/js/consts.js to 1.13.0, converting the CHANGELOG '## [Unreleased]' heading to '## [1.13.0] — 2026-09-17', with docs/index.html highlights and manual updates.",
  "assumptions": [
    "Repo /home/user/Core-Facility-CRM, branch claude/jolly-maxwell-95wu0m. Actual HEAD is 34a3f11 ('chore: CMA planner script for the 1.13.0 run'), one tooling commit on top of a106b75 (1.12.1); every js/, css/, test/ and docs/manual file is identical to a106b75. Executors run `git rev-parse --short HEAD` and `git status --short` first; they may see uncommitted work from earlier tasks of THIS run (tags picker, typeBadge, instrument profile, 1.13 fences) and must build on it, never revert it. core.autocrlf is unset and line endings are LF; never introduce CRLF (wiring.test.js and boot-ready-signal.test.js are LF-anchored source scrapers).",
    "Baseline at HEAD: `TZ=Asia/Jerusalem node --test 'test/unit/*.test.js'` -> 318 pass / 0 fail; `TZ=Asia/Jerusalem node --test 'test/browser/*.spec.js'` -> 65 pass / 0 fail (Playwright + Chromium preinstalled, PLAYWRIGHT_BROWSERS_PATH=/opt/pw-browsers). Every criterion means 'no new failures vs this baseline'; tasks that knowingly change an assertion name it (T1: training.test.js fixture level literals; T10: training.test.js '9 training rows' -> 16). New tests add to the pass count.",
    "Environment: Linux, Node v22.22.2, both python3 and python exist (the lessons file's 'python3 does not exist' note is Windows-only and does not apply). Always pass the test glob, never the directory. Run every check from the repo root with absolute paths.",
    "No git command that mutates the working tree (stash/reset/checkout --/clean/commit/switch/pull) in any task; tasks run back to back with no commits between them, the orchestrator commits after verified batches. Read-only comparisons only: `git diff`, `git show HEAD:<path> > /tmp/claude-0/.../scratchpad/<name>`.",
    "NO VERSION BUMP in T1-T12: index.html ?v=1.12.1 (8 tags), sw.js CACHE_VERSION + PRECACHE_URLS, js/consts.js APP_VERSION all stay 1.12.1 until T13. CHANGELOG bullets from feature tasks go under a `## [Unreleased]` heading (created by T1 directly above `## [1.12.1] — 2026-09-17`); versions.test.js's heading regex needs digits, so that heading does not break it. T13 converts it to `## [1.13.0] — 2026-09-17`.",
    "CLAUDE.md invariants apply to every task and are restated in scope where relevant: dates are local calendar days (UI.ymd / UI.today / UI.todayPlusDays, never toISOString().slice(0,10) - wiring.test.js lints this); selectable = not retired OR already selected; denormalized meetings.attendees is only ever written together with meeting_people (seed uses seedBooking({peopleIds})); explicit child-row deletes in delete paths; Title Case for controls/column headings, sentence case for prose/hints/empty states; danger confirm dialogs keep the red Cancel; aggregation figures come from js/reports.js compute* functions, never re-derived; every literal data-act=\"x\" needs a `case 'x':` inside handleAct's switch and vice versa (wiring.test.js scrapes both directions - so picker option buttons must NOT use data-act); every ic('name') must be a key of ui.js ICONS (home folder users user cpu calendar settings gear plus check check-circle alert x file file-plus sparkles rocket layers compass trash target play chevron chevron-left chevron-right sun moon edit search filter external tag clock eye collapse expand copy mail archive book); js/app.js never reads document.querySelector('.modal') (use UI.topModal()).",
    "Reports API as it exists: `Reports.makeFacts(from, to)` returns a facts bundle bound to that range; `Reports.computeInstrumentRows(from, to, facts)` (three arguments, not one) returns `{ rows, totalHours }` with rows `{ id, name, retired, bookings, hours, revenue, sharePct }` where bookings/hours exclude every cancelled booking and revenue follows the Project Costs rule. It has NO distinct-users figure, so the instrument profile's Distinct Users tile is one small direct query (COUNT(DISTINCT meeting_people.person_id) over this instrument's non-cancelled bookings in range) and is documented as such in the code comment.",
    "Demo seed facts at HEAD (js/db.js seedSampleData): 8 people (ids 1-8; 6,7,8 are Facility Staff), 5 instruments (1 Leica SP8, 2 Olympus FV3000, 3 Zeiss Lightsheet, 4 Nikon AX R, 5 Glacios Cryo-TEM), 3 projects, 3 grants, 14 seedBooking calls (booking #1 'Project Kickoff & Laser Alignment Review' is the lowest meeting id and must keep 490 / 546.25 / 589.95), 9 training rows (exactly one NULL trained_on), exactly one 'workshop' booking, tags in use: STED, Fiji, Napari, Lightsheet, Grant Planning, Workshop. No seeded booking falls on day(0) today. Existing same-day occupancy to avoid (instrument or staff): day(-100) Glacios+staff7 09-12; day(-95) FV3000+staff6 09-11; day(-80) staff6 13:00-14:30; day(-70) staff6 10-11:30; day(-60) Glacios+staff7 14:00-14:45; day(-50) Zeiss+staff7 09-10:30; day(-45) Leica+Nikon+staff6 09-13; day(-30) Zeiss+staff8 09-12; day(-20) staff6 11-11:30; day(-8) Leica+staff6 09-10:30 (cancelled); day(-6) staff8 14-16; day(-3) FV3000 09-11; day(-1) staff8 09-13; day(+10) Nikon+staff6 09-10 (cancelled).",
    "The manual (docs/manual/*.html) currently names the training levels nowhere; the only doc text naming 'User or Super User' is the 1.12.0 Highlights card at docs/index.html line 225. docs/screenshots' highest existing number is 72, so new screenshot slots are 73, 74, 75. Manual pages hard-code 'Chapter N' kickers and use manual.js?v=3; no chapter is inserted, so no renumbering. Docs prose is for facility managers: no 'modal' (say dialog), 'CRUD', 'schema', 'join table', 'denormalized', 'boolean', 'foreign key'.",
    "Test helpers to reuse: test/unit/helpers/load-module.js `loadApp([...names], {search})` returns {CONST, DB, UI, Views, Reports, ...}; test/unit/helpers/sqlite.js `freshDb({search})` boots a real in-memory sql.js DB and `seedFixture(DB)` gives alice=1 (PI), sam=2 (Facility Staff, is_staff), scopeA=1, prepB=2, liveProject=1, archivedProject=2; test/unit/helpers/app-harness.js for app.js internals; test/browser/helpers/browser.js (tryRequirePlaywright, chromiumLaunchOptions, startServer, QUIET_FIRST_RUN, setFlagScript, waitForAppReady) and smoke.spec.js's `viewText(route, id)` pattern for rendering a screen in headless Chromium under ?demo=1."
  ],
  "tasks": [
    {
      "id": "T1",
      "title": "Training levels Regular / Super User (+ migration) and 'Facility Staff' in PERSON_TYPES",
      "scope": "Files: /home/user/Core-Facility-CRM/js/consts.js, /home/user/Core-Facility-CRM/js/db.js, /home/user/Core-Facility-CRM/js/app.js, /home/user/Core-Facility-CRM/test/unit/training.test.js, /home/user/Core-Facility-CRM/CHANGELOG.md. (a) consts.js: `TRAINING_LEVELS: ['Regular', 'Super User']`; `PERSON_TYPES: ['Undergrad', 'MSc', 'PhD', 'Postdoc', 'Technician', 'PI', 'Facility Staff', 'Other']` (Facility Staff immediately before Other). APP_VERSION untouched. (b) db.js: both `level TEXT NOT NULL DEFAULT 'User'` (schema ~line 250 and the #47 migrate CREATE ~line 644) become `DEFAULT 'Regular'`; the explanatory comment ~line 2258 `('User')` becomes `('Regular')`; the seven `'User'` literals in the seed `trainingRows` array (~lines 3332-3345) become `'Regular'`; in migrate(), immediately after the line `// --- #41 end ---` (~line 665) and before the function's closing brace, add exactly:\n    // --- 1.13 begin ---\n    // 1.13 renamed the entry training level from 'User' to 'Regular'. Idempotent; runs on boot\n    // and on restore (restoreBackup goes through this same migrate()), so a pre-1.13 backup\n    // reads back with the current vocabulary.\n    try { db.exec(\"UPDATE person_instrument_training SET level='Regular' WHERE level='User'\"); } catch (_) {}\n    // --- 1.13 end ---\n(c) app.js ~line 2374: the dialog default `(cur ? cur.level : 'User')` becomes `'Regular'`. (d) training.test.js: change the three fixture INSERTs that write level 'User' (~lines 87, 102, 122) to 'Regular' (these are the only assertions this task knowingly changes); append a new `describe('1.13 training level rename', ...)` with: test 1 - extract the SQL between `// --- 1.13 begin ---` and `// --- 1.13 end ---` from js/db.js source (same fence-regex technique as booking-tags.test.js line 55), pull the `UPDATE ...` statement out of it verbatim, run it on a raw sql.js database (require libs/sql-asm.js, `new SQL.Database()`, create person_instrument_training with the CREATE from the #47 fence) holding one 'User' row and one 'Super User' row, assert levels read ['Regular','Super User'], run it again and assert no change (idempotent); test 2 - `freshDb()` + seedFixture, insert a training row with level 'User' via DB.run, `await DB.buildBackup()` (install the FileReader stub exactly as test/unit/backup-roundtrip.test.js lines 17-25 do), `await DB.restoreBackup(backup)`, assert `SELECT COUNT(*) FROM person_instrument_training WHERE level='User'` is 0 and `WHERE level='Regular'` is 1; test 3 - `loadApp(['consts']).CONST`: TRAINING_LEVELS deepEqual ['Regular','Super User'], PERSON_TYPES.indexOf('Facility Staff') === PERSON_TYPES.indexOf('Other') - 1. (e) CHANGELOG.md: insert directly above `## [1.12.1] — 2026-09-17`:\n## [Unreleased]\n\n### Added\n- 'Facility Staff' is a real Role / Position option in the person dialog and People filter, not only demo text.\n\n### Changed\n- Training levels are now **Regular** and **Super User**; records saved as 'User' are renamed automatically on load and on restore.\n\n(blank line before the 1.12.1 heading). Do not touch docs/, index.html, sw.js.",
      "depends_on": [],
      "parallel_safe": false,
      "verification": "From /home/user/Core-Facility-CRM: `grep -c \"level='User'\" js/db.js` prints 1 (the migration line only); `grep -c \"DEFAULT 'Regular'\" js/db.js` prints 2; `grep -c \"'User'\" js/app.js` prints 0; `grep -c \"1.13 begin\" js/db.js` prints 1 and `grep -c \"1.13 end\" js/db.js` prints 1; `awk '/#41 end ---/{f=1} f&&/1.13 begin/{print \"ORDER-OK\"; exit}' js/db.js` prints ORDER-OK. `node -e \"const {loadApp}=require('./test/unit/helpers/load-module');const C=loadApp(['consts']).CONST;console.log(JSON.stringify(C.TRAINING_LEVELS),C.PERSON_TYPES.indexOf('Facility Staff'),C.PERSON_TYPES.indexOf('Other'),C.PERSON_TYPES.length)\"` prints `[\"Regular\",\"Super User\"] 6 7 8`. `node -e \"const {freshDb,seedFixture}=require('./test/unit/helpers/sqlite');(async()=>{const {DB}=await freshDb();seedFixture(DB);DB.run(\\\"INSERT INTO person_instrument_training (person_id, instrument_id, trained_on) VALUES (1,1,'2026-01-01')\\\");console.log(DB.row('SELECT level FROM person_instrument_training').level)})()\"` prints `Regular`. `grep -c '^## \\[Unreleased\\]' CHANGELOG.md` prints 1 and `grep -n '^## \\[' CHANGELOG.md | head -2` shows Unreleased on the line above 1.12.1. `for f in js/*.js; do node --check \"$f\"; done` exits 0. `TZ=Asia/Jerusalem node --test 'test/unit/*.test.js'` reports 0 fail and at least 321 pass (318 baseline + 3 new), including a passing test whose name contains 'idempotent' or 'rename'."
    },
    {
      "id": "T2",
      "title": "DB.bookingTagCounts() with unit tests",
      "scope": "Files: /home/user/Core-Facility-CRM/js/db.js, /home/user/Core-Facility-CRM/test/unit/booking-tags.test.js. In db.js, next to vocabList/addVocab (~line 2009), add `function bookingTagCounts()`: read `SELECT tags FROM meetings WHERE TRIM(COALESCE(tags,''))<>'' ORDER BY id` (ALL rows - cancelled included, no date filter), split each with `global.UI.parseTags`, count per tag; then union `vocabList('BOOKING_TAG')` terms with count 0 for terms not yet seen. Merge is case-insensitive with the FIRST spelling encountered winning (meetings in id order first, then vocab terms) - the same rule as app.js's unionNames. Return `[{ tag, count }]` sorted by count desc, then `tag.localeCompare` asc. Export it from the `global.DB = {...}` object (add `bookingTagCounts,` right after `addVocab,`). Comment it: this feeds the booking dialogs' tag picker and deliberately counts cancelled bookings (a tag's usage history, not occupancy). In booking-tags.test.js append `describe('1.13 DB.bookingTagCounts', ...)` using `freshDb()` + `seedFixture(DB)` with tests: (1) a booking tagged 'SIM, Fiji' counts once under each tag; (2) a cancelled booking (`is_cancelled=1`) tagged 'sim' still counts and merges into 'SIM' (first spelling wins, count 2); (3) `DB.addVocab('BOOKING_TAG','Napari')` with no booking appears with count 0; (4) ordering is count desc then name; (5) an empty database with no vocab returns []. Insert meetings with `INSERT INTO meetings (title, date, tags[, is_cancelled]) VALUES (...)` - title is the only NOT NULL column. Also add a one-line row for the new describe to test/README.md's booking-tags entry ONLY if that table lists it (it does not at HEAD - then leave README alone). No CHANGELOG bullet here (T4 writes the user-facing one).",
      "depends_on": [
        "T1"
      ],
      "parallel_safe": true,
      "verification": "From /home/user/Core-Facility-CRM: `node -e \"const {freshDb,seedFixture}=require('./test/unit/helpers/sqlite');(async()=>{const {DB}=await freshDb();seedFixture(DB);DB.run(\\\"INSERT INTO meetings (title,date,tags) VALUES ('a','2026-01-05','SIM, Fiji')\\\");DB.run(\\\"INSERT INTO meetings (title,date,tags,is_cancelled) VALUES ('b','2026-01-06','sim',1)\\\");DB.addVocab('BOOKING_TAG','Napari');console.log(JSON.stringify(DB.bookingTagCounts()))})()\"` prints exactly `[{\"tag\":\"SIM\",\"count\":2},{\"tag\":\"Fiji\",\"count\":1},{\"tag\":\"Napari\",\"count\":0}]`. `grep -c 'bookingTagCounts' js/db.js` prints at least 2 (definition + export). `node --check js/db.js` exits 0. `TZ=Asia/Jerusalem node --test 'test/unit/*.test.js'` reports 0 fail and at least 5 more passing tests than after T1 (>= 326)."
    },
    {
      "id": "T3",
      "title": "Views.typeBadge helper; one role pill on the person profile and People list",
      "scope": "File: /home/user/Core-Facility-CRM/js/views.js (+ one CHANGELOG.md bullet). Add `function typeBadge(type)` directly after `statusBadge` (~line 225): `PI` -> `badge primary`, `Facility Staff` -> `badge success`, anything else (Postdoc, PhD, MSc, Undergrad, Technician, Researcher, Other, custom vocab, empty) -> `badge neutral`; text is `esc(type || '—')` verbatim, no suffix. Returns `<span class=\"badge X\">TEXT</span>` with no extra attributes. Export it in `global.Views = {...}` next to `statusBadge`. Person profile header (~lines 781-785): replace `<span class=\"badge neutral\">${esc(p.type)}</span>` with `${typeBadge(p.type)}` and DELETE the hardcoded `${p.is_staff ? '<span class=\"badge success\" data-tooltip=\"Billable by the hour on bookings\">Facility Staff</span>' : ''}` line, so the header carries exactly one pill (plus nothing else). The Rate/hr meta item and everything else in the profile stay. People list (~line 723): Role column `<td><span class=\"badge neutral\">${esc(r.type)}</span></td>` becomes `<td>${typeBadge(r.type)}</td>`; the Facility Staff check column (~line 729) is unchanged (it is the billing flag). Grep-check before editing that 'Facility Staff' in js/ is only data, labels and tooltips (it is: app.js 2821-2869, 3182-3189, 3610, 3746, 3955, 5302-5319 are comments/labels/tips; db.js seeds; exports.js a column header) - do not touch those. CHANGELOG.md under `## [Unreleased]` / `### Changed`: `- The person profile shows one role pill, coloured by role (PI, Facility Staff, everyone else), and the People list uses the same colouring.`",
      "depends_on": [
        "T1"
      ],
      "parallel_safe": true,
      "verification": "From /home/user/Core-Facility-CRM: `node -e \"const {loadApp}=require('./test/unit/helpers/load-module');const {freshDb,seedFixture}=require('./test/unit/helpers/sqlite');(async()=>{const a=loadApp(['consts','db','ui','views']);globalThis.initSqlJs=require('./libs/sql-asm.js');await a.DB.boot();seedFixture(a.DB);const V=a.Views;console.log(V.typeBadge('PI')+'|'+V.typeBadge('Facility Staff')+'|'+V.typeBadge('PhD'));const h=V.personDetail(2);const head=h.split('Personal Details')[0];console.log((head.match(/class=\\\"badge /g)||[]).length, head.includes('badge success\\\">Facility Staff'), h.includes('data-tooltip=\\\"Billable by the hour on bookings\\\">Facility Staff'));})()\"` prints `<span class=\"badge primary\">PI</span>|<span class=\"badge success\">Facility Staff</span>|<span class=\"badge neutral\">PhD</span>` then `1 true false`. `grep -c 'typeBadge' js/views.js` prints at least 4 (definition, export, profile, list). `node --check js/views.js` exits 0. `TZ=Asia/Jerusalem node --test 'test/unit/*.test.js'` 0 fail, no new failures. Render check: `TZ=Asia/Jerusalem node --test 'test/browser/smoke.spec.js'` passes (people screen renders, 0 JS errors)."
    },
    {
      "id": "T4",
      "title": "Booking Tags picker (mountTagPicker) in both booking dialogs, CSS, browser spec",
      "scope": "Files: /home/user/Core-Facility-CRM/js/app.js, /home/user/Core-Facility-CRM/css/app.css, new /home/user/Core-Facility-CRM/test/browser/tag-picker.spec.js, /home/user/Core-Facility-CRM/test/README.md (one line), CHANGELOG.md (one bullet). In app.js: (1) replace the body of `tagsField(id, value)` (~line 104) to return:\n<div class=\"field\"><label>Tags</label>\n  <div class=\"tag-picker\" data-for=\"${id}\">\n    <div class=\"token-list\"></div>\n    <input type=\"text\" class=\"input tag-search\" placeholder=\"Search or create a tag…\" autocomplete=\"off\" aria-label=\"Search or create a tag\" />\n    <div class=\"tag-dropdown\" hidden></div>\n    <input type=\"hidden\" id=\"${id}\" value=\"${esc(value)}\" />\n  </div></div>\nDelete `bookingTagSuggestions()` (tagsField was its only caller) and its comment. (2) Add `function mountTagPicker(m, id)` right after `mountTokenPicker`, modeled on it but simpler (string ids, free-text create, no lock/filter/onChange). State: `known` = `DB.bookingTagCounts()` copied to an array of {tag,count}; `selected` = `UI.parseTags(hidden.value)` (existing chips render at mount, so an edit dialog shows stored tags). render(): chips into `.token-list` as `<span class=\"token\" data-tag=\"${esc(t)}\"><button type=\"button\" class=\"token-x\" aria-label=\"Remove ${esc(t)}\">&times;</button>${esc(t)}</span>`; dropdown items for known tags not selected whose tag contains the typed text case-insensitively, as `<button type=\"button\" class=\"tag-option\" data-tag=\"${esc(t)}\">${esc(t)} <span class=\"tag-count\">×${count}</span></button>`, sorted as bookingTagCounts returns them; when the typed text is non-empty and no known or selected tag equals it case-insensitively, append `<button type=\"button\" class=\"tag-option tag-create\" data-create=\"${esc(typed)}\">Create “${esc(typed)}”</button>`; dropdown `hidden` when it has no items or the search box is not focused. sync(): `hidden.value = UI.joinTags([...selected])` (stored format identical to today, so bookingSave/bookingEditSave at ~3817/4019 stay unchanged). Events: search `input` and `focus` -> render/show; `blur` -> hide after a short timeout (and dropdown `mousedown` preventDefault so a click registers before blur); `keydown` Enter -> preventDefault + stopPropagation (so the modal's Enter-to-save never fires from this box), then: exact case-insensitive match among known -> select that spelling; else exactly one visible option -> select it; else non-empty typed -> create (push {tag: typed, count: 0} into known, select); clear the box; Esc with a non-empty box -> stopPropagation + clear (empty box lets Esc bubble to close the modal, same as token-search). Clicking `.tag-option` selects `data-tag`, or creates `data-create`; clicking `.token-x` removes the chip (a created tag remains in `known` so it can be re-added). NEVER use data-act on these buttons (wiring.test.js would flag them); wire with addEventListener inside mountTagPicker. Tags pass through UI.parseTags on every add. (3) In `mountBookingModal(m, opts)` (~line 3584) call `mountTagPicker(m, opts.ids.prefix + '-tags');` after the three mountTokenPicker calls (confirm the two opts literals at ~3757 and ~3970 use prefix 'bk' and 'bke'; if the edit modal's prefix differs, pass the id explicitly instead). Vocab learn-back after save (~3909, ~4139) unchanged; calendar chips and views.js tagChips unchanged. (4) css/app.css after the `.token-select.locked` rule (~line 519) add: `.tag-picker { display: flex; flex-direction: column; gap: 6px; }`, `.tag-dropdown { display: flex; flex-direction: column; gap: 2px; max-height: 180px; overflow-y: auto; padding: 4px; border: 1px solid var(--border); border-radius: 8px; background: var(--surface-2); }`, `.tag-dropdown[hidden] { display: none; }` (REQUIRED: the flex display would otherwise defeat the hidden attribute - the #41 T5 lesson), `.tag-option { display: flex; align-items: center; justify-content: space-between; text-align: left; padding: 6px 8px; border: 0; border-radius: 6px; background: transparent; color: inherit; font: inherit; cursor: pointer; }`, `.tag-option:hover, .tag-option:focus { background: var(--surface); }`, `.tag-count { font-size: 11px; color: var(--text-muted); margin-left: 8px; }`, `.tag-create { font-style: italic; }` - use only CSS custom properties that already exist in css/app.css (check with `grep -o -- '--[a-z0-9-]*' css/app.css | sort -u`). (5) New test/browser/tag-picker.spec.js modeled on smoke.spec.js (same before/after, ?demo=1, timezoneId Asia/Jerusalem, pageerror/console-error capture, UI.stopTour): route to 'calendar', click the first `[data-act=\"new-booking\"]`, wait for `.modal #bk-tags`; assert `.tag-picker .tag-dropdown` becomes visible on focusing `.tag-search` and its innerText matches /×\\d+/; type 'Fiji' + Enter -> `.token[data-tag=\"Fiji\"]` exists and `#bk-tags` value is 'Fiji'; type 'Brandnew' + Enter -> value 'Fiji, Brandnew'; click the Fiji chip's `.token-x` -> value 'Brandnew'; close the modal (Esc twice); then open an edit dialog for the seeded tagged booking `Open Office Hours: Image Analysis Pipeline Consultation` via `page.evaluate(() => { const id = DB.row(\"SELECT id FROM meetings WHERE tags<>'' ORDER BY id LIMIT 1\").id; document.body.insertAdjacentHTML('beforeend', '<button id=\"t-edit\" data-act=\"edit-booking\" data-id=\"'+id+'\"></button>'); document.getElementById('t-edit').click(); })`, wait for `#bke-tags`, assert its value equals the stored tags string and that `.token` count equals `UI.parseTags(stored).length`; final test asserts zero page errors. (6) test/README.md: add `tag-picker.spec.js` to the browser-group paragraph (one sentence). (7) CHANGELOG under `### Added`: `- **A tag picker in both booking dialogs**: pick from every tag already used (with how many bookings carry it, cancelled ones included) or type to create a new one; stored tags are unchanged.`",
      "depends_on": [
        "T2"
      ],
      "parallel_safe": false,
      "verification": "From /home/user/Core-Facility-CRM: `grep -c 'function mountTagPicker' js/app.js` prints 1; `grep -c 'mountTagPicker(m' js/app.js` prints 2 (definition + call); `grep -c 'bookingTagSuggestions' js/app.js` prints 0; `grep -c 'type=\"hidden\" id=\"${id}\"' js/app.js` prints 1; `grep -c 'data-act' <(sed -n \"$(grep -n 'function mountTagPicker' js/app.js | cut -d: -f1),$(grep -n 'function mountBookingModal' js/app.js | cut -d: -f1)p\" js/app.js) ` prints 0 (no data-act inside the picker); `grep -c '\\.tag-dropdown\\[hidden\\]' css/app.css` prints 1; `for f in js/*.js; do node --check \"$f\"; done` exits 0; `TZ=Asia/Jerusalem node --test 'test/unit/*.test.js'` 0 fail (wiring.test.js dispatch completeness passes with no new data-act); `TZ=Asia/Jerusalem node --test 'test/browser/tag-picker.spec.js'` passes every test incl. the zero-errors test and prints the hidden-value sequence `Fiji` / `Fiji, Brandnew` / `Brandnew` in its assertions; `TZ=Asia/Jerusalem node --test 'test/browser/*.spec.js'` has no new failures vs baseline 65 (total >= 65 + new tests)."
    },
    {
      "id": "T5",
      "title": "Views.instrumentDetail renderer and Instruments list Open Profile link",
      "scope": "File: /home/user/Core-Facility-CRM/js/views.js. Add `function instrumentDetail(id)` after `instruments()` (before the Calendar section), exported in global.Views next to personDetail, mirroring personDetail's card shape. `const i = DB.row('SELECT * FROM instruments WHERE id=?')`; missing -> `emptyState('cpu', 'Instrument not found', 'This instrument may have been deleted.')`. Cards in order: (1) HEADER `<div class=\"card mb-16\">`: `<span class=\"project-title\">${esc(UI.retiredName(i.name, i.is_retired))}</span>`, kind `<span class=\"chip-sm\">` when set, status badge with the SAME class map as the list row (Available->success, In-use->primary, Down->danger, else warning), a `badge neutral` Retired pill when retired; buttons: `<button class=\"btn btn-secondary btn-sm\" data-goto=\"instruments\">Back to Instruments</button>`, `<button class=\"btn btn-mint btn-sm\" data-act=\"new-booking\" data-date=\"${today()}\" data-inst=\"${i.id}\">${ic('calendar')} New Booking</button>` rendered ONLY when `!i.is_retired` (retired instruments are never offered for new work), `<button class=\"btn btn-primary btn-sm\" data-act=\"edit-instrument\" data-id=\"${i.id}\">Edit Instrument</button>`. (2) UTILIZATION: two `grid cols-4 mb-16` rows labelled This Month / This Year (a `faint small` caption above each). Ranges: `const now = new Date(); const monthFrom = UI.ymd(new Date(now.getFullYear(), now.getMonth(), 1)); const yearFrom = UI.ymd(new Date(now.getFullYear(), 0, 1)); const to = today();` - never toISOString. Per range: `const facts = global.Reports.makeFacts(from, to); const r = global.Reports.computeInstrumentRows(from, to, facts).rows.find((x) => x.id === i.id) || { bookings: 0, hours: 0, revenue: 0 };` and distinct users `DB.row('SELECT COUNT(DISTINCT mp.person_id) AS n FROM meetings m JOIN meeting_instruments mi ON mi.meeting_id=m.id JOIN meeting_people mp ON mp.meeting_id=m.id WHERE mi.instrument_id=? AND m.is_cancelled=0 AND m.date>=? AND m.date<=?', [i.id, from, to]).n` (comment: Reports has no distinct-users figure for instruments; cancelled excluded like occupancy). Tiles: `<div class=\"card stat\" data-tile=\"${prefix}-bookings\" data-value=\"${r.bookings}\"><span class=\"n\">${r.bookings}</span><span class=\"l\">Bookings</span></div>`, `${prefix}-hours` with data-value = raw `r.hours` and text `Math.round(r.hours * 100) / 100`, `${prefix}-users` (n), `${prefix}-charges` with data-value raw `r.revenue` and text `UI.fmtMoney(r.revenue)`; prefix 'month' / 'year'; labels Bookings · Booked Hours · Distinct Users · Charges. Caption sentence: 'Cancelled bookings are excluded from bookings and hours; charges follow the Project Costs rule.' (3) DETAILS card (`metadata-grid`/`meta-item` like personDetail, dv() for em-dash fallbacks): Modality / Kind, Status, Location, Cost (`UI.fmtMoney(i.cost||0)`), Billed Per (`UI.unitLabel(i.cost_unit||'time')`), Min Duration / Max Duration (`${n} min` or '—' when 0), Min Gap (`${n} min` or '—'), Min Notice (`${n} h` or '—'), Supervisors (`GROUP_CONCAT(pe.name || CASE WHEN pe.is_retired THEN ' (Retired)' ELSE '' END, ', ')` from instrument_staff), Config Notes (span 2). (4) UPCOMING BOOKINGS: `SELECT m.id, m.title, m.date, m.start_time, m.end_time, users, assisted FROM meetings m JOIN meeting_instruments mi ON mi.meeting_id=m.id WHERE mi.instrument_id=? AND m.is_cancelled=0 AND m.date>=? AND m.date<=? ORDER BY m.date, m.start_time, m.id` with `[i.id, today(), UI.todayPlusDays(14)]`; columns Date · Time · Title · User · Assisted; rows `<tr class=\"row-link\" data-act=\"edit-booking\" data-id=\"${m.id}\">`; empty -> `emptyState('calendar', 'Nothing booked in the next 14 days', 'Upcoming bookings on this instrument will appear here.')`. (5) RECENT ACTIVITY: same join, `m.date <= today()` ORDER BY m.date DESC, m.start_time DESC, m.id DESC LIMIT 25, cancelled KEPT; subselects `users` = GROUP_CONCAT of meeting_people names with the retired CASE suffix, `assisted` = GROUP_CONCAT of meeting_staff names with the same CASE; columns exactly `<th>User</th><th>Date</th><th>Start–End</th><th>Duration</th><th>Assisted</th><th>Notes</th><th>Status</th>`; cells: User `m.users ? esc(m.users) : '—'`; Date `fmt(m.date)`; Start–End `m.start_time ? esc(m.start_time) + (m.end_time ? '–' + esc(m.end_time) : '') : '—'`; Duration via a module helper `durationLabel(start, end)`: `const h = UI.hoursBetween(start, end); if (!h) return '—'; const H = Math.floor(h), M = Math.round((h - H) * 60); return H + 'h ' + M + 'm';` (1.5 -> '1h 30m', 2 -> '2h 0m'); Assisted `m.assisted ? esc(m.assisted) : '—'`; Notes via helper `notePreview(note)`: html = UI.noteHtml(note); strip tags `/<[^>]*>/g` -> ' ', decode &amp; &lt; &gt; &quot; &#39; &nbsp;, collapse whitespace, trim; '' -> '—'; else `<span title=\"${esc(full)}\">${esc(full.length > 80 ? full.slice(0, 79) + '…' : full)}</span>`; Status `m.is_cancelled ? '<span class=\"badge danger\">Cancelled</span>' : '—'`; row class `row-retired` when cancelled; footer hint 'Showing the 25 most recent bookings on this instrument. Cancelled bookings are kept and marked.'; empty -> `emptyState('calendar', 'No bookings yet', 'Bookings on this instrument will show up here.')`. (6) TRAINED USERS: `DB.listInstrumentTraining(i.id)`; columns Person · Level · Trained On · Trainer · Expires On (+ status badge via `DB.trainingStatusOn(rec, today())` exactly like personDetail: Expired -> badge warning, pending -> 'Not Yet Valid' badge neutral); Person `UI.retiredName(rec.person_name, rec.person_retired)` as `<td class=\"row-link\" data-goto=\"person\" data-id=\"${rec.person_id}\">`; Trainer needs a trainer name: extend nothing in db.js - do a second lookup map `DB.rows('SELECT id, name, is_retired FROM people')` and resolve `rec.trainer_id` in JS with UI.retiredName ('—' when null); empty -> `emptyState('cpu', 'No trained users yet', 'Training records are added from a person\\'s profile.')`. (7) ACTIVE PROJECTS: `SELECT p.id, p.code, p.title, p.status, p.is_archived FROM project_instruments pi JOIN projects p ON p.id=pi.project_id WHERE pi.instrument_id=? ORDER BY p.is_archived, p.title` rows `<tr class=\"row-link ${archived ? 'row-retired' : ''}\" data-goto=\"project\" data-id>` with Code · Title · Status (statusBadge + Archived pill); empty -> `emptyState('folder', 'No projects yet', 'Projects this instrument is assigned to will appear here.')`. Instruments list row (~line 985): `<tr class=\"row-link ${r.is_retired ? 'row-retired' : ''}\" data-goto=\"instrument\" data-id=\"${r.id}\">` and prepend `<button class=\"btn btn-ghost btn-xs\" data-goto=\"instrument\" data-id=\"${r.id}\" title=\"Open Profile\">${ic('chevron')}</button>` to its Actions cell (same as the People list). Only icons from the ICONS list; Title Case for headings/buttons, sentence case for captions/empty states. Also add `instrumentDetail` to the global.Views export. Note: until T7 lands, clicking the row routes to the dashboard (hashFor fallback) - expected.",
      "depends_on": [
        "T3"
      ],
      "parallel_safe": false,
      "verification": "From /home/user/Core-Facility-CRM: `node -e \"const {loadApp}=require('./test/unit/helpers/load-module');const {seedFixture}=require('./test/unit/helpers/sqlite');(async()=>{const a=loadApp(['consts','db','ui','views','reports']);globalThis.initSqlJs=require('./libs/sql-asm.js');await a.DB.boot();seedFixture(a.DB);const {DB,UI,Views,Reports}=a;const d=new Date();const from=UI.ymd(new Date(d.getFullYear(),d.getMonth(),1));DB.run(\\\"INSERT INTO meetings (title,date,start_time,end_time) VALUES ('Solo run',?,'09:00','10:30')\\\",[from]);DB.run('INSERT INTO meeting_instruments (meeting_id,instrument_id,amount,line_cost) VALUES (1,1,0,300)');const h=Views.instrumentDetail(1);const t=(k)=>(h.match(new RegExp('data-tile=\\\"'+k+'\\\" data-value=\\\"([^\\\"]+)\\\"'))||[])[1];const row=h.split('Solo run')[0].split('<tr').pop();console.log(t('month-bookings'),t('month-hours'),t('month-users'),t('month-charges'),h.includes('1h 30m'),(h.split('<th>User</th><th>Date</th><th>Start–End</th><th>Duration</th><th>Assisted</th><th>Notes</th><th>Status</th>').length-1),Reports.computeInstrumentRows(from,UI.today()).rows[0].hours,(h.match(/data-act=\\\"new-booking\\\" data-date=\\\"[0-9-]+\\\" data-inst=\\\"1\\\"/)||[]).length,Views.instrumentDetail(99).includes('Instrument not found'))})()\"` prints `1 1.5 0 300 true 1 1.5 1 true`. `grep -c 'data-goto=\"instrument\"' js/views.js` prints 2 (row + chevron button). `grep -c 'Views\\.instrumentDetail\\|instrumentDetail,' js/views.js` prints at least 1 (export). `grep -o \"ic('[a-z-]*')\" js/views.js | sort -u` contains only names from the ICONS list. `node --check js/views.js` exits 0. `TZ=Asia/Jerusalem node --test 'test/unit/*.test.js'` 0 fail (wiring.test.js's date lint and icon check pass). `TZ=Asia/Jerusalem node --test 'test/browser/smoke.spec.js'` passes (instruments list renders with the new link, 0 JS errors)."
    },
    {
      "id": "T6",
      "title": "Unit test test/unit/instrument-profile.test.js",
      "scope": "Files: new /home/user/Core-Facility-CRM/test/unit/instrument-profile.test.js, /home/user/Core-Facility-CRM/test/README.md (one table row). Header comment in the style of dashboard.test.js. `freshApp()` = `loadApp(['consts','db','ui','views','reports'])` + initSqlJs + boot; `seedFixture(DB)`. Tests, each on a fresh app: (1) 'em-dash fallbacks: a booking with no attendees, staff, note or times renders — in User, Start–End, Duration, Assisted, Notes and Status' - insert meeting titled 'Bare Row' dated UI.todayPlusDays(-2) with no times, meeting_instruments (id,1); slice the html between 'Bare Row' and the next '</tr>' and assert it contains at least 5 occurrences of '—' and no 'undefined'/'null'. (2) 'Duration is UI.hoursBetween formatted Xh Ym' - 09:00-10:30 -> '1h 30m'; 09:00-11:00 -> '2h 0m'. (3) 'Assisted lists meeting_staff names, retired suffix included' - meeting_staff (meeting, sam) -> row contains 'Sam'; then `DB.setRetired('people', 2, true)` -> row contains 'Sam (Retired)'. (4) 'Recent Activity keeps a cancelled booking and marks it' - cancelled meeting -> 'badge danger\">Cancelled' and 'row-retired' present in its row. (5) 'Upcoming Bookings shows a booking 3 days out and not one 20 days out, and never a cancelled one'. (6) 'Utilization tiles equal Reports.computeInstrumentRows for the same range' - two bookings in the current month (one live 09:00-11:00 line_cost 200, one cancelled retained 09:00-10:00 line_cost 100 on another day), read `data-tile=\"month-bookings|hours|charges\"` data-values and compare to `Reports.computeInstrumentRows(from, UI.today()).rows.find(r=>r.id===1)` (bookings 1, hours 2, revenue 300 because a retained cancellation still bills), and `month-users` equals COUNT(DISTINCT) of meeting_people on the live booking (insert alice on it -> 1). Use `UI.ymd(new Date(y, m, 1))` for the month start; keep every fixture date inside the range its assertion uses (the #47 T5 lesson). (7) 'Trained Users lists DB.listInstrumentTraining rows with level and status badge' - insert a Regular row for alice on scopeA expiring yesterday -> 'Regular' and 'Expired' appear. (8) 'the missing-instrument empty state renders'. test/README.md: add a row `| \\`instrument-profile.test.js\\` | The Instrument Profile: em-dash fallbacks, the Xh Ym duration string, the Assisted column, cancelled rows kept and marked, the 14-day upcoming window, and utilization tiles equal to \\`Reports.computeInstrumentRows\\` for the same range |` after the training.test.js row.",
      "depends_on": [
        "T5"
      ],
      "parallel_safe": false,
      "verification": "From /home/user/Core-Facility-CRM: `TZ=Asia/Jerusalem node --test test/unit/instrument-profile.test.js 2>&1 | grep -E '^# (pass|fail)'` prints `# pass 8` and `# fail 0`. `grep -c 'instrument-profile.test.js' test/README.md` prints 1. `TZ=Asia/Jerusalem node --test 'test/unit/*.test.js'` 0 fail, total pass >= previous + 8."
    },
    {
      "id": "T7",
      "title": "Route #/instrument/<id> in app.js; wiring test and smoke spec coverage",
      "scope": "Files: /home/user/Core-Facility-CRM/js/app.js, /home/user/Core-Facility-CRM/test/unit/wiring.test.js, /home/user/Core-Facility-CRM/test/browser/smoke.spec.js, CHANGELOG.md (one bullet). app.js: `const ctx = { route: 'dashboard', project: null, person: null, instrument: null };` (line 6); `TITLES` gains `instrument: 'Instrument Profile',` right after `person:`; `hashFor`: add `if (name === 'instrument' && id) return '#/instrument/' + Number(id);`; `parseHash`: add the `parts[0] === 'instrument'` branch identical to the person one; `applyRoute`: `ctx.instrument = name === 'instrument' ? Number(id) : null;`; `onHashChange`: add the stale-id guard `if (parsed.name === 'instrument' && !DB.row('SELECT id FROM instruments WHERE id=?', [parsed.id])) { location.replace('#/instruments'); return; }`; `initRouting`: extend the validity condition with `&& (parsed.name !== 'instrument' || DB.row('SELECT id FROM instruments WHERE id=?', [parsed.id]))`; `renderView`: add `name === 'instrument' ? Views.instrumentDetail(ctx.instrument) :` after the person line. Do NOT add 'instrument' to HASH_ROUTES and do NOT add any data-nav. Keep `function handleAct(act, el) {` / `switch (act) {` / boot() shapes untouched (source scrapers). Confirm the existing `case 'new-booking'` forwards `el.dataset.inst` (it does at ~1491) and `case 'edit-instrument'`/`'edit-booking'` exist - no new dispatcher case is needed because the profile only emits existing data-act values plus data-goto. wiring.test.js 'data-nav completeness' test: next to the 'project' asymmetry assertions add `assert.ok(!hashRoutes.has('instrument'), ...)` and `assert.ok(titleKeys.has('instrument'), ...)`, plus the same pair for 'person' if absent. smoke.spec.js: add a test 'instrument profile renders, with its activity and training cards' after the project-detail test: `const id = await page.evaluate(() => DB.row('SELECT id FROM instruments ORDER BY id LIMIT 1').id); const info = await viewText('instrument', id); assert.ok(info.len > 500); assert.ok(/Recent Activity/.test(info.txt)); assert.ok(/Trained Users/.test(info.txt)); assert.ok(/Booked Hours/.test(info.txt));` and a second test 'a stale instrument id falls back to the Instruments list': `await page.evaluate(() => { location.hash = '#/instrument/99999'; }); await page.waitForTimeout(450); assert.equal(await page.evaluate(() => location.hash), '#/instruments');`. CHANGELOG `### Added`: `- **An Instrument Profile page**, opened from the Instruments list, with this month's and this year's bookings, booked hours, distinct users and charges (the same figures as Reports), details and supervisors, the next 14 days of bookings, recent activity (user, time, duration, assisting staff, notes, status), trained users and active projects, plus a New Booking button pre-set to that instrument.`",
      "depends_on": [
        "T5"
      ],
      "parallel_safe": false,
      "verification": "From /home/user/Core-Facility-CRM: `grep -c \"'#/instrument/'\" js/app.js` prints 1; `grep -c \"instrument: 'Instrument Profile'\" js/app.js` prints 1; `grep -c 'Views.instrumentDetail(ctx.instrument)' js/app.js` prints 1; `grep -c \"location.replace('#/instruments')\" js/app.js` prints 1; `grep -c \"'instrument'\" <(grep -o 'const HASH_ROUTES = \\[[^]]*\\]' js/app.js)` prints 0. `node -e \"const {loadApp}=require('./test/unit/helpers/load-module');const s=require('fs').readFileSync('js/app.js','utf8');const m=s.match(/function hashFor[\\s\\S]*?\\n  \\}/)[0]+s.match(/function parseHash[\\s\\S]*?\\n  \\}/)[0];const HASH_ROUTES=['dashboard','projects','people','instruments','calendar','reports','settings'];eval(m);console.log(hashFor('instrument',3),JSON.stringify(parseHash('#/instrument/3')),JSON.stringify(parseHash('#/instrument/x')))\"` prints `#/instrument/3 {\"name\":\"instrument\",\"id\":3} null`. `for f in js/*.js; do node --check \"$f\"; done` exits 0. `TZ=Asia/Jerusalem node --test 'test/unit/*.test.js'` 0 fail (wiring.test.js data-nav test passes with the new asymmetry assertions; boot-ready-signal.test.js still passes). `TZ=Asia/Jerusalem node --test 'test/browser/smoke.spec.js' 2>&1 | grep -E '^# (pass|fail)'` shows fail 0 and pass = previous smoke count + 2, including the 'instrument profile renders' test; the zero-JS-errors test still passes."
    },
    {
      "id": "T8",
      "title": "Dashboard Today's Agenda card",
      "scope": "Files: /home/user/Core-Facility-CRM/js/views.js (dashboard(), lines ~20-83), /home/user/Core-Facility-CRM/test/unit/dashboard.test.js, CHANGELOG.md (one bullet). In dashboard(): compute `const todayStr = now` (already UI.today()) and `const agendaDate = new Date().toLocaleDateString(DATE_LOCALE, { weekday: 'long', year: 'numeric', month: 'long', day: 'numeric' })` (display only). Queries adapted from app.js openTodayModal (~5832-5846): bookingsToday = `SELECT m.id, m.title, m.start_time, m.end_time, m.is_cancelled, m.project_id, p.title AS project_title, (SELECT GROUP_CONCAT(i.name || CASE WHEN i.is_retired THEN ' (Retired)' ELSE '' END, ', ') FROM meeting_instruments mi JOIN instruments i ON i.id=mi.instrument_id WHERE mi.meeting_id=m.id) AS instruments, (SELECT GROUP_CONCAT(pe.name || CASE WHEN pe.is_retired THEN ' (Retired)' ELSE '' END, ', ') FROM meeting_staff ms JOIN people pe ON pe.id=ms.person_id WHERE ms.meeting_id=m.id) AS staff FROM meetings m LEFT JOIN projects p ON p.id=m.project_id WHERE m.date=? AND (m.project_id IS NULL OR p.is_archived=0) ORDER BY m.start_time, m.id` (LEFT JOIN + the NULL guard so facility-wide bookings are never dropped); msToday = `SELECT m.id, m.name, m.due_date, m.status, p.id AS project_id, p.title AS project_title FROM milestones m JOIN projects p ON p.id=m.project_id WHERE p.is_archived=0 AND m.due_date=? ORDER BY m.status='done', m.id`. After the closing `</div>` of the existing `grid cols-2` block append: `<div class=\"card mt-16\"><div class=\"card-title\">${ic('calendar')} Today's Agenda — ${esc(agendaDate)}</div><div class=\"card-body\"><div class=\"grid cols-2\"> ... </div></div></div>` with two columns: 'Bookings Today (N)' - each row `<div class=\"row milestone-quick-row ${m.is_cancelled ? 'row-retired' : ''}\" data-act=\"edit-booking\" data-id=\"${m.id}\" style=\"cursor:pointer\">` containing `<span class=\"mono small\">${start}${end ? '–' + end : ''}</span>` ('All day' when no start), `<div class=\"grow\"><div class=\"font-medium\">${esc(m.title)}</div><div class=\"faint small\">${m.project_title ? esc(m.project_title) : 'Facility-wide'}${m.instruments ? ' · ' + esc(m.instruments) : ''}${m.staff ? ' · ' + esc(m.staff) : ''}</div></div>` and `<span class=\"badge danger\">Cancelled</span>` when cancelled; empty -> `emptyState('calendar', 'No bookings today', 'Bookings dated today will appear here.')`; 'Milestones Due Today (N)' - reuse the Upcoming row markup verbatim (`data-goto=\"project\"`, `data-act=\"toggle-ms-status\"` badge with `UI.msStatusLabel`, `fmt(m.due_date)`; done rows get badge success); empty -> `emptyState('target', 'Nothing due today', 'Milestones due today will appear here.')`. A card outside a grid is already full width - no new CSS; `mt-16` exists. Title Case for the card and column titles; sentence case for empty states. The openTodayModal modal in app.js is untouched. dashboard.test.js: add `describe(\"Dashboard: Today's Agenda card\", ...)` with tests: (1) a booking on UI.today() ('Agenda Booking Z', 10:00-11:00) appears inside the section after 'Bookings Today' with `data-act=\"edit-booking\"`, and a booking dated todayPlusDays(1) does not; (2) a facility-wide booking (project_id NULL) on today appears with 'Facility-wide'; (3) a milestone due today on liveProject appears under 'Milestones Due Today' while one due today on archivedProject does not; (4) the card title contains \"Today's Agenda\". CHANGELOG `### Added`: `- **Today's Agenda on the Dashboard**: a full-width card under the milestone boxes lists today's bookings (time, title, project or Facility-wide, instruments, staff; cancelled ones marked) and the milestones due today.`",
      "depends_on": [
        "T3"
      ],
      "parallel_safe": false,
      "verification": "From /home/user/Core-Facility-CRM: `node -e \"const {loadApp}=require('./test/unit/helpers/load-module');const {seedFixture}=require('./test/unit/helpers/sqlite');(async()=>{const a=loadApp(['consts','db','ui','views']);globalThis.initSqlJs=require('./libs/sql-asm.js');await a.DB.boot();seedFixture(a.DB);const {DB,UI,Views}=a;DB.run(\\\"INSERT INTO meetings (title,date,start_time,end_time) VALUES ('Agenda Booking Z',?,'10:00','11:00')\\\",[UI.today()]);DB.run(\\\"INSERT INTO meetings (title,date) VALUES ('Tomorrow Q',?)\\\",[UI.todayPlusDays(1)]);DB.run(\\\"INSERT INTO milestones (project_id,name,due_date,status) VALUES (1,'Due Today M',?,'pending')\\\",[UI.today()]);DB.run(\\\"INSERT INTO milestones (project_id,name,due_date,status) VALUES (2,'Archived Due A',?,'pending')\\\",[UI.today()]);const h=Views.dashboard();const ag=h.split(\\\"Today's Agenda\\\")[1]||'';console.log(ag.includes('Agenda Booking Z'),ag.includes('Facility-wide'),ag.includes('Tomorrow Q'),ag.split('Milestones Due Today')[1].includes('Due Today M'),ag.includes('Archived Due A'),(ag.match(/data-act=\\\"edit-booking\\\"/g)||[]).length)})()\"` prints `true true false true false 1`. `grep -c 'toISOString' js/views.js` is unchanged from `git show HEAD:js/views.js | grep -c toISOString`. `node --check js/views.js` exits 0. `TZ=Asia/Jerusalem node --test test/unit/dashboard.test.js 2>&1 | grep -E '^# (pass|fail)'` prints pass 6 / fail 0. `TZ=Asia/Jerusalem node --test 'test/unit/*.test.js'` 0 fail. `TZ=Asia/Jerusalem node --test 'test/browser/smoke.spec.js'` passes (dashboard renders, 0 JS errors)."
    },
    {
      "id": "T9",
      "title": "Richer demo dataset, part A: nine more lab members with real roles",
      "scope": "File: /home/user/Core-Facility-CRM/js/db.js seedSampleData only. (1) peopleData (~line 2817): change Alex Chen's type 'Researcher' -> 'Postdoc' and Maya Patel's 'Researcher' -> 'PhD' (their notes already say so). Append nine rows AFTER Tom Alvarez so ids are 9-17 in this order (name, type, organization, department, email, note, is_staff 0, rate 0): 9 'Dr. Lena Okafor' Postdoc, Bio-Photonics Lab, Harvard Immunology; 10 'Jonah Reyes' PhD, Bio-Photonics Lab, Harvard Immunology; 11 'Ruth Adler' Technician, Bio-Photonics Lab, Harvard Immunology; 12 'Dr. Samir Haddad' Postdoc, Neural Dynamics Institute, MIT; 13 'Yuki Tanaka' MSc, Neural Dynamics Institute, MIT; 14 'Grace Mbeki' Technician, Neural Dynamics Institute, MIT; 15 'Dr. Ines Ferreira' Postdoc, Therapeutics & Onco-Therapy, Stanford; 16 'Noa Levi' PhD, Therapeutics & Onco-Therapy, Stanford; 17 'Ben Carter' Undergrad, Therapeutics & Onco-Therapy, Stanford - fictional example.edu-style emails (e.g. lokafor@harvard.edu style matching the lab's domain pattern already used), one-sentence notes describing their work. Every type must be a PERSON_TYPES value (T1 added Facility Staff; these use Postdoc/PhD/Technician/MSc/Undergrad). (2) project_people (~line 2959): add rows project 1 <- 9 'Postdoc', 10 'Student', 11 'Technician'; project 2 <- 12 'Postdoc', 13 'Student', 14 'Technician'; project 3 <- 15 'Postdoc', 16 'Student', 17 'Student' (roles from CONST.ROLE). (3) grantUserPairs (~line 2874): add [1, 9], [1, 10], [2, 12], [3, 16] with comments naming the people correctly (the lesson: a seed comment that misnames its rows is a defect). (4) Inside the #47 seed fence's campus/mobile UPDATEs (~line 3348-3356) add: `UPDATE people SET campus='Longwood' WHERE id IN (9,10,11)`, `'Cambridge' WHERE id IN (12,13,14)`, `'Palo Alto' WHERE id IN (15,16,17)`, and mobiles for ids 9 and 12 in the existing '+1 617 555 0xxx' style. Fix the seed's people comment if it counts people. Do not touch bookings, training rows, tags, or booking #1. The people count also feeds smoke.spec ('people >= 5') - stays true.",
      "depends_on": [
        "T1"
      ],
      "parallel_safe": false,
      "verification": "From /home/user/Core-Facility-CRM: `node -e \"const {freshDb}=require('./test/unit/helpers/sqlite');(async()=>{const {DB,CONST}=await freshDb({search:'?demo=1'});await DB.seedSampleData({force:true});const q=(s)=>DB.row(s).c;console.log(q('SELECT COUNT(*) c FROM people'),DB.rows('SELECT type FROM people WHERE id IN (4,5) ORDER BY id').map(r=>r.type).join(','),q('SELECT COUNT(*) c FROM project_people'),q('SELECT COUNT(*) c FROM grant_users'),q(\\\"SELECT COUNT(*) c FROM people WHERE TRIM(COALESCE(campus,''))=''\\\"),DB.rows('SELECT DISTINCT type FROM people').every(r=>CONST.PERSON_TYPES.includes(r.type)),q('SELECT COUNT(*) c FROM people WHERE is_staff=1'),DB.row('SELECT total_cost FROM meetings ORDER BY id LIMIT 1').total_cost,q('SELECT COUNT(*) c FROM meetings'))})()\"` prints `17 Postdoc,PhD 16 9 0 true 3 589.95 14`. `node --check js/db.js` exits 0. `TZ=Asia/Jerusalem node --test 'test/unit/*.test.js'` 0 fail (training.test.js's '9 training rows' and booking-tags' seed assertions still hold - this task adds no bookings or training rows)."
    },
    {
      "id": "T10",
      "title": "Richer demo dataset, part B: 17 more bookings (two today) and 7 training rows, test counts updated",
      "scope": "Files: /home/user/Core-Facility-CRM/js/db.js seedSampleData, /home/user/Core-Facility-CRM/test/unit/training.test.js, CHANGELOG.md (one bullet). Add a new fenced block `// --- 1.13 begin ---` ... `// --- 1.13 end ---` inside seedSampleData immediately after the line `// --- #41 end ---` (~line 3394) and before `markDirty();`. Inside it, 17 `seedBooking({...})` calls (every one with `peopleIds`, never a hand-written attendees string; realistic titles/notes; categories only from consult / training / assisted session / sync - NO 'workshop', its ref count must stay exactly 1). The set (instrument ids: 1 Leica, 2 FV3000, 3 Zeiss, 4 Nikon, 5 Glacios; staff 6 Kim, 7 Anand, 8 Alvarez): B1 day(-42) 10:00-12:00 Leica, staff 6, people [9,10], project 1, assisted session; B2 day(-35) 13:00-15:00 Nikon, staff 6, people [12,13], project 2, assisted session; B3 day(-28) 09:00-11:00 Zeiss, staff 7, people [15,16], project 3, training; B4 day(-25) 14:00-16:00 FV3000, staff 6, people [9], project 1, assisted session; B5 day(-21) 09:00-09:45 no instrument, staff 8, people [13,14], project 2, consult; B6/B7/B8 the weekly trio: day(-21), day(-14), day(-7) each 11:00-13:00 Nikon, staff 6, people [12], project 2, assisted session, same title 'Weekly Calcium Imaging Block'; B9 day(-14) 09:00-12:00 Leica, no staff, people [10,11], project 1, assisted session; B10 day(-12) 10:00-12:00 Glacios (amount 4), staff 7, people [16], project 3, assisted session; B11 day(-5) 13:00-15:00 Zeiss, staff 8, people [15,17], project 3, assisted session; B12 day(-2) 10:00-10:30 no instrument, staff 6, people [11], project 1, consult; B13 day(0) 09:00-11:00 FV3000, staff 6, people [9,4], project 1, assisted session; B14 day(0) 13:00-15:00 Leica, staff 8, people [12,13], project 2, training; B15 day(3) 09:00-11:00 Nikon, staff 6, people [5,12], project 2, assisted session; B16 day(7) 10:00-12:00 Zeiss, staff 7, people [16], project 3, assisted session; B17 day(14) 11:00-11:30 no instrument, staff 8, people [10], project 1, consult. groupOrg = the lab of the project (Bio-Photonics Lab / Neural Dynamics Institute / Therapeutics & Onco-Therapy, as the existing calls do); grantId as the existing calls do per project. None of these overlaps an existing booking's instrument or staff on the same date (checked against the occupancy list in the assumptions) and none overlaps each other. Then tag at least 9 of them with `run('UPDATE meetings SET tags=? WHERE title=?', [...])` using ONLY tags already in the set (STED, Fiji, Napari, Lightsheet, Grant Planning, Workshop) so picker counts read > 1 - e.g. the trio 'STED, Napari', B13 'Fiji', B3/B16 'Lightsheet', B9 'Fiji', B14 'STED', B17 'Napari', B1 'Fiji'. Then 7 training INSERTs (same column list as trainingRows) with non-null trained_on and no expiry or a future one: [9,2,'Regular',day(-60),6,'',''], [10,1,'Regular',day(-40),6,day(325),''], [11,1,'Regular',day(-30),8,'',''], [12,4,'Super User',day(-150),6,'','Runs the weekly calcium block independently'], [13,4,'Regular',day(-33),6,'',''], [15,3,'Regular',day(-27),7,day(338),''], [16,5,'Regular',day(-11),7,'','']. Each comment in the block must name the right person/instrument for the rows it describes. training.test.js '#47 demo seed' test: change the expected count 9 -> 16 in both the assertion and the test title ('loads 16 training rows ...') - the one assertion this task knowingly changes; 'exactly one NULL trained_on' stays true. CHANGELOG `### Changed`: `- **A fuller demo dataset**: each lab has three more members with real roles, 17 more bookings across every optical instrument (two of them today, so the Dashboard agenda and instrument profiles have something to show), reused tags so the tag picker shows counts, and training records for the new members.`",
      "depends_on": [
        "T9"
      ],
      "parallel_safe": false,
      "verification": "From /home/user/Core-Facility-CRM: `grep -c 'seedBooking({' js/db.js` prints 31. `node -e \"const {freshDb}=require('./test/unit/helpers/sqlite');(async()=>{const {DB,UI}=await freshDb({search:'?demo=1'});await DB.seedSampleData({force:true});const q=(s,p)=>DB.row(s,p||[]).c;const overlap=q(\\\"SELECT COUNT(*) c FROM meetings a JOIN meetings b ON a.id<b.id AND a.date=b.date AND a.start_time<b.end_time AND b.start_time<a.end_time WHERE EXISTS (SELECT 1 FROM meeting_instruments x JOIN meeting_instruments y ON x.instrument_id=y.instrument_id WHERE x.meeting_id=a.id AND y.meeting_id=b.id) OR EXISTS (SELECT 1 FROM meeting_staff x JOIN meeting_staff y ON x.person_id=y.person_id WHERE x.meeting_id=a.id AND y.meeting_id=b.id)\\\");const k=DB.row(\\\"SELECT subtotal,total_before_tax,total_cost,id FROM meetings WHERE title='Project Kickoff & Laser Alignment Review'\\\");console.log(q('SELECT COUNT(*) c FROM meetings'),q('SELECT COUNT(*) c FROM meetings WHERE date=?',[UI.today()]),overlap,k.id===DB.row('SELECT MIN(id) m FROM meetings').m,k.subtotal,k.total_before_tax,k.total_cost,DB.countBookingCategoryRefs('workshop'),q('SELECT COUNT(*) c FROM person_instrument_training'),q('SELECT COUNT(*) c FROM person_instrument_training WHERE trained_on IS NULL'),q(\\\"SELECT COUNT(*) c FROM meetings WHERE attendees<>'' AND NOT EXISTS (SELECT 1 FROM meeting_people mp WHERE mp.meeting_id=meetings.id)\\\"),DB.bookingTagCounts().filter(t=>t.count>1).length>=4)})()\"` prints `31 2 0 true 490 546.25 589.95 1 16 1 0 true`. `awk '/#41 end ---/{f=1} f&&/1.13 begin/{print \"SEED-ORDER-OK\"; exit}' <(sed -n \"$(grep -n 'async function seedSampleData' js/db.js | cut -d: -f1),\\$p\" js/db.js)` prints SEED-ORDER-OK. `node --check js/db.js` exits 0. `TZ=Asia/Jerusalem node --test 'test/unit/*.test.js'` 0 fail (training.test.js '16 training rows' passes; booking-tags seed test passes). `TZ=Asia/Jerusalem node --test 'test/browser/*.spec.js'` no new failures (smoke: bookings >= 10, isolation unaffected)."
    },
    {
      "id": "T11",
      "title": "Extensions and importers concept document (docs only)",
      "scope": "Files: new /home/user/Core-Facility-CRM/docs/plans/extensions-and-importers-concept.md, /home/user/Core-Facility-CRM/ROADMAP.md (one link line). Write the concept doc (Markdown, ~150-250 lines) with these `##` sections in order: 'Why this document'; 'What an extension can and cannot do in a client-only PWA' (possible: file in / file out via SheetJS and Blob downloads, mailto: hand-off like Email Attendees, in-browser OAuth (PKCE) against a public client id; not possible: anything needing a server-held secret, push, scheduled jobs while the tab is closed, server-to-server sync - cite CLAUDE.md's 'no backend' rule); 'Proposed registry: window.Plugins.register' with the shape `Plugins.register({ id, name, kind: 'importer' | 'sync' | 'exporter', run })`, load order (a plugin is a plain <script> after app.js), how a Settings card 'Extensions' lists registered plugins with a Run button, and how run() receives {DB, UI, Views, App} and returns a summary the card shows; 'Importer notes per target' with subsections PPMS / Stratocore, BookitLab, Zoom exports, Google Calendar two-way sync (Calendar API with a stored sync token in UI.storage, incremental pulls, our bookings pushed as events with a private extended property carrying the meeting id) - every assumption about a vendor file format prefixed `[Unverified]`; 'Mapping onto existing entities' (people/labs -> people.organization + vocab ORG, instruments -> instruments + cost_unit, bookings -> meetings plus the three join tables meeting_people / meeting_instruments / meeting_staff written together with the attendees display string, grants -> grants/grant_users, training -> person_instrument_training with levels Regular / Super User); 'Duplicate and conflict rules' (match on external id kept in a new column or a plugin-owned mapping table, reuse findBookingConflicts before insert, cancelled bookings free the slot, never delete on re-import - retire/archive rules apply); 'Dates and money' (external timestamps are converted to LOCAL calendar days with UI.ymd, never toISOString().slice; totals imported verbatim into the frozen cost columns, or recomputed via the same BOM path seedBooking uses - state the trade-off); 'Open questions' (bullet list). Mark `[Unverified]` at least 4 times. ROADMAP.md: in the 'Milestone — Migration/import tool (important, not now)' section, after the '[Unverified] Vendor export formats have not been inspected yet.' paragraph add one line: `Concept notes for the plugin registry and the per-vendor importers: [docs/plans/extensions-and-importers-concept.md](docs/plans/extensions-and-importers-concept.md).` No version bump, no CHANGELOG entry (docs only).",
      "depends_on": [],
      "parallel_safe": true,
      "verification": "From /home/user/Core-Facility-CRM: `test -f docs/plans/extensions-and-importers-concept.md && echo EXISTS` prints EXISTS; `grep -c '^## ' docs/plans/extensions-and-importers-concept.md` prints at least 8; `grep -c '\\[Unverified\\]' docs/plans/extensions-and-importers-concept.md` prints at least 4; `grep -c 'Plugins.register' docs/plans/extensions-and-importers-concept.md` prints at least 2; `grep -c 'findBookingConflicts' docs/plans/extensions-and-importers-concept.md` prints at least 1; `grep -c 'extensions-and-importers-concept.md' ROADMAP.md` prints 1 and `awk '/Migration\\/import tool/{f=1} f&&/extensions-and-importers-concept/{print \"LINK-IN-SECTION\"; exit} f&&/^## Tier 5/{exit}' ROADMAP.md` prints LINK-IN-SECTION; `git diff --stat -- js css index.html sw.js CHANGELOG.md` shows no change caused by this task (compare with the diff before the task); `TZ=Asia/Jerusalem node --test 'test/unit/*.test.js'` unchanged."
    },
    {
      "id": "T12",
      "title": "Docs page and manual for 1.13.0 (docs only)",
      "scope": "Files: /home/user/Core-Facility-CRM/docs/index.html, /home/user/Core-Facility-CRM/docs/manual/instruments.html, /home/user/Core-Facility-CRM/docs/manual/dashboard.html, /home/user/Core-Facility-CRM/docs/manual/people-labs.html, /home/user/Core-Facility-CRM/docs/manual/bookings.html. docs/index.html: (1) rewrite the hero `<p class=\"lede\">` (line ~208) for 1.13.0 - tag picker with counts, instrument profile, Today's Agenda on the dashboard, one role pill, Regular / Super User rename, fuller demo; leave `#hero-release` and `#footer-version` static fallbacks alone (derived from CHANGELOG at load). (2) Prepend three Highlights cards (before the '🏷️ Tags on bookings ... 1.12.0' card) in the existing `<div class=\"card\"><div class=\"ic\">…</div><h3>… &mdash; 1.13.0</h3><p>…</p></div>` pattern: 'Pick a tag, see how often it was used', 'A profile for every instrument', \"Today's Agenda, on the Dashboard itself\". (3) In the 1.12.0 person-profile card (line ~225) change '(User or Super User)' to '(Regular or Super User)'. (4) Add three 'In detail' feature blocks at the top of that section copying an existing block's markup exactly, with `screenshots/73-instrument-profile.png`, `screenshots/74-booking-tag-picker.png`, `screenshots/75-dashboard-agenda.png` (73-75 are the next free numbers after the highest existing 72) and matching 'Save as <code>docs/screenshots/NN-…png</code>' placeholders and the onerror handler. Manual (prose for facility managers - no 'modal', say 'dialog'; no schema/join/denormalized/boolean/foreign key): instruments.html - new `<section>` with `<h2 id=\"instrument-profile\">The instrument profile</h2>` between 'Operational status' and 'Retiring and restoring an instrument' explaining Open Profile (chevron or click the row), the utilization tiles (this month / this year; cancelled bookings don't count as hours; charges follow the Project Costs rule), details, the next 14 days, recent activity columns (User, Date, Start–End, Duration, Assisted, Notes, Status), trained users, active projects and the New Booking button pre-set to that instrument; add a matching `<li>` to its 'What you'll be able to do' list. dashboard.html - in the 'Today's Agenda' section add a paragraph that the same agenda now sits as a full-width card under the milestone boxes (bookings today with time, project or Facility-wide, instruments and staff; milestones due today), and that the button still opens the fuller dialog. people-labs.html - a short `<section>` `<h2 id=\"instrument-training\">Instrument training records</h2>` (before 'Check yourself' or the last section) naming the two levels **Regular** and **Super User**, that older records saved as 'User' now read Regular, and that the profile shows one coloured role pill. bookings.html - in the section describing the booking dialog fields, one paragraph on the Tags picker (search, counts, Create). Do not change any `manual.js?v=` / `manual.css?v=` value or any `ch-kicker`. No version bump, no CHANGELOG entry.",
      "depends_on": [
        "T10"
      ],
      "parallel_safe": true,
      "verification": "From /home/user/Core-Facility-CRM: `grep -c '1.13.0' docs/index.html` prints at least 3; `grep -c 'screenshots/7[345]-' docs/index.html` prints 6 (img + placeholder for each of three blocks); `grep -c 'User or Super User' docs/index.html` prints 0 and `grep -c 'Regular or Super User' docs/index.html` prints 1; `ls docs/screenshots | sort -V | tail -1` still prints `72-research-output-detail.png` (no screenshot is fabricated); `grep -c 'id=\"instrument-profile\"' docs/manual/instruments.html` prints 1; `grep -c 'id=\"instrument-training\"' docs/manual/people-labs.html` prints 1; `grep -c 'Regular' docs/manual/people-labs.html` at least 1; `grep -ci 'modal' docs/manual/instruments.html docs/manual/dashboard.html docs/manual/people-labs.html docs/manual/bookings.html | awk -F: '{s+=$2} END{print s}'` prints 0; `grep -o 'manual.js?v=[0-9]*' docs/manual/*.html | sort -u | wc -l` prints 1; `git diff --stat -- js css index.html sw.js CHANGELOG.md` shows nothing from this task; `TZ=Asia/Jerusalem node --test test/unit/versions.test.js` passes (manual cache-buster consistency)."
    },
    {
      "id": "T13",
      "title": "Release plumbing: bump to 1.13.0, finalize CHANGELOG, full verification incl. restore migration",
      "scope": "Files: /home/user/Core-Facility-CRM/index.html, /home/user/Core-Facility-CRM/sw.js, /home/user/Core-Facility-CRM/js/consts.js, /home/user/Core-Facility-CRM/CHANGELOG.md. (1) index.html: all 8 `?v=1.12.1` -> `?v=1.13.0`. (2) sw.js: `const CACHE_VERSION = '1.13.0'` and all 8 PRECACHE_URLS `?v=1.12.1` -> `?v=1.13.0`. (3) js/consts.js: `window.APP_VERSION = '1.13.0'`. (4) CHANGELOG.md: replace the line `## [Unreleased]` with `## [1.13.0] — 2026-09-17` (em dash, exactly the format of the 1.12.1 heading); reread the Added/Changed bullets written by T1, T3, T4, T7, T8, T10 and tidy wording only (no new claims - every claim must be true of js/ as it now is, e.g. 'both booking dialogs', 'cancelled bookings included in tag counts', 'this month / this year tiles'); keep `### Added` before `### Changed`. (5) Full verification pass listed below, including the export-then-restore check of the User -> Regular migration and rendering every screen plus the new profile in headless Chromium. Fix nothing outside these four files; if a check fails because of an earlier task, report blocked with the failing output rather than patching js/ here.",
      "depends_on": [
        "T4",
        "T6",
        "T7",
        "T8",
        "T10",
        "T11",
        "T12"
      ],
      "parallel_safe": false,
      "verification": "From /home/user/Core-Facility-CRM: `grep -c '?v=1.13.0' index.html` prints 8 and `grep -c '?v=1.12.1' index.html sw.js | awk -F: '{s+=$2} END{print s}'` prints 0; `grep -c \"CACHE_VERSION = '1.13.0'\" sw.js` prints 1; `grep -c \"?v=1.13.0\" sw.js` prints 8; `grep -c \"APP_VERSION = '1.13.0'\" js/consts.js` prints 1; `grep -c '^## \\[Unreleased\\]' CHANGELOG.md` prints 0 and `grep -n '^## \\[' CHANGELOG.md | head -1` prints `## [1.13.0] — 2026-09-17` on its line; `for f in js/*.js; do node --check \"$f\"; done` exits 0; `TZ=Asia/Jerusalem node --test 'test/unit/*.test.js' 2>&1 | grep -E '^# (pass|fail)'` shows `# fail 0` and pass >= 340 (318 baseline + the tests added by T1, T2, T6, T8); `TZ=Asia/Jerusalem node --test 'test/browser/*.spec.js' 2>&1 | grep -E '^# (pass|fail)'` shows `# fail 0` and pass >= 72 (65 baseline + smoke +2 + tag-picker spec); restore-migration one-liner: `node -e \"const {freshDb}=require('./test/unit/helpers/sqlite');if(typeof FileReader==='undefined'){globalThis.FileReader=class{readAsDataURL(b){b.arrayBuffer().then(a=>{this.result='data:application/octet-stream;base64,'+Buffer.from(a).toString('base64');this.onload&&this.onload()})}}}(async()=>{const {DB}=await freshDb({search:'?demo=1'});await DB.seedSampleData({force:true});DB.run(\\\"UPDATE person_instrument_training SET level='User' WHERE level='Regular'\\\");const before=DB.row(\\\"SELECT COUNT(*) c FROM person_instrument_training WHERE level='User'\\\").c;const bk=await DB.buildBackup();await DB.restoreBackup(bk);console.log(before>0, DB.row(\\\"SELECT COUNT(*) c FROM person_instrument_training WHERE level='User'\\\").c, DB.row(\\\"SELECT COUNT(*) c FROM person_instrument_training WHERE level='Regular'\\\").c)})()\"` prints `true 0 14` (16 rows minus the two Super User rows... verifier: if the FileReader stub shape differs, copy the one from test/unit/backup-roundtrip.test.js lines 17-25 verbatim; the expected output remains `true 0 14`); headless render: a Playwright script (or `TZ=Asia/Jerusalem node --test test/browser/smoke.spec.js test/browser/tag-picker.spec.js`) loads `?demo=1`, visits dashboard (agenda card present: innerText matches /Today's Agenda/ and /Bookings Today \\(2\\)/), `#/instrument/2` (innerText matches /Instrument Profile|Recent Activity/ and the New Booking button has data-inst=\"2\"), `#/person/6` (exactly one `.badge` inside the header `.project-title`'s parent row and it reads 'Facility Staff'), opens New Booking and reads /×\\d+/ in the tag dropdown, and reports zero pageerror/console errors; `git status --short` lists only files touched by T1-T13 and no untracked scratch files inside the repo."
    }
  ],
  "batches": [
    [
      "T1",
      "T11"
    ],
    [
      "T2",
      "T3"
    ],
    [
      "T4"
    ],
    [
      "T5"
    ],
    [
      "T6",
      "T7"
    ],
    [
      "T8"
    ],
    [
      "T9"
    ],
    [
      "T10"
    ],
    [
      "T12"
    ],
    [
      "T13"
    ]
  ],
  "risks": [
    {
      "risk": "js/app.js, js/views.js and js/db.js are each touched by several tasks; an executor starting from a stale mental model (or a verifier extracting HEAD copies) could undo an earlier task's uncommitted work.",
      "mitigation": "Batches are strictly sequential for shared files; every task's scope names the exact anchors it edits and tells the executor to expect and build on earlier uncommitted work; no task uses a git command that mutates the tree; HEAD comparisons only via `git show HEAD:<path> > scratchpad`."
    },
    {
      "risk": "wiring.test.js scrapes every literal data-act and every handleAct case; the tag picker's option/create/remove buttons or the profile's rows could introduce an unwired data-act (or a CRLF line ending could make the scraper throw before asserting, hiding the gap).",
      "mitigation": "T4 wires picker buttons with addEventListener and explicitly forbids data-act on them; T5/T7/T8 emit only existing values (new-booking, edit-booking, edit-instrument, toggle-ms-status) plus data-goto; every task runs the unit suite and T7 adds the TITLES/HASH_ROUTES asymmetry assertions; assumptions require LF and a passing (not throwing) scraper."
    },
    {
      "risk": "The tag dropdown uses the `hidden` attribute on a flex container - exactly the #41 T5 failure where `.field { display: flex }` defeated `[hidden]` and no unit test could see it.",
      "mitigation": "T4 requires the `.tag-dropdown[hidden] { display: none; }` rule, verifies it by grep, and adds a browser spec that asserts the dropdown becomes visible on focus and that the hidden input's value round-trips through create/select/remove in headless Chromium."
    },
    {
      "risk": "Instrument profile utilization figures could drift from the Reports screen (re-derived hours/money), or the Distinct Users tile could be mistaken for a Reports figure.",
      "mitigation": "T5 reads bookings/hours/revenue only from Reports.makeFacts + Reports.computeInstrumentRows(from, to, facts) and exposes raw values via data-value; T6 asserts tile values equal computeInstrumentRows for the same range including a cancelled-retained booking; the distinct-users query is documented as a direct query with the same cancelled-excluded rule."
    },
    {
      "risk": "Demo seed additions could break hard invariants: booking #1 losing lowest id or its 490 / 546.25 / 589.95 figures, a second 'workshop' booking, same-day instrument/staff self-overlap (the seed bypasses the conflict gate), or attendees strings without meeting_people rows.",
      "mitigation": "T10 appends bookings in a fenced block after every existing call, forbids the workshop category and hand-written attendees, lists concrete non-overlapping slots against the recorded occupancy, and its verification runs a self-join overlap query (expected 0), checks MIN(id) and the three figures, countBookingCategoryRefs('workshop') === 1 and a zero count of attendee strings lacking join rows."
    },
    {
      "risk": "Date handling east of Greenwich: month/year range boundaries or 'today' comparisons built with toISOString would show yesterday's data.",
      "mitigation": "Scopes mandate UI.ymd(new Date(y, m, 1)) / UI.today() / UI.todayPlusDays; wiring.test.js's date lint runs in every task; all suites run under TZ=Asia/Jerusalem and the browser context uses timezoneId Asia/Jerusalem."
    },
    {
      "risk": "The training-level migration might run on boot but not on restore, or the seed/tests might still write 'User' somewhere, leaving mixed vocabularies.",
      "mitigation": "T1 places the UPDATE inside migrate() (which restoreBackup also runs), tests it on a raw sql.js DB via the fence-extracted SQL and through a real buildBackup/restoreBackup round trip, and greps that `level='User'` appears exactly once in db.js (the migration itself); T13 repeats the export-then-restore check on the seeded demo."
    },
    {
      "risk": "Documentation claims drifting from code (the repo's recorded 23-false-claims history), and stale labels such as 'User or Super User' surviving in docs.",
      "mitigation": "T12 and T13 must verify each doc sentence against js/ as it now is, not against CHANGELOG; T12's verification greps the old level wording to 0; the release CHANGELOG tidy-up is wording-only with per-dialog/per-screen phrasing rather than 'the export' generalities."
    },
    {
      "risk": "Forgetting the three-place version bump makes every js/css change invisible behind the service worker, or bumping early breaks versions.test.js mid-run.",
      "mitigation": "All feature tasks are forbidden from touching the version strings; T13 alone bumps index.html (8 tags), sw.js (CACHE_VERSION + 8 PRECACHE_URLS) and js/consts.js together and converts the Unreleased heading, with grep counts and versions.test.js as the gate."
    }
  ]
}
