export const meta = {
  name: 'cma-learn',
  description: 'CMA learning phase: after an execute run, an Opus learner distills which practices succeeded/failed and why into docs/cma-lessons.md, which planner/executor/verifier agents read before working.',
  phases: [
    { title: 'Learn', detail: 'Opus distills run evidence into docs/cma-lessons.md' },
  ],
}

// ---- Inputs (via args) -------------------------------------------------------
// args.runReport : object|string (required) — the execute run's results: per-task
//                  {id, title, exec, verdict, passed}, stoppedAt, allPassed, plus
//                  any orchestrator notes (workflow failures, schema-retry deaths,
//                  hotfixes, human interventions). Pass whatever evidence exists.
// args.repoPath  : string (required) — absolute path of the repo whose
//                  docs/cma-lessons.md should be updated.
let input = {"runReport":{"run":"1.13.0 release, 2026-09-17, plan docs/plans/1.13.0-tags-profiles-agenda-task-graph.json","allPassed":true,"tasks":[{"id":"T1","attempts":1,"verdicts":[{"pass":true,"problems":[],"recommendation":"accept — note that the app-shell version bump (index.html ?v=, sw.js CACHE_VERSION/PRECACHE_URLS, APP_VERSION) is deliberately deferred by this task's scope and must land in a later task, or the js/ change will be invisible in the browser.","evidence":"Inspected /home/user/Core-Facility-CRM/docs/cma-lessons.md first, then the real diff (`git diff HEAD`) rather than the executor's summary. Modified files are exactly the five in scope (CHANGELOG.md, js/app.js, js/consts.js, js/db.js, test/unit/training.test.js); index.html, sw.js and docs/ are untouched and `window.APP_VERSION = '1.12.1'` in js/consts.js is unchanged as the task required.\n\nStatic criteria, all run by me from the repo root: `grep -c \"level='User'\" js/db.js` = 1 (the migration line only); `grep -c \"DEFAULT 'Regular'\" js/db.js` = 2 (schema ~line 250 and the #47 migrate CREATE ~line 644); `grep -c \"'User'\" js/app.js` = 0; `grep -c \"1.13 begin\" js/db.js` = 1 and `1.13 end` = 1; the awk order check prints ORDER-OK (fence sits right after `// --- #41 end ---`, before migrate()'s closing brace); `grep -c '^## \\[Unreleased\\]' CHANGELOG.md` = 1 with `grep -n '^## \\['` showing Unreleased at line 6 and 1.12.1 at line 14, blank line between (confirmed with cat -A). All seven seed trainingRows literals and the ~line 2258 explanatory comment are 'Regular'.\n\nRuntime: `node -e ... loadApp(['consts'])` prints `[\"Regular\",\"Super User\"] 6 7 8` exactly. The freshDb+seedFixture insert-without-level prints `Regular`. `for f in js/*.js; do node --check \"$f\"; done` exits 0. `TZ=Asia/Jerusalem node --test 'test/unit/*.test.js'` → 321 pass / 0 fail (318 baseline + 3), including the new `1.13 training level rename` suite whose first test name contains \"idempotently\".\n\nAdversarial non-vacuity check: I copied the repo to the scratchpad, deleted only the fenced `db.exec(\"UPDATE ... level='Regular' WHERE level='User'\")` line, and re-ran training.test.js — 2 tests fail (the raw-sql.js fence-extraction/idempotency test and the buildBackup/restoreBackup rename test). So the new tests genuinely exercise the migration rather than passing regardless. The fence test also asserts the extracted statement verbatim via regex, so a drifted UPDATE would be caught.\n\nGrepped all of js/ for any other `'User'` level comparison or hardcoded level string (views.js, reports.js, exports.js): none — remaining \"User\" hits are unrelated identifiers (grantUserItems, trainedUserCountsAsOf, \"New User Training\" meeting title, \"User manual\" link). CHANGELOG's claims check out against code: PERSON_TYPES feeds both the person dialog (app.js vocabField at 1586/2147/2213) and the People filter select (views.js:683), and migrate() runs on boot and via restoreBackup, as the restore test proves."}],"executorOutcome":"done","executorClaim":"All stated verification criteria were run and matched exactly: level='User' count=1 (migration only), DEFAULT 'Regular' count=2, 'User' in app.js=0, 1.13 begin/end fences each=1, order check ORDER-OK, consts.js CONST values match exactly, training insert defaults to 'Regular', CHANGELOG has exactly one Unreleased section directly above 1.12.1, all js files pass node --check, and the full unit suite reports 321 pass / 0 fail (up from 318 baseline + 3 new), including the new '1.13 training level rename' suite with an 'idempotent'-named test and a restore-based rename test.</verification>\n<next_step>None — task complete. Ready for the next task in the plan.</next_step>\n","passed":true,"retries":0},{"id":"T2","attempts":1,"verdicts":[{"pass":true,"problems":["Nit only, not a failure: the 'ordering is count desc then name asc' test does not actually exercise a name tiebreak — Zeta(2), Alpha(1), Beta(0) are each alone in their count tier, so the deepEqual would pass even with a broken localeCompare comparator. The test's own assertion message admits this. I verified the tiebreak separately with a runtime trace (Mid before zulu at count 1), so the implementation is correct; the test just guards less than its title claims."],"recommendation":"accept — all four criteria independently reproduced (exact one-liner output, grep 2, node --check 0, 326/326 pass vs a measured 321 baseline); optionally strengthen the ordering test to put two same-count tags in one tier.","evidence":"Inspected the real diff (only js/db.js and test/unit/booking-tags.test.js are modified; nothing else touched).\n\njs/db.js:2038-2060 — `bookingTagCounts()` sits immediately after `addVocab`, with a comment stating it feeds the booking dialogs' tag picker and deliberately counts cancelled bookings as usage history (not occupancy). It runs exactly the specified SQL `SELECT tags FROM meetings WHERE TRIM(COALESCE(tags,''))<>'' ORDER BY id`, splits with `global.UI.parseTags`, keys a Map on `toLowerCase()` (first spelling wins), then unions `vocabList('BOOKING_TAG')` at count 0 for unseen keys, and sorts `(b.count - a.count) || a.tag.localeCompare(b.tag)`. Exported at js/db.js:3485 directly after `addVocab,`. `grep -c 'bookingTagCounts' js/db.js` = 2. `global.UI.*` at call time is established precedent in db.js (lines 1986, 2186, 2788), so the consts→db→ui load order is not a problem.\n\nRan the exact verification one-liner myself: printed `[{\"tag\":\"SIM\",\"count\":2},{\"tag\":\"Fiji\",\"count\":1},{\"tag\":\"Napari\",\"count\":0}]` — byte-identical to the criterion. `node --check js/db.js` exited 0.\n\nTest counts verified independently, not taken from the report: `git archive HEAD | tar -x` into a clean scratch copy and ran `TZ=Asia/Jerusalem node --test 'test/unit/*.test.js'` there → 321 tests / 321 pass / 0 fail. Same command in the working tree → 326 tests / 326 pass / 0 fail. Exactly +5 new tests, 0 fail, meets the \">= 326 and >= 5 more than T1\" bar with no pre-existing red to excuse.\n\nExtra adversarial runtime trace beyond the criterion (scratch harness): tags `'zulu'`, `'Alpha'`, `'  Mid ,, Alpha '`, a whitespace-only `'   '` row, plus vocab `'ZULU'` and `'New'` → `[Alpha:2, Mid:1, zulu:1, New:0]`. This confirms (a) a real localeCompare tiebreak within a count tier (Mid before zulu), (b) vocab term `ZULU` merging into the earlier meeting spelling `zulu` rather than adding a second row, (c) whitespace-only / empty tag segments being dropped by both the SQL TRIM filter and parseTags. `test/README.md` genuinely has no booking-tags entry (grep returns nothing), so leaving it untouched was correct per the task."}],"executorOutcome":"done","executorClaim":"One-liner printed exactly [{\\\"tag\\\":\\\"SIM\\\",\\\"count\\\":2},{\\\"tag\\\":\\\"Fiji\\\",\\\"count\\\":1},{\\\"tag\\\":\\\"Napari\\\",\\\"count\\\":0}]. grep -c 'bookingTagCounts' js/db.js returned 2 (definition + export). node --check js/db.js exited 0. Full unit suite: 326 tests, 326 pass, 0 fail (>= 326 required, and >= 5 more than pre-T2 baseline of 321 since exactly 5 new tests were added). test/README.md has no existing booking-tags entry, so left untouched per task instructions.","passed":true,"retries":0},{"id":"T3","attempts":1,"verdicts":[{"pass":true,"problems":[],"recommendation":"accept — the only cosmetic nit is that `map[type]` inherits statusBadge's existing quirk for prototype-named keys (e.g. a vocab value literally called \"constructor\" or \"toString\"), which is pre-existing house style and not in scope.","evidence":"Inspected /home/user/Core-Facility-CRM/js/views.js and CHANGELOG.md via git diff and direct sed reads, then ran every stated criterion myself.\n\n1) typeBadge is defined at js/views.js lines 226-229, directly after statusBadge (which ends line 224): `const map = { 'PI': 'primary', 'Facility Staff': 'success' }; return `<span class=\"badge ${map[type] || 'neutral'}\">${esc(type || '—')}</span>`;` — no extra attributes, no suffix. Exported in `global.Views = {...}` at line 1892, immediately after `statusBadge`.\n2) Task's node -e one-liner printed exactly `<span class=\"badge primary\">PI</span>|<span class=\"badge success\">Facility Staff</span>|<span class=\"badge neutral\">PhD</span>` then `1 true false` — so the profile header carries exactly one badge, it is the success/Facility Staff pill for the seeded staff person, and the old `data-tooltip=\"Billable by the hour on bookings\"` pill is gone from the whole page.\n3) `grep -c typeBadge js/views.js` = 4 (definition, export, list, profile). `node --check js/views.js` exits 0.\n4) `TZ=Asia/Jerusalem node --test 'test/unit/*.test.js'`: 326 tests, 326 pass, 0 fail, 0 skipped. `TZ=Asia/Jerusalem node --test 'test/browser/smoke.spec.js'`: 16/16 pass, including \"no JavaScript errors were logged while visiting every screen\".\n5) Profile header (line 785-790) is title + `${typeBadge(p.type)}` only; the hardcoded is_staff pill line is deleted. The `Rate/hr` meta item (line 809, still `p.is_staff`-gated) and the rest of the profile are intact. People list Role column (line 728) is `<td>${typeBadge(r.type)}</td>`; the Facility Staff check column (line 734) and the Rate/hr column (735) are byte-identical to before.\n6) CSS backing exists: css/app.css lines 276-278 define `.badge.neutral`, `.badge.primary`, `.badge.success` (plus dark-theme override for success) — the colours actually render, not just the class names.\n7) Edge cases exercised directly: '', null, undefined all render `badge neutral` with the em-dash; 'Other'/'Postdoc' → neutral; HTML input is escaped (`&lt;b&gt;x&lt;/b&gt;`), so esc() is applied as specified.\n8) Scope: `git diff --stat` touches only CHANGELOG.md (+1), js/views.js (+11/-3), plus js/db.js and test/unit/booking-tags.test.js, which are a different task's bookingTagCounts work already in the tree and unrelated to T3. app.js, exports.js and the other 'Facility Staff' occurrences are untouched.\n9) CHANGELOG.md bullet added under `## [Unreleased]` / `### Changed`, verbatim as specified. Per CLAUDE.md, no versi"}],"executorOutcome":"done","executorClaim":"All verification criteria satisfied exactly: node -e one-liner printed `<span class=\\\"badge primary\\\">PI</span>|<span class=\\\"badge success\\\">Facility Staff</span>|<span class=\\\"badge neutral\\\">PhD</span>` then `1 true false`. grep -c typeBadge js/views.js = 4. node --check js/views.js exits 0. Unit suite: 326/326 pass, 0 fail (no new failures). Browser smoke.spec.js: 16/16 pass including \\\"no JavaScript errors were logged while visiting every screen\\\". Grep-checked all other 'Facility Staff' occurrences in js/ before editing and confirmed they are data/labels/tooltips/comments per task's list, left untouched (People table's Facility Staff check column at line ~734 is unchanged).","passed":true,"retries":0},{"id":"T4","attempts":1,"verdicts":[{"pass":true,"problems":["Minor (not a criterion): the shipped spec's \"Esc twice\" step runs with an already-empty search box, so the Esc-clears-then-bubbles branch is not actually covered by the test; I verified it manually instead.","Out of scope but worth flagging for the run: no version bump (index.html ?v=, sw.js CACHE_VERSION, js/consts.js APP_VERSION all still 1.12.1) despite js/ and css/ changes; the CHANGELOG bullet sits under [Unreleased], matching the preceding commits in this run, so this evidently belongs to a separate bump task. Until that task lands, the picker will not appear in an already-installed browser (cma-lessons: \"a js/ or css/ edit that skips the version bump is invisible\")."],"recommendation":"accept — implementation, CSS hide rule, tests and docs all verified at runtime; ensure the run still has a pending version-bump task for 1.12.1 → next.","evidence":"Inspected the real diff (js/app.js, css/app.css, test/README.md, CHANGELOG.md, new test/browser/tag-picker.spec.js) and ran every criterion myself from /home/user/Core-Facility-CRM.\n\nStatic criteria: `grep -c 'function mountTagPicker' js/app.js` = 1; `grep -c 'mountTagPicker(m' js/app.js` = 2; `grep -c bookingTagSuggestions js/app.js` = 0 (function and its comment removed; tagsField was its only caller); `grep -c 'type=\"hidden\" id=\"${id}\"' js/app.js` = 1; `grep -c '\\.tag-dropdown\\[hidden\\]' css/app.css` = 1; `for f in js/*.js; do node --check \"$f\"; done` exits 0.\n\nThe literal sed-range data-act criterion: prints 8, NOT 0. I independently confirmed the executor's explanation against clean HEAD (`git show HEAD:js/app.js`): the same mountTokenPicker→mountBookingModal range printed 8 before this task, and the 8 hits are pre-existing unrelated code (confirmRecurringEditScope's cancel/once/future buttons, bomSectionHtml's bom-group-revoke/bom-group-apply). mountTagPicker's own body (function line through `function readTokenIds`) contains zero `data-act` — the actual intent. The criterion was unsatisfiable as written; the intent is met, and wiring.test.js's dispatch-completeness check passes.\n\nTest suites (TZ=Asia/Jerusalem): unit 326/326 pass, 0 fail; `test/browser/tag-picker.spec.js` 4/4 pass including the zero-page-errors test and the exact hidden-value sequence Fiji / \"Fiji, Brandnew\" / Brandnew; full browser suite 69/69 pass, 0 fail (baseline 65 + 4 new, no regressions).\n\nIndependent adversarial runtime harness (headless Chromium, served over http, demo dataset, Asia/Jerusalem) beyond the executor's spec, all passing: (a) the hidden dropdown's computed `display` is `none`, so the #41 T5 cascade trap is genuinely closed — not just the attribute set; (b) Esc with a non-empty search box clears the box and leaves the modal open (a path the shipped spec never exercises, since its box is already empty at that point); (c) Enter inside the tag box does not submit the booking (stopPropagation works — both global keydown listeners in ui.js/app.js are bubble-phase on document); (d) clicking a `.tag-option` from the dropdown adds it (hidden value became \"Fiji, Napari\" after clicking the Napari option, proving mousedown-preventDefault beats blur); (e) the value survives a real save — after booking-save, `SELECT tags FROM meetings WHERE title='ADV TAG TEST'` returned `Fiji, Napari`, so the stored format is unchanged and bookingSave (~3900, reads `#bk-tags`.value through U"}],"executorOutcome":"done","executorClaim":"All listed automatable checks passed as specified except one: the literal command `grep -c data-act <(sed -n \\\"$(mountTagPicker line),$(mountBookingModal line)p\\\" js/app.js)` prints 8, not 0. This is NOT from code introduced by this task: mountTagPicker's own body (from its `function` line to the following `function readTokenIds`) contains zero `data-act` occurrences (verified separately), matching the actual intent stated in the task prose ('NEVER use data-act on these buttons ... wire with addEventListener'). The 8 matches come from pre-existing, unrelated functions that already sat between mountTokenPicker and mountBookingModal before this task (confirmRecurringEditScope's dialog buttons at original lines ~253-260, and bomSectionHtml/renderBomRows' bom-group-revoke/bom-group-apply buttons at original lines ~406-407) — these predate this change and are unrelated to the tag picker. The literal sed-range criterion, as written, was checkable against HEAD (git show HEAD:js/app.js) and would have failed on the pre-task file too for this same reason, since mountBookingModal already sat far below several unrelated data-act-bearing helpers. All other verification criteria pass exactly as specified, including the full unit suite (326/326) and the full browser suite (69/69, no regressions, 4 new tests all passing with the exact hidden-value sequence Fiji / Fiji, Brandnew / Brandnew asserted).","passed":true,"retries":0},{"id":"T5","attempts":1,"verdicts":[{"pass":true,"problems":["Nit (spec-inherent, not a defect): notePreview calls UI.noteHtml, which needs a real DOMParser, so Views.instrumentDetail throws under Node for any instrument whose bookings carry a non-empty note. The stated criteria harness only inserts a note-less booking, so it never hits this. Verified fine in the browser; but any future unit test of this renderer must use note-less fixtures or a DOMParser stub.","Nit: no version bump (index.html ?v=, sw.js CACHE_VERSION/PRECACHE_URLS, js/consts.js APP_VERSION) or CHANGELOG entry accompanies this js/ change. The task scope is explicitly views.js only, so this is presumably a separate pipeline task — but per docs/cma-lessons.md it must actually land, or the change is invisible in a served browser.","Nit: no test was added for the new renderer (CLAUDE.md asks for a test with any new rule); the durationLabel/notePreview helpers are currently guarded only by the ad-hoc criteria one-liner."],"recommendation":"accept — implementation matches the spec and was confirmed rendering live in headless Chromium with 0 JS errors; ensure the separate version-bump task lands.","evidence":"Read docs/cma-lessons.md first, then inspected the actual diff (`git diff js/views.js`: +251/-1, the only modified file) rather than trusting the report.\n\nStated criteria, all run by me from /home/user/Core-Facility-CRM:\n- The literal node -e harness printed exactly `1 1.5 0 300 true 1 1.5 1 true`.\n- `grep -c 'data-goto=\"instrument\"' js/views.js` -> 2 (the `<tr class=\"row-link ...\">` and the new chevron button).\n- `grep -c 'Views\\.instrumentDetail\\|instrumentDetail,' js/views.js` -> 1 (export added next to `instruments`).\n- `grep -o \"ic('[a-z-]*')\" js/views.js | sort -u` -> every name (calendar, chevron, cpu, folder, target, edit, ...) is present in the ICONS map at js/ui.js:534.\n- `node --check js/views.js` exits 0.\n- `TZ=Asia/Jerusalem node --test 'test/unit/*.test.js'` -> 326 pass / 0 fail (date lint + icon check included).\n- `TZ=Asia/Jerusalem node --test 'test/browser/smoke.spec.js'` -> 16 pass / 0 fail.\n\nBeyond the criteria (lesson: static + unit checks pass on silently dead features, so I loaded the app in headless Chromium over http at TZ=Asia/Jerusalem, demo sandbox):\n- Instruments list DOM really contains 5 `tr.row-link[data-goto=\"instrument\"]` rows and 5 `button[data-goto=\"instrument\"][title=\"Open Profile\"]` chevrons; programmatically clicking a chevron logs no error and does not navigate (hashFor fallback, expected until T7). Dispatcher at js/app.js:1192-1201 skips `data-goto` when a `[data-act]` ancestor exists, so the per-row Edit/Retire buttons still work — same contract as the People list.\n- `Views.instrumentDetail(id)` rendered without throwing for all 5 demo instruments, and the injected HTML renders live: the `data-tile=\"month-bookings\"` stat is `display:flex` inside a real 4-column grid (`222.5px x4`), 11 `.meta-item` rows, 3 `table.tbl`, zero pageerror/console errors. (Cascade trap from the lessons checked — the tiles are not invisible.)\n- Utilization: exactly 8 tiles (month-/year- x bookings/hours/users/charges), two `grid cols-4 mb-16` rows, ranges built via `UI.ymd(new Date(...))` and `today()` with no `toISOString` anywhere in the addition.\n- Retired path: setting `is_retired=1` hides the `data-act=\"new-booking\"` button and renders the `badge neutral\">Retired` pill.\n- durationLabel truth table in-browser: 09:00–10:30 -> \"1h 30m\", 09:00–11:00 -> \"2h 0m\", equal times -> \"—\", missing times -> \"—\".\n- notePreview against a note containing `<b>`, `&amp;` and 200 chars: markup stripped, entity decoded (\"Hello & bye ...\"), clipped text is "}],"executorOutcome":"done","executorClaim":"All stated verification criteria reproduced exactly: the literal node -e one-liner printed `1 1.5 0 300 true 1 1.5 1 true`; grep counts for data-goto=\"instrument\" (2) and instrumentDetail export (>=1) match; icon grep shows only names from the ICONS map; node --check exits 0; unit suite (326 tests) and browser smoke suite (16 tests) both pass with 0 failures under TZ=Asia/Jerusalem.","passed":true,"retries":0},{"id":"T6","attempts":1,"verdicts":[{"pass":true,"problems":[],"recommendation":"accept — one optional hardening nit for a later pass: the `month-users` assertion does not detect the tile's `m.is_cancelled=0` clause being removed (my mutation of it kept all 8 green), because the cancelled fixture booking carries no `meeting_people` row; adding an attendee to the cancelled booking would pin that rule too. This is exactly what the brief asked for, so it is not a criteria failure.","evidence":"Read /home/user/Core-Facility-CRM/docs/cma-lessons.md first and applied its \"static checks are not verification\" and \"#47 T5 fixture-inside-range\" lessons.\n\nFiles inspected: /home/user/Core-Facility-CRM/test/unit/instrument-profile.test.js (new, 13.4 KB, 8 tests in one `describe('Instrument Profile')`), /home/user/Core-Facility-CRM/test/README.md (single added row), /home/user/Core-Facility-CRM/js/views.js (`instrumentDetail`, unchanged — `git status` shows only the new test file and the README edit, so the test was written against already-committed code at 8c2f208).\n\nCommands run myself from /home/user/Core-Facility-CRM:\n- `TZ=Asia/Jerusalem node --test test/unit/instrument-profile.test.js` -> `# tests 8`, `# pass 8`, `# fail 0`.\n- `grep -c 'instrument-profile.test.js' test/README.md` -> `1`; the row is at line 24, immediately after the `training.test.js` row, with exactly the wording the brief specified.\n- `TZ=Asia/Jerusalem node --test 'test/unit/*.test.js'` -> 334 tests, 334 pass, 0 fail. I re-ran the same glob with the new file temporarily moved aside: 326 pass, 0 fail. So the delta is exactly +8 and there are no pre-existing failures being masked.\n\nSpec conformance checked line by line: `freshApp()` = `loadApp(['consts','db','ui','views','reports'])` + `globalThis.initSqlJs = require(libs/sql-asm.js)` + `DB.boot()`; `seedFixture(DB)` used in every test; header comment in dashboard.test.js style; every fixture date built from `UI.today()`/`UI.todayPlusDays()`; the month start uses `UI.ymd(new Date(y, m, 1))`; the second (cancelled) utilization booking is deliberately pinned to `monthFrom` with a comment so it stays inside the asserted range (the #47 T5 lesson). Test 1 asserts exactly one Recent Activity row and >=5 em-dashes (I measured the real row: 6) plus no 'undefined'/'null'. Test 6 reads `data-tile=\"month-bookings|hours|charges|users\" data-value` and compares to `Reports.computeInstrumentRows(monthFrom, UI.today()).rows.find(r=>r.id===1)`, asserting bookings 1, hours 2, revenue 300 and users 1 — i.e. it pins both the tile/report agreement and the retained-cancellation billing rule.\n\nAdversarial non-vacuity check (mutation testing): I copied js/libs/test into the scratchpad (the loader resolves REPO relative to the test file, so the copy runs identically — baseline 8/0 there) and applied 13 single-point mutations to `js/views.js`, each scoped to `instrumentDetail`. Every one that mapped uniquely turned a test red (pass=7 fail=1): duration format "}],"executorOutcome":"done","executorClaim":"All three verification commands from the task were run from /home/user/Core-Facility-CRM and produced the required output: instrument-profile.test.js alone prints '# pass 8' / '# fail 0'; test/README.md contains exactly one occurrence of the filename; and the full unit suite glob passes with 0 failures and a total pass count 8 higher than the prior baseline (326 -> 334).","passed":true,"retries":0},{"id":"T7","attempts":2,"verdicts":[{"pass":false,"problems":["Booking created from the Instrument Profile silently preselects an unrelated project: applyRoute sets ctx.project to the instrument id, and case 'new-booking' forwards ctx.project as the project — verified in headless Chromium (instrument 1 -> project 1 selected). Money-relevant, no error surfaces.","Consequently the CHANGELOG bullet's claim of 'a New Booking button pre-set to that instrument' is true but incomplete — it is also pre-set to a wrong project."],"recommendation":"Fix then re-verify: in js/app.js `applyRoute`, scope the project id to its own route — `ctx.project = name === 'project' && id ? Number(id) : null;` (safe: only project/person/instrument routes pass an id, and neither detail view emits project-scoped actions other than the instrument profile's New Booking) — and add a browser assertion that the dialog opened from `#/instrument/<id>` has no project preselected while the instrument token is set.","evidence":"Independently verified in /home/user/Core-Facility-CRM (uncommitted working tree on top of 8c2f208).\n\nStatic/spec criteria — all met:\n- grep counts: `'#/instrument/'` = 1, `instrument: 'Instrument Profile'` = 1, `Views.instrumentDetail(ctx.instrument)` = 1, `location.replace('#/instruments')` = 1, `'instrument'` inside HASH_ROUTES = 0 (HASH_ROUTES is unchanged: dashboard, projects, people, instruments, calendar, reports, settings).\n- The prescribed node -e one-liner printed exactly `#/instrument/3 {\"name\":\"instrument\",\"id\":3} null`.\n- `for f in js/*.js; do node --check \"$f\"; done` exits 0.\n- `TZ=Asia/Jerusalem node --test 'test/unit/*.test.js'` → 334 pass / 0 fail; wiring.test.js 'data-nav completeness' now carries both the person and instrument HASH_ROUTES-absent / TITLES-present assertions (diff reviewed).\n- `TZ=Asia/Jerusalem node --test 'test/browser/smoke.spec.js'` → 18 pass / 0 fail / 0 skipped. Spec reporter confirms the two new tests actually ran and passed ('instrument profile renders, with its activity and training cards', 'a stale instrument id falls back to the Instruments list'), placed right after the project-detail test, and 'no JavaScript errors were logged while visiting every screen' still passes. Test-function count went 9 → 11 (+2) vs `git show HEAD:test/browser/smoke.spec.js`.\n- js/app.js diff is exactly the seven prescribed edits; `handleAct`/`switch (act)`/boot shapes untouched; no data-nav=\"instrument\" anywhere; `case 'new-booking'` (line 1494) does forward `el.dataset.inst`, and `case 'edit-instrument'` (1488) / `case 'edit-booking'` (1497) exist. CHANGELOG.md has the single required `### Added` bullet under [Unreleased]. test/README.md + test/unit/instrument-profile.test.js are indeed prior-task artefacts, untouched here.\n\nRuntime adversarial trace (headless Chromium, ?demo=1, TZ Asia/Jerusalem, reusing test/browser/helpers/browser.js) — REAL DEFECT:\nRouted to `#/instrument/1` (Leica SP8 FALCON) and clicked the profile's `[data-act=\"new-booking\"][data-inst=\"1\"]` button. The instrument token prefills correctly (\"×Leica SP8 FALCON\"), but the dialog's project select came up `value=\"1\"` → \"Intravital Multi-Photon Imaging of CAR-T Cell Infiltration\" — an unrelated project silently preselected, so a booking created from the Instrument Profile is billed to whatever project happens to share the instrument's id.\nCause: js/app.js `applyRoute` keeps the pre-existing `ctx.project = id ? Number(id) : null;` (line ~366), so on the instrument ro"},{"pass":true,"problems":[],"recommendation":"accept — note for the run: the release bump is still pending (CHANGELOG is under [Unreleased]; APP_VERSION / index.html ?v= / sw.js CACHE_VERSION all still 1.12.1), so the later version-bump task must land or this js/ change stays invisible behind the service worker.","evidence":"Inspected /home/user/Core-Facility-CRM/js/app.js, test/unit/wiring.test.js, test/browser/smoke.spec.js, CHANGELOG.md (git diff) and ran every stated criterion myself, plus independent browser traces.\n\nStatic criteria (all as specified): grep -c \"'#/instrument/'\" = 1; \"instrument: 'Instrument Profile'\" = 1; 'Views.instrumentDetail(ctx.instrument)' = 1; \"location.replace('#/instruments')\" = 1; HASH_ROUTES is unchanged (['dashboard','projects','people','instruments','calendar','reports','settings']) and contains no 'instrument' (grep count 0). No data-nav for instrument was added. handleAct/switch/boot shapes untouched (diff shows no changes there); `case 'new-booking'` still forwards el.dataset.inst (app.js:1494), `case 'edit-instrument'` (1488) and `case 'edit-booking'` (1497) exist; no new dispatcher case added.\n\nRouting shape: ctx line 6 now `{ route, project, person, instrument }`; hashFor:329, parseHash instrument branch:346-349 identical in shape to the person one; onHashChange stale-id guard:407-410; initRouting validity extended:421; renderView:444 after the person line. The planner's node -e harness printed exactly `#/instrument/3 {\"name\":\"instrument\",\"id\":3} null`. `for f in js/*.js; do node --check \"$f\"; done` exited 0.\n\nTests: TZ=Asia/Jerusalem node --test 'test/unit/*.test.js' → tests 334, pass 334, fail 0 (includes the js/*.js node --check suite and boot-ready-signal). TZ=Asia/Jerusalem node --test 'test/browser/smoke.spec.js' → tests 18, pass 18, fail 0; HEAD's spec had 10 `test(` blocks vs 12 now, i.e. 16 → 18 (+2), and the named ok lines include 'instrument profile renders, with its activity and training cards' (ok 10), 'a stale instrument id falls back to the Instruments list' (ok 11) and 'no JavaScript errors were logged while visiting every screen' (ok 18). wiring.test.js gained the instrument AND person asymmetry assertion pairs next to the project ones, and the data-nav completeness test passes.\n\nIndependent browser trace (headless Chromium over http, TZ=Asia/Jerusalem, ?demo=1): clicking the Instruments-list row (views.js:992/1004 data-goto=\"instrument\") set location.hash to '#/instrument/1', page title 'Instrument Profile', no [data-nav] marked active (correct, no nav entry), App.project === null, and the profile's New Booking button carries data-inst=\"1\"; clicking it opened the booking dialog with no stray project preselection and zero page/console errors. Deep-link reload to '#/instrument/<id>' renders the profile (11k chars); '#/in"}],"executorOutcome":"done","executorClaim":"All verification-criteria greps pass (instrument route wiring, hashFor/parseHash node -e check, node --check on all js/*.js files). Unit test suite: 334/334 pass including wiring.test.js data-nav completeness assertions. Browser smoke spec: 18/18 pass including the new 'instrument profile renders' and stale-id-fallback tests, and the zero-JS-errors test. Root cause of the reported failure (new-booking preselecting an unrelated project from the instrument route) fixed by scoping ctx.project assignment to name === 'project' only.","passed":true,"retries":1},{"id":"T8","attempts":1,"verdicts":[{"pass":true,"problems":["Nit (cosmetic, spec-mandated): the cancelled row gets class `row-retired`, but css/app.css only defines `.row-retired > td` (lines 1162-1163), a table rule — the agenda row is a div, so cancelled rows are not dimmed. Confirmed in the browser that the `Cancelled` badge does render, so the state is still visible; no behavioural defect.","Nit: a milestone due today now appears twice on the dashboard — once in 'Upcoming Milestones (Next 30 Days)' (due_date >= today) and again in 'Milestones Due Today'. This follows directly from the specified queries, not from an implementation mistake.","Out of this task's declared scope, but required before the change is visible to users: no ?v= / sw.js CACHE_VERSION / APP_VERSION bump was made (per cma-lessons, this must land in the release task or the service worker keeps serving the old views.js)."],"recommendation":"accept","evidence":"Inspected /home/user/Core-Facility-CRM/js/views.js (dashboard(), lines ~20-140), test/unit/dashboard.test.js and CHANGELOG.md diffs. `git status --short` / `git diff --stat`: exactly 3 files touched (CHANGELOG.md +1, js/views.js +58, test/unit/dashboard.test.js +66); js/app.js (openTodayModal) untouched.\n\nRan every stated criterion myself:\n- The literal node -e harness printed `true true false true false 1` (exit 0).\n- `grep -c toISOString js/views.js` = 4; `git show HEAD:js/views.js | grep -c toISOString` = 4 (unchanged). The new agendaDate uses `new Date().toLocaleDateString(DATE_LOCALE, …)` — display only, no toISOString, no UTC date derivation; DATE_LOCALE is genuinely exported from js/ui.js (line 613/1103) and destructured at views.js line 9.\n- `node --check js/views.js` exit 0.\n- `TZ=Asia/Jerusalem node --test test/unit/dashboard.test.js` → `# pass 6 / # fail 0`.\n- `TZ=Asia/Jerusalem node --test 'test/unit/*.test.js'` → 338 pass / 0 fail / 0 cancelled.\n- `TZ=Asia/Jerusalem node --test test/browser/smoke.spec.js` → 18 pass / 0 fail, including \"no JavaScript errors were logged while visiting every screen\" (real Chromium, not skipped).\n\nBeyond the criteria (per cma-lessons: \"looks correct\" is not verification), I loaded the served app in headless Chromium at TZ=Asia/Jerusalem (?demo=1, via test/browser/helpers/browser.js), inserted a booking dated UI.today() plus a cancelled one, and routed to the dashboard:\n- The card renders live: \"Today's Agenda — Thursday, September 17, 2026 / Bookings Today (2) / 10:00–11:00 BrowserCheck Today / Facility-wide / 14:00 BrowserCheck Cancelled / Facility-wide / Cancelled / Milestones Due Today (0) / Nothing due today …\".\n- Layout: card width 980px vs #view 1036px, i.e. genuinely full width outside the cols-2 grid (mt-16 used, no new CSS).\n- Clicking the row is wired end-to-end: `page.click('[data-act=\"edit-booking\"][data-id=N]')` opened the Edit Booking dialog with `#bke-title` = \"BrowserCheck Today\". (An earlier apparent failure was my harness: the guided tour's `.tour-blocker` + capture-phase tourEventGuard swallows clicks; with `UI.stopTour()` first the click works. `case 'edit-booking'` exists at js/app.js:1497.)\n- 0 pageerrors / 0 console errors throughout.\nSQL matches spec (LEFT JOIN + `m.project_id IS NULL OR p.is_archived=0` so facility-wide rows survive; msToday joins projects with is_archived=0); retired instruments/people get the \" (Retired)\" suffix via CASE as required. CHANGELOG bullet is the exact specifi"}],"executorOutcome":"done","executorClaim":"All verification criteria satisfied exactly: the literal node -e one-liner printed `true true false true false 1`; toISOString count in js/views.js unchanged at 4; node --check js/views.js exits 0; dashboard.test.js prints pass 6 / fail 0; full unit suite (test/unit/*.test.js) 338 pass / 0 fail; browser smoke suite (test/browser/smoke.spec.js) 18 pass / 0 fail including 'no JavaScript errors were logged while visiting every screen'. Only the three scoped files were touched (js/views.js, test/unit/dashboard.test.js, CHANGELOG.md) per `git status --short`.","passed":true,"retries":0},{"id":"T9","attempts":1,"verdicts":[{"pass":true,"problems":[],"recommendation":"accept — note only that this is a js/ change, so the run still owes the separate version-bump/CHANGELOG task (index.html ?v=, sw.js CACHE_VERSION+PRECACHE_URLS, js/consts.js APP_VERSION) before the demo roster appears in a cached browser.","evidence":"Read /home/user/Core-Facility-CRM/docs/cma-lessons.md first, then inspected the actual diff (`git diff js/db.js` — 32 insertions, 4 deletions, the only modified file in the tree) rather than trusting the report.\n\nScope: every hunk is inside `seedSampleData` (peopleData ~L2849, grantUserPairs ~L2915, project_people ~L3007-3021, #47 campus/mobile fence ~L3408-3416). No bookings, training rows, tags or booking #1 touched — confirmed by the diff containing no changes near the trainingRows/meetings/tag blocks.\n\n(1) peopleData: Alex Chen 'Researcher'->'Postdoc', Maya Patel 'Researcher'->'PhD'. Nine rows appended after Tom Alvarez in the required order with the required names/types/orgs/departments, is_staff 0, rate 0, lab-matching fictional emails (lokafor@harvard.edu, jreyes@harvard.edu, radler@harvard.edu, shaddad@mit.edu, ytanaka@mit.edu, gmbeki@mit.edu, iferreira@stanford.edu, nlevi@stanford.edu, bcarter@stanford.edu) and one-sentence notes.\n(2) Types checked against js/consts.js `PERSON_TYPES: ['Undergrad','MSc','PhD','Postdoc','Technician','PI','Facility Staff','Other']` — all nine plus the two edits are valid values.\n(3) project_people: 1<-9 Postdoc / 10 Student / 11 Technician; 2<-12 Postdoc / 13 Student / 14 Technician; 3<-15 Postdoc / 16 Student / 17 Student. All three role strings are in `CONST.ROLE: [... 'Postdoc','Student','Technician' ...]`.\n(4) grantUserPairs: [1,9],[1,10],[2,12],[3,16] added. Comment-vs-row audit (the named lesson): 9=Dr. Lena Okafor, 10=Jonah Reyes, 12=Dr. Samir Haddad, 16=Noa Levi — every comment matches its row. The inline `// Dr. Lena Okafor` etc. comments on the project_people lines also all match.\n(5) #47 fence: campus 'Longwood' IN (9,10,11), 'Cambridge' IN (12,13,14), 'Palo Alto' IN (15,16,17); mobiles '+1 617 555 0211' (id 9) and '+1 617 555 0233' (id 12), matching the existing '+1 617 555 0xxx' style. No seed comment counts people (grepped for numeric/word people counts — none exists), so nothing to fix there.\n\nRan the criteria myself:\n- `node --check js/db.js` -> exit 0.\n- Verification harness printed exactly `17 Postdoc,PhD 16 9 0 true 3 589.95 14` (byte-identical to the required string: 17 people, ids 4/5 Postdoc,PhD, 16 project_people, 9 grant_users, 0 blank campuses, all types valid, 3 staff, booking #1 total_cost 589.95 unchanged, 14 meetings).\n- `TZ=Asia/Jerusalem node --test 'test/unit/*.test.js'` -> tests 338, pass 338, fail 0, skipped 0 (so training.test.js's 9-training-rows and booking-tags seed assertions re"}],"executorOutcome":"done","executorClaim":"Verification command output matched exactly: `17 Postdoc,PhD 16 9 0 true 3 589.95 14`. node --check js/db.js exits 0. Full unit suite: 338 pass, 0 fail (includes training.test.js's 9-training-rows assertion and booking-tags seed assertions, both untouched by this task).","passed":true,"retries":0},{"id":"T10","attempts":1,"verdicts":[{"pass":true,"problems":["Nit (not a criterion, and it follows existing precedent in the same seed): B5, B12 and B17 carry a projectId but omit groupOrg, though the task text said groupOrg should be the project's lab. Effect is only that Bio-Photonics Lab's 5% standing discount / group tier does not apply to those two staff-time consults; pre-existing seed consults (ids 2, 3) do the same, so nothing regressed.","Nit: grantId is set only on B1/B2/B3 and omitted on the other 14 project-linked bookings. The pre-existing calls are equally mixed, so this is consistent with 'as the existing calls do', but it is not uniform per project.","Nit (plausibility only): B10 is Noa Levi's Glacios session on day(-12) while her Glacios training row is dated day(-11) — trained the day after the session she ran.","Observation, out of this task's declared file scope: js/db.js changed but no version bump (index.html ?v=, sw.js CACHE_VERSION/PRECACHE_URLS, js/consts.js APP_VERSION). Per CLAUDE.md and the lessons file this must land in a later task of the run, or the demo change will be invisible behind the service worker."],"recommendation":"accept","evidence":"Inspected the real diff (git diff: only /home/user/Core-Facility-CRM/js/db.js +281, test/unit/training.test.js ±4, CHANGELOG.md +1 — scope respected) and re-ran every criterion myself.\n\nStatic: `grep -c 'seedBooking({' js/db.js` → 31. `node --check js/db.js` → exit 0. The awk seed-order check printed SEED-ORDER-OK (the `// --- 1.13 begin ---` fence follows `// --- #41 end ---`; the diff shows `// --- 1.13 end ---` immediately before `markDirty();`, inside seedSampleData).\n\nThe required node -e one-liner printed exactly `31 2 0 true 490 546.25 589.95 1 16 1 0 true` — i.e. 31 meetings, 2 today (2026-09-17), 0 instrument/staff overlaps anywhere in the dataset (covers new-vs-new and new-vs-existing), booking #1 still the min id with 490/546.25/589.95, 'workshop' category ref count still 1, 16 training rows with exactly one NULL trained_on, 0 bookings with an attendees string but no meeting_people rows, and ≥4 tags with count>1.\n\nDumped all 31 meetings: new ids 15–31 match the B1–B17 spec on date offsets, times, instruments (1 Leica, 2 FV3000, 3 Zeiss, 4 Nikon, 5 Glacios amount 4), staff (6 Kim / 7 Anand / 8 Alvarez / none for B9), attendee sets, projects and categories (only consult / training / assisted session — no 'workshop'). Every booking uses peopleIds; all attendees strings are populated and every one has matching meeting_people rows. bookingTagCounts: Fiji 7, Napari 7, STED 6, Lightsheet 3, Grant Planning 1, Workshop 1 — only the pre-existing vocabulary, and 10 new bookings are tagged (the trio shares one title/UPDATE).\n\nRegression check against HEAD: re-seeded with `git show HEAD:js/db.js` and diffed the first 14 bookings' tags, subtotals and totals — byte-identical (IDS-1-14-IDENTICAL), so none of the `UPDATE ... WHERE title=?` statements clobbered an existing booking's tags (only 'Weekly Calcium Imaging Block' is a duplicate title, and it is the intended trio). Baseline training count was 9 → now 16.\n\nTraining rows: the 7 new rows (p9/i2 2026-07-19 by6; p10/i1 2026-08-08 exp 2027-08-08; p11/i1 2026-08-18 by8; p12/i4 Super User 2026-04-20 with the specified note; p13/i4 2026-08-15; p15/i3 2026-08-21 exp 2027-08-21; p16/i5 2026-09-06 by7) match the spec exactly, all with non-null trained_on and no past expiry; the single pre-existing NULL trained_on row survives.\n\nComment accuracy (a known repo failure mode): checked every in-block comment against the seeded people (9 Dr. Lena Okafor, 10 Jonah Reyes, 11 Ruth Adler, 12 Dr. Samir Haddad, 13 Yuki Tanaka,"}],"executorOutcome":"done","executorClaim":"All verification criteria from the task were reproduced exactly: grep count 31 seedBooking calls; the required node -e one-liner printed the exact expected string '31 2 0 true 490 546.25 589.95 1 16 1 0 true'; the awk seed-order check printed SEED-ORDER-OK; node --check js/db.js exited 0; the full unit suite (338 tests) passed with 0 failures including the updated training.test.js '16 training rows' test; the full browser suite (71 tests) passed with 0 failures, no new failures introduced.","passed":true,"retries":0},{"id":"T11","attempts":1,"verdicts":[{"pass":true,"problems":["Minor, not a criterion: the doc is 312 lines against the task's stated '~150-250 lines' target, so it overshoots the requested length by roughly 25%. Content is on-topic throughout, no padding worth rejecting for.","Minor: the '## ' section count is exactly 8, the stated minimum, so any future trim of a section would break the criterion; there is no margin."],"recommendation":"accept","evidence":"Read /home/user/Core-Facility-CRM/docs/cma-lessons.md first, then independently ran every stated criterion from the repo root rather than trusting the report.\n\nMechanical checks (all run by me):\n- `test -f docs/plans/extensions-and-importers-concept.md` -> EXISTS (untracked new file per `git status`).\n- `grep -c '^## '` -> 8 (>=8, exactly at the bound).\n- `grep -c '\\[Unverified\\]'` -> 9 (>=4).\n- `grep -c 'Plugins.register'` -> 3 (>=2).\n- `grep -c 'findBookingConflicts'` -> 2 (>=1).\n- `grep -c 'extensions-and-importers-concept.md' ROADMAP.md` -> 1; the awk section probe printed LINK-IN-SECTION. `git diff -- ROADMAP.md` shows exactly one added line (plus blank), placed immediately after the \"[Unverified] Vendor export formats have not been inspected yet.\" paragraph and before \"## Tier 5\", with the byte-exact link text the task specified.\n- `TZ=Asia/Jerusalem node --test 'test/unit/*.test.js'` -> tests 321, pass 321, fail 0, skipped 0. No test threw before its assertions (125 suites all ok), so the lessons-file \"a test that never ran\" failure mode does not apply here.\n\nProtected-files claim independently checked, not accepted: `git diff --stat -- js css index.html sw.js CHANGELOG.md` shows CHANGELOG.md, js/app.js, js/consts.js, js/db.js modified. I read the full diff content: it is entirely the training-level vocabulary change (TRAINING_LEVELS 'User' -> 'Regular', PERSON_TYPES + 'Facility Staff', an idempotent `UPDATE person_instrument_training SET level='Regular'` migration, seed rows, and a matching [Unreleased] CHANGELOG entry). `grep -rn 'Plugins|extensions-and-importers' js/ index.html sw.js css/` returns nothing, so none of that diff is attributable to T11; it belongs to the concurrent training task. `test/unit/training.test.js` is also modified, same other task.\n\nContent verification (read the file end to end, 312 lines): the 8 `##` sections appear in the specified order — Why this document; What an extension can and cannot do in a client-only PWA; Proposed registry: `window.Plugins.register`; Importer notes per target; Mapping onto existing entities; Duplicate and conflict rules; Dates and money; Open questions. The possible/not-possible lists cover SheetJS+Blob file in/out, mailto: hand-off citing Email Attendees, PKCE public-client OAuth, and on the negative side server-held secrets, push, scheduled jobs with the tab closed and server-to-server sync, explicitly quoting CLAUDE.md's \"no backend, no server, no accounts/auth\" and the \"major, explicitly-"}],"executorOutcome":"done","executorClaim":"All verification criteria satisfied: file exists; 8 '## ' sections; 9 [Unverified] tags (>=4); 3 mentions of Plugins.register (>=2); 2 mentions of findBookingConflicts (>=1); ROADMAP.md has exactly 1 mention of the link and it falls within the Migration/import tool section (LINK-IN-SECTION printed); git diff --stat for js/css/index.html/sw.js/CHANGELOG.md shows only pre-existing unrelated changes (app.js, consts.js, db.js, CHANGELOG.md) that were already present before this task began (I did not touch any of those files - only docs/plans/*.md and ROADMAP.md were edited); unit test suite passes 321/321 tests, 0 failures, unchanged from before.","passed":true,"retries":0},{"id":"T12","attempts":1,"verdicts":[{"pass":true,"problems":["docs/manual/dashboard.html new paragraph names the wrong control: \"The Today button on the Calendar screen still opens the fuller dialog\". In js/views.js:1357 the dialog is opened by a separate button labelled Agenda (data-act=\"open-today-modal\"); Today is data-act=\"cal-today\", which only resets the grid offset. bookings.html:51 and rough-edges.html:51 already state this correctly, so the manual now contradicts itself. Mitigating: the surrounding dashboard.html section is pre-existing stale prose making the same claim, which the new sentence echoes via \"described above\".","docs/manual/people-labs.html renders the spec clause \"the profile shows one coloured role pill\" as a claim about training level instead: \"shows their level for each instrument as a single coloured pill\". The actual role pill is typeBadge() in js/views.js (PI=primary, Facility Staff=success, else neutral) and is not described; the training-level badge is always `badge primary`, i.e. not coloured by level. That sub-requirement is effectively unmet.","Minor: people-labs.html says of the rename \"nothing was lost or renamed in your data\", whereas db.js:670 does rewrite the stored value in place (`UPDATE ... SET level='Regular' WHERE level='User'`). The user-visible outcome stated is correct, but the reassurance is technically false.","Cosmetic: the new In-detail blocks are feature / feature reverse / feature, and the following pre-existing block is also plain `feature`, so two non-reversed blocks now sit adjacent. The pre-existing section already had this pattern later on, so no rule is broken."],"recommendation":"accept — every listed criterion is met (the two that print unexpected numbers are faulty as written: the `modal` count of 5 is a verified pre-existing baseline from \"Modality\"/`21-new-booking-modal.png`, and the manual.js?v= check needs `-h`), and all feature prose checks out against js/. Follow-up nit for a later docs pass: change dashboard.html's \"The Today button\" to \"the Agenda button next to Today\" and rewrite the people-labs.html pill sentence to describe the person's coloured role pill (PI / Facility Staff / other) rather than the training level.","evidence":"Ran every listed criterion myself in /home/user/Core-Facility-CRM:\n- `grep -c '1.13.0' docs/index.html` = 5 (≥3 ✓); `grep -c 'screenshots/7[345]-' docs/index.html` = 6 ✓; `grep -c 'User or Super User'` = 0 and `'Regular or Super User'` = 1 ✓; `ls docs/screenshots | sort -V | tail -1` = 72-research-output-detail.png (no 73–75 fabricated) ✓; `id=\"instrument-profile\"` in instruments.html = 1 ✓; `id=\"instrument-training\"` in people-labs.html = 1, `grep -c Regular` = 2 ✓; `git diff --stat -- js css index.html sw.js CHANGELOG.md` = empty ✓; manual.js/manual.css are `?v=3` in all 18 manual pages, unchanged ✓; no `ch-kicker` touched ✓.\n- `TZ=Asia/Jerusalem node --test test/unit/versions.test.js` → 8/8 pass; full unit suite `test/unit/*.test.js` → 338/338 pass, 0 fail.\n- Two criteria are unsatisfiable as literally written, and I confirmed the executor's explanation rather than relaying it: `grep -ci modal` over the four manual pages sums to 5, and `git show HEAD:docs/manual/<f>.html | grep -in modal` proves all 5 are pre-existing (\"Pick a Modality\", the `12-instruments.png` alt text, `21-new-booking-modal.png` img+placeholder, \"a new Modality or Department\"). The added diff contains zero hits for modal/schema/join table/denormaliz/boolean/foreign key. The `grep -o 'manual.js?v=...' docs/manual/*.html | sort -u | wc -l` criterion prints 18 only because GNU grep prefixes filenames with multiple file args; with `-h` it prints 1.\n- docs/index.html diff: hero lede rewritten for 1.13.0 covering all six required points; `#hero-release` and `#footer-version` static fallbacks untouched; three 1.13.0 Highlights cards prepended immediately before the 1.12.0 \"Tags on bookings\" card in the exact `<div class=\"card\"><div class=\"ic\">…` pattern; 1.12.0 person-profile card now reads \"(Regular or Super User)\"; three \"In detail\" blocks added at the top of that section, byte-for-byte matching the existing block markup (figure.shot / img loading=lazy / identical onerror `this.closest('.shot').classList.add('pending')` / `<div class=\"ph\"><span class=\"badge\">Snapshot pending</span><span>Save as <code>…</code></span></div>` / figcaption).\n- Tag/section balance check (div/section/figure/p) over docs/index.html and all manual pages: balanced.\n- Adversarially verified every substantive doc claim against js/ (lessons file: never verify docs against CHANGELOG): instrumentDetail in js/views.js:1104 exists with Utilization tiles for this month/this year (Bookings, Booked Hours, Distinct Users, Ch"}],"executorOutcome":"done","executorClaim":"All docs-only files (docs/index.html, docs/manual/{instruments,dashboard,people-labs,bookings}.html); git diff --stat over js/css/index.html/sw.js/CHANGELOG.md is empty, confirming no app-shell files were touched (correctly no version bump). All manual.js/manual.css remain v=3 (unchanged) across every manual page. `TZ=Asia/Jerusalem node --test test/unit/versions.test.js` passes (8/8). Verified each criterion:\n- grep -c '1.13.0' docs/index.html -> 5 (>=3, satisfied)\n- grep -c 'screenshots/7[345]-' docs/index.html -> 6 (satisfied)\n- grep -c 'User or Super User' -> 0, 'Regular or Super User' -> 1 (satisfied)\n- ls docs/screenshots | sort -V | tail -1 -> 72-research-output-detail.png, unchanged; no 73-75 files fabricated (satisfied)\n- id=\"instrument-profile\" in instruments.html -> 1 (satisfied)\n- id=\"instrument-training\" in people-labs.html -> 1 (satisfied); \"Regular\" count -> 2 (>=1, satisfied)\n- manual.js?v= is 3 in every one of the 18 manual pages (verified with `sort -u` per-line values, all identical); note the literal verification command `grep -o ... docs/manual/*.html | sort -u | wc -l` prints 18 rather than 1 because GNU grep prefixes each match with its filename when given multiple file arguments (unrelated to this task — a `-h` flag would show the intended count of 1). Not something this task's edits caused or can fix without changing the check itself.\n- `grep -ci 'modal' ...` sums to 5, not 0. Confirmed via `git stash`/`git stash pop` that this baseline of 5 pre-dates","passed":true,"retries":0},{"id":"T13","attempts":1,"verdicts":[{"pass":true,"problems":["Three numeric criteria are faulty as written rather than unmet by this task: unit pass 338 vs '>=340' and browser pass 71 vs '>=72' are planner estimates of how many tests T1/T2/T6/T8 would add (actual growth +20 and +6, zero failures anywhere), and the restore one-liner's expected 'true 0 14' assumed 2 Super User seed rows where js/db.js seeds 3 (16 total training rows, so 13 Regular). None indicate a defect in the four files T13 owns, and none could be fixed inside this task's scope.","Minor: the executor reported outcome 'blocked' when the in-scope work is in fact complete and correct — the block is purely these three estimate mismatches."],"recommendation":"accept — the release plumbing is exactly right and every substantive check passes; treat the 340/72/14 numbers as planner-estimate errors and record the true figures (unit 338/0, browser 71/0, restore \"true 0 13\") as the new baseline.","evidence":"Independently re-ran every criterion from /home/user/Core-Facility-CRM.\n\nStatic (all exact): grep -c '?v=1.13.0' index.html = 8; leftover ?v=1.12.1 across index.html+sw.js = 0; \"CACHE_VERSION = '1.13.0'\" in sw.js = 1; \"?v=1.13.0\" in sw.js = 8; \"APP_VERSION = '1.13.0'\" in js/consts.js = 1; '^## \\[Unreleased\\]' in CHANGELOG.md = 0; first '## [' heading is line 6 \"## [1.13.0] — 2026-09-17\". od -c confirms the dash is 342 200 224 (em dash), byte-identical in form to the 1.12.1 heading on line 19. git diff shows only the heading line changed in CHANGELOG.md; diffstat is exactly the 4 in-scope files (index.html 16 lines, sw.js 18, consts.js 2, CHANGELOG.md 2) and `git status --short` lists only those 4 modified files with no untracked files (earlier tasks T1-T12 are committed; I removed the temporary baseline worktree I created under the scratchpad).\n\n`for f in js/*.js; do node --check \"$f\"; done` exits 0.\n\nCHANGELOG claims checked against js/ (not against prose): PERSON_TYPES in js/consts.js:26 contains 'Facility Staff' and is consumed by the person dialogs (js/app.js:1589/2150/2216 via vocabField) and by the People filter (js/views.js:746) — claim true. TRAINING_LEVELS = ['Regular','Super User'] (consts.js:27); js/db.js:670 runs \"UPDATE person_instrument_training SET level='Regular' WHERE level='User'\" inside migrate(), which runs on boot (db.js:761) and on restore (proven empirically below) — \"on load and on restore\" true. DB.bookingTagCounts (db.js:2044) selects all meetings with tags with no is_cancelled filter — \"cancelled ones included\" true. mountTagPicker is called from the single shared mountBookingModal (app.js:3691), which both the new-booking (app.js:3853) and edit-booking (app.js:4066) dialogs mount — \"both booking dialogs\" true. views.js:1201/1203 render \"This Month\"/\"This Year\" tiles on the instrument profile; views.js:101 renders the \"Today's Agenda\" dashboard card. Demo data: 31 meetings, 2 dated today, people 5/5/4 in the three research labs plus 3 facility staff, 16 training rows — consistent with the T10 bullets.\n\nSuites: unit 338 pass / 0 fail (baseline at a106b75 re-measured in a clean worktree = 318, so +20 real added tests); browser 71 pass / 0 fail / 0 skipped (baseline 65, +6; tag-picker.spec.js is new and its /×\\d+/ usage-count assertion inside the #bk-tags dropdown ran and passed).\n\nRestore migration one-liner (FileReader stub per backup-roundtrip.test.js): printed \"true 0 13\". I extended it to print the total: 16 training rows, grou"}],"executorOutcome":"blocked","executorClaim":"All checks strictly scoped to the 4 files in this task pass exactly as specified: 8/8 ?v=1.13.0 in index.html, 0 leftover ?v=1.12.1 across index.html+sw.js, CACHE_VERSION and all 8 PRECACHE_URLS bumped in sw.js, APP_VERSION bumped in consts.js, CHANGELOG's top heading is exactly '## [1.13.0] — 2026-09-17' with no remaining '## [Unreleased]', Added-before-Changed order preserved, and every Added/Changed claim verified true against current js/ (grepped PERSON_TYPES, TRAINING_LEVELS, Today's Agenda markup, Instrument Profile route label, tag-picker code). node --check passes on every js/*.js file. Headless-Chromium rendering of dashboard, instrument profile (#/instrument/2), person profile (#/person/6), and the New Booking tag-count dropdown all render correctly with zero page/console errors.\\n\\nThree verification-criteria numbers fall short, and none stem from anything in this task's scope (I did not touch js/db.js or any test file, as instructed):\\n- Unit suite: pass 338, fail 0 (criterion requires pass >= 340). All 338 tests that exist pass; the estimated +22 over the 318 baseline from T1/T2/T6/T8 materialized as +20 instead.\\n- Browser suite: pass 71, fail 0 (criterion requires pass >= 72). Same shape: the estimated baseline+3 came in as baseline+2.\\n- Restore-migration one-liner (run verbatim, including the FileReader stub copied from test/unit/backup-roundtrip.test.js lines 17-25 as the task allows): printed \\\"true 0 13\\\" instead of the expected \\\"true 0 14\\\" — i.e. the de","passed":true,"retries":0}],"orchestratorNotes":["Environment: Linux container (Claude Code on the web), Node 22, python3 present. cma-run was not installed; cloned from GitHub and installed with `bash install.sh` (install.sh lacks the executable bit).","cma-plan.js failed once with HTTP 400 ‘Invalid effort level’: Fable only accepts effort medium in this environment. Fixed in the generated planner script (fable call effort: medium); the installed cma-plan.js still says high and should default to medium for fable or be configurable.","Prompts in cma-execute.js/cma-plan.js/cma-learn.js build the lessons path with Windows backslashes (${repoPath}\\\\docs\\\\cma-lessons.md); replaced with forward slashes in the generated scripts. Agents found the file anyway, but the path should be OS-neutral.","Execution was split into one generated workflow script per dependency batch (10 scripts) so every verified batch was committed and pushed before the next started. No data loss.","Retries: 1 of 13 tasks (T7). The verifier found a real pre-existing bug the task exposed: applyRoute set ctx.project for any route carrying an id, so New Booking from an instrument or person profile preselected a bogus project. The retry fixed it.","Orchestrator post-verify fixes after re-scanning accepted problems (lesson 30): T2 ordering test had no same-count tiebreak (strengthened); T5 notePreview used UI.noteHtml, which needs DOMParser and would throw under Node when a booking has a note (rewritten to strip tags from the stored string); T8 row-retired on a div did not dim (CSS rule added); T10 one training date fell after the session it enabled (moved); T12 three manual sentences were wrong on inspection (wrong button name, role pill described as training level, ‘nothing renamed’ while the migration renames rows).","T12 executor used `git stash` / `git stash pop` to establish a baseline despite the no-tree-mutation rule. Nothing was lost (stash list empty, tree intact), but the rule was violated; executor prompts should name the read-only alternative (`git show HEAD:<path>`).","Planner criteria faulty as written in 4 places: T4 sed-range data-act count (range included unrelated pre-existing functions), T12 grep -ci modal (pre-existing ‘Modality’ and a screenshot filename) and manual.js?v= count (needs grep -h), T13 numeric thresholds (unit >=340, browser >=72, restore 14) were estimates of other tasks’ outputs. Verifiers judged intent correctly in every case and stated so.","Verifiers went beyond criteria consistently: headless Chromium harnesses (T4, T5, T7, T8, T13), mutation testing of new tests (T6: 13 mutations), byte-identical re-seed diff against HEAD (T10), doc claims checked against js/ (T12, T13).","Final suites after T13: unit 338/0 (baseline 318), browser 71/0 (baseline 65). 13/13 verified, 1 retry."]},"repoPath":"/home/user/Core-Facility-CRM"}
if (typeof input === 'string') {
  try {
    input = JSON.parse(input)
  } catch (err) {
    throw new Error('cma-learn: args arrived as a string that is not valid JSON: ' + (err && err.message ? err.message : err))
  }
}
const runReport = input && input.runReport
const repoPath = input && input.repoPath

if (!runReport) {
  throw new Error('cma-learn: provide args.runReport (the execute run results + orchestrator notes)')
}
if (!repoPath) {
  throw new Error('cma-learn: provide args.repoPath (absolute path of the repo)')
}

const LEARN_SCHEMA = {
  type: 'object',
  additionalProperties: false,
  required: ['lessons_added', 'lessons_updated', 'lessons_removed', 'no_changes', 'notes'],
  properties: {
    lessons_added: { type: 'array', items: { type: 'string' } },
    lessons_updated: { type: 'array', items: { type: 'string' } },
    lessons_removed: { type: 'array', items: { type: 'string' } },
    no_changes: { type: 'boolean' },
    notes: { type: 'string' },
  },
}

const LEARN_PROMPT = `You are the LEARNING stage of a plan-gated, multi-model build pipeline (Fable plans, a human gates, Sonnet executes, Opus verifies, you distill). Your job: update the pipeline's shared lessons file so the OTHER agents learn which practices succeeded, which failed, and WHY.

TARGET REPOSITORY (absolute path): ${repoPath}
LESSONS FILE (the only file you may edit): ${repoPath}/docs/cma-lessons.md

THE RUN REPORT (evidence — executor claims, verifier verdicts, retries, orchestrator notes):
${JSON.stringify(runReport, null, 2)}

Method:
1. Read the existing lessons file FULLY first. Follow its own maintenance rules (merge/dedupe, "seen N×" on recurrence, ~150-line cap, evidence-dated lessons, practice + evidence + mechanism format, sections A-E).
2. Mine the run report for practice-level causes: why did tasks pass first-try or need retries? What did verifiers catch or miss? Did anything about prompts, schemas, task sizing, batch ordering, or verification style help or hurt?
3. MERGE into the file: strengthen recurring lessons, add genuinely new ones, revise or remove lessons this run's evidence contradicts. A clean run with nothing new means NO edit — set no_changes true.
4. Never edit anything except docs/cma-lessons.md. No product code, no workflows, no agent definitions, no git/deploy commands.

Report via the structured output. Your StructuredOutput call MUST include ALL FIVE fields in one object — omitting any is a schema error that wastes a retry:
- lessons_added (array of strings — [] when none)
- lessons_updated (array of strings — [] when none)
- lessons_removed (array of strings — [] when none)
- no_changes (boolean)
- notes (string — "" when nothing to flag; use it for anything that implies a workflow-script or agent-definition fix, which the orchestrator owns)`

phase('Learn')

const outcome = await agent(LEARN_PROMPT, {
  label: 'learn:opus',
  model: 'opus',
  agentType: 'cma-learner',
  schema: LEARN_SCHEMA,
})

if (!outcome) {
  throw new Error('cma-learn: the learner agent returned nothing.')
}

log(
  outcome.no_changes
    ? 'Learner: no changes — the run added no new evidence.'
    : `Learner: +${outcome.lessons_added.length} added, ~${outcome.lessons_updated.length} updated, -${outcome.lessons_removed.length} removed.`
)
return outcome
